---
phase: 07-data-fixes
plan: 11
type: execute
wave: 11
depends_on:
  - 07-10
files_modified:
  - packages-user/data-base/src/hero/equipStore.ts
  - packages-user/data-base/src/flag/system.ts
  - packages-user/data-base/src/hero/state.ts
  - packages-user/data-base/src/hero/follower.ts
  - packages-user/data-base/src/hero/types.ts
  - packages-user/data-base/src/hero/equipStore.test.ts
  - packages-user/data-base/src/hero/follower.test.ts
  - packages-user/data-base/src/hero/saveLoad.test.ts
  - packages-user/data-base/src/flag/system.test.ts
  - packages-user/data-state/test/saveablesRoundTrip.test.ts
autonomous: false
requirements:
  - FIX-01
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "`HeroEquipsStore.loadState` 不再清空重建：同一 uid 的 `EquipmentState` 在读档前后为**同一实例**（`===` 成立），外部从 `get`/`instancesOf`/`instances` 持有的引用跨读档仍指向活 store 中的实例（`#06-17-4` / 审计 D 关闭）"
    - "`FlagSystem.loadState` 不再清空重建：同一 key 的 `FlagCommonField` 在读档前后为**同一实例**，外部从 `getField`/`getOrInsert` 持有的引用跨读档仍读写到活字段（`#06-17-5` / 审计 E 关闭）"
    - "`HeroState.loadState` 不再 `removeAllFollowers()` 全量重建：按裁决口径保留的 `IHeroFollower` 实例在读档前后为同一实例，其 `location`/`rendering` 原地读档（`#06-17-6` / 审计 F 关闭）"
    - "保留实例后状态仍以存档为准：读档后的值/修饰器/槽位/位置与三档压缩的存档点一致（同引用不改变恢复结果）"
    - "存档中不存在而内存中存在的实例（装备实例 / flag 字段 / follower）按 Task 0 裁决处置并有可观测断言"
    - "`pnpm test:ci` 全绿，不新增 `it.skip`、不弱化既有断言；`hero/state.test.ts`、`flag/saveLoad.test.ts`、`hero/equipment.test.ts` 等既有用例不改写（Task 0 裁决要求调整的除外）"
  artifacts:
    - path: packages-user/data-base/src/hero/equipStore.ts
      provides: "`HeroEquipsStore.loadState` 改为「按 uid 复用现有实例 + `instance.loadState(...)`」，多余实例按裁决销毁；`nextUid` 语义保持（IN-02 不在本计划范围）"
    - path: packages-user/data-base/src/flag/system.ts
      provides: "`FlagSystem.loadState` 改为「按 key 复用现有 `FlagCommonField` + `fromStructured`」，多余字段按裁决删除"
    - path: packages-user/data-base/src/hero/state.ts
      provides: "`HeroState.loadState` 的 followers 段改为保留式恢复（调用 `HeroFollowersController` 的保留式入口或既有个体 `loadState`），不再 `removeAllFollowers()` 全量重建"
    - path: packages-user/data-base/src/hero/follower.ts
      provides: "`HeroFollowersController` 的保留式恢复入口（按 Task 0 裁决：索引对齐复用 / 槽位替换 + 钩子语义），`HeroFollower` 未变或按裁决最小变更"
    - path: packages-user/data-base/src/hero/types.ts
      provides: "仅当 Task 0 裁决需要时：`IHeroFollowersController` 新增保留式恢复成员的声明与 jsDoc（接口源头、中文）；`IHeroFollower` 契约如按裁决变更亦在此处"
    - path: packages-user/data-base/src/hero/equipStore.test.ts
      provides: "`#06-17-4` 回归：同 uid 实例跨 `loadState` 保持同一实例且读档后值与修饰器为存档点"
    - path: packages-user/data-base/src/flag/system.test.ts
      provides: "`#06-17-5` 回归：同 key 字段跨 `loadState` 保持同一实例且读档后值/结构化内容为存档点"
    - path: packages-user/data-base/src/hero/follower.test.ts
      provides: "`#06-17-6` 回归：按裁决口径的 follower 实例保留与位置/渲染恢复"
    - path: packages-user/data-base/src/hero/saveLoad.test.ts
      provides: "三档压缩下 `hero.loadState` 的装备实例 / follower 实例保真回归（与既有容器往返用例并存）"
    - path: packages-user/data-state/test/saveablesRoundTrip.test.ts
      provides: "顶层 `CoreState.loadState` 的跨读档同引用集成断言（装备实例、flag 字段、follower 实例）"
  key_links:
    - "`HeroEquipsStore.loadState`（`equipStore.ts:267-288`）的 `instanceMap.clear()`（`:271`）+ `new EquipmentState(...)`（`:278`）↔ 外部 `get`/`instancesOf`（`:201-253`）持有的实例——复用实例后二者指向同一对象"
    - "`FlagSystem.loadState`（`flag/system.ts:65-72`）的 `fieldMap.clear()`（`:66`）+ `new FlagCommonField(...)`（`:68`）↔ 外部 `getField`/`getOrInsert`（`:16-35`）持有的实例"
    - "`HeroState.loadState` 的 followers 段（`state.ts:148-152`）的 `removeAllFollowers()`（`:148`）+ `addFollower`（`:150`）↔ `HeroFollowersController.followers`（`follower.ts` 私有数组）与外部缓存的 `IHeroFollower`"
    - "`HeroFollower.loadState`（`follower.ts` 中 `rendering.loadState` + `location.loadState`）——两个子对象本就原地读档，保留实例后无需新逻辑"
    - "`HeroFollowersController.removeAllFollowers`（`follower.ts`）与 `removeFollower` 的 `onRemoveFollower` 钩子 ↔ 渲染端跟随者同步：保留式恢复是否仍发钩子由 Task 0 裁决决定"
    - "`EquipmentState` 的 `item`（`equipStore.ts:16`）为 `readonly` 且构造期由 `itemStore.getData(save.num)` 决定 ↔ 同 uid 但 `save.num` 不同的存档（理论上不应出现）：复用前必须比对 `item.num`，不同则按裁决处置"
  assumptions:
    - "A1（D-10 例外，本计划授权）：本计划为修复计划，为 `#06-17-4/5/6` 新增同引用回归是必需项；既有断言的改写仅限 Task 0 裁决要求的情形，并逐条在 SUMMARY 说明"
    - "A2（同引用原则延续）：07-09 已为 `HeroAttribute` 确立「存读档在自身实例上原地完成」的原则；本计划把同一原则推广到装备实例、flag 字段与 follower 三类容器，是否逐子系统保留由 Task 0 裁决"
    - "A3（项目未发布，与 07-09 的 A2 同口径）：不承诺读回仓外旧存档；本计划不新增存档字段、不改存档形状，除非 Task 0 裁决明确要求"
    - "A4（同步语义）：`HeroState.loadState` 是同步方法，`HeroFollowersController.removeAllFollowers`/`removeFollower` 是 `async` 但方法体无 `await`（`follower.ts`），故保留式恢复可在同步读档路径内完成；实现不得改为异步"
    - "A5（范围守卫）：仅修 `#06-17-4/5/6`（审计 D/E/F）；review 的 IN-02（空存档不重置 `nextUid`）与本计划无关，只登记不修"
  prohibitions:
    - "不修 `#06-17-7`（`data-fallback` hero Proxy）与 `#06-17-8`（`mapLayer.setMapRef`）——属 07-14 / 07-13"
    - "不修 CR-01/CR-02 与 replay 编解码（07-10 / 07-13）"
    - "不改 `HeroAttribute` 的存读档行为（07-09 已确立）、不改 `HeroItems.loadState`（审计判定为安全原地读档）、不改 `HeroLocation`/`HeroRendering` 的读档实现"
    - "不改 `data-state/src/core.ts` 的 saveables 顺序与顶层存读档接线；不改渲染端 `@user/client-*` 与 legacy 接线"
    - "不新增存档字段、不改 `IHeroStateSave`/`IHeroEquipsStoreSave`/`IFlagSystemSave` 形状（除非 Task 0 裁决明确要求）"
    - "不引入新依赖、不新建源码文件、不新增 `import type`、不使用连续 `as` 断言；不为让套件转绿改写既有断言（Task 0 裁决情形除外）"
    - "不新增 `it.skip`；`equipment.test.ts:306`（码 147，D-06）保持原样"
---

<objective>
把 07-09 确立的「同引用原则」推广到三个容器型 saveable：`HeroEquipsStore`（`#06-17-4` / 审计 D）、`FlagSystem`（`#06-17-5` / 审计 E）、`HeroFollowersController`（`#06-17-6` / 审计 F），使外部持有的 `IEquipmentState` / `IFlagCommonField` / `IHeroFollower` 引用跨读档仍然有效。

现状（逐条锚点）：
1. **`#06-17-4`（中，审计 D）** `HeroEquipsStore.loadState`（`equipStore.ts:267-288`）先 `instanceMap.clear()`（`:271`）再逐个 `new EquipmentState(...)`（`:278`）——外部从 `get(uid)`/`instancesOf`/`instances`（`:201-257`）拿到的实例读档后与活 store 脱钩：继续改它不会进入存档，读它得到读档前的旧状态。
2. **`#06-17-5`（中/低，审计 E）** `FlagSystem.loadState`（`flag/system.ts:65-72`）先 `fieldMap.clear()`（`:66`）再 `new FlagCommonField(...)`（`:68`）——持有 `getField`/`getOrInsert` 结果的调用方（含 legacy 侧）保留的是被丢弃的字段对象；`saveState` 反过来枚举 `fieldMap`，故对保留实例的写入永久丢失。
3. **`#06-17-6`（中/低，审计 F）** `HeroState.loadState`（`state.ts:148-152`）先 `removeAllFollowers()`（`:148`）再 `addFollower(save.num)`（`:150`）——缓存的 `IHeroFollower` 及其 `location`/`rendering` 全部换成新实例，渲染端若持有旧引用即脱钩；`HeroFollower.loadState` 本身是原地读档（`follower.ts`），所以缺的只是「不要换实例」这一步。

Purpose: 数据层与渲染端之间靠实例传递状态，实例脱钩会让「读档后数值正确」取决于谁持有引用（同一个根因族 07-09 已在属性上消除）。容器层不修则属性层的修复仍会被绕过。

Output: `equipStore.ts` / `flag/system.ts` / `state.ts`（+ 按裁决的 `follower.ts` / `types.ts`）的保留式读档实现 + 三个子系统的同引用回归 + 顶层 `CoreState` 集成断言。

**Artifacts this phase produces（本计划产出物）**：修改文件见 frontmatter `files_modified`；回归用例见 `must_haves.artifacts`；无新建源码文件、无新增依赖。

**Phase 7 验证已失效**：`07-VERIFICATION.md`（2026-09-16T06:04:11Z）在 07-09 之前即已过期，本轮追加 07-10..07-14 后结论必然失效。本计划执行完毕后**必须重跑 `/gsd-verify-work`**（或 `gsd-verify-work 7`）重新出具验证结论。

Phase 7 裁决对应（逐条引用）：D-01: 本计划是 Phase 7 第 11 个计划，承接 `07-LOADSTATE-AUDIT.md` 的 D/E/F（`#06-17-4/5/6`）。D-02: 属 hero 与 flag 两个系统（`equipStore.ts` / `flag/system.ts` / `hero/state.ts`）。D-03/D-04/D-05/D-06: 其它系统契约裁决，不涉及。D-07: path 接线，不涉及。D-08: enemy 复用映射，不涉及。D-09: 由 Task 0 关卡承载。D-10: 本计划为修复计划，回归用例为必需项（A1）。D-11: 保留式读档的口径变更需同步更新 `types.ts` 的 jsDoc。D-12: 沿用 06-CONTEXT D-44 文件级三步门禁。D-13: 按子系统原子提交（Task 1 / Task 2 / Task 3 + 集成断言）。D-14: 三条均无 `WINDOWS.md` 条目，只登记不新建。

执行序：`depends_on: 07-10`（Wave 11）。门禁型依赖（D-09 逐计划裁决关卡 + D-12 全量测试基线逐计划重录），源码层面与 replay 无耦合。

**基线（2026-09-16 实测）**：`pnpm test:ci` = 66 文件 / 690 通过 / 1 跳过（`equipment.test.ts:306` 码 147，D-06）。执行前重录当日基线并与本行对比。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-fixes/07-CONTEXT.md
@.planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md
@.planning/phases/07-data-fixes/07-REVIEW.md
@.planning/phases/07-data-fixes/07-09-PLAN.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@dev.md
@packages-user/data-base/src/hero/equipStore.ts
@packages-user/data-base/src/flag/system.ts
@packages-user/data-base/src/flag/field.ts
@packages-user/data-base/src/hero/state.ts
@packages-user/data-base/src/hero/follower.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: 契约裁决关卡（D-09）——同引用保留范围（equipStore / flag / followers）、followers 保留口径与钩子语义、存档缺失实例的处置、诊断码</name>
  <files>（只读汇报，不修改任何文件）</files>
  <read_first>
    - packages-user/data-base/src/hero/equipStore.ts（:15-165 `EquipmentState`（`item` 为 readonly、:46-55 `rebuildModifiers`、:104-164 三档 save/load）、:167-289 `HeroEquipsStore`（:201-257 `get`/`instancesOf`/`instances`、:267-288 `loadState` 的 clear + 重建））
    - packages-user/data-base/src/flag/system.ts（:4-73 全文件，重点 :16-35 字段获取入口与 :65-72 `loadState`）
    - packages-user/data-base/src/flag/field.ts（`FlagCommonField` 的 `value`/`key` 私有字段与 `fromStructured`）
    - packages-user/data-base/src/hero/state.ts（:138-153 `loadState` 的 followers 段）
    - packages-user/data-base/src/hero/follower.ts（`HeroFollower`（`num` 为 readonly、构造器按 num 解析、`loadState` 原地）、`HeroFollowersController`（`followers` 私有数组、`addFollower`/`getFollower`/`getAllFollowers`、`removeFollower`/`removeAllFollowers` 的 `onRemoveFollower` 钩子））
    - packages-user/data-base/src/hero/types.ts（`IHeroFollower`、`IHeroFollowersController`、`IHeroEquipsStore`、`IEquipmentState` 的公开契约）
    - packages-user/data-base/src/hero/saveLoad.test.ts（三档压缩既有夹具与 `HeroState` 容器往返用例）
    - packages-user/data-state/test/saveablesRoundTrip.test.ts（`CoreState.saveState/loadState` 三档往返与 `seedState`/`mutateState`/`assertRestored` 夹具）
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（D/E/F 行、判定为「安全」的同类边界、未确定项 2）
    - .planning/phases/07-data-fixes/07-09-PLAN.md（同引用原则的既有裁决与实现口径）
  </read_first>
  <decision>
    三个容器的保留式读档需要用户裁决：
    （Q1）逐子系统是否保留实例（`equipStore` / `flag` / `followers`）；（Q2）followers 的保留口径与 `onRemoveFollower`/`onAddFollower` 钩子语义；（Q3）存档中不存在而内存中存在的实例如何处置；（Q4）这些处置是否需要诊断、用哪些码。
  </decision>
  <context>
    保留实例会把「以存档为准」的语义从「整表替换」改成「逐项合并」：必须回答「存档里没有的项怎么办」（删除 = 与今日等价；保留 = 合并语义），以及「同一 id 但内容指代不同」（`EquipmentState.item` 是构造期 readonly，uid 相同但 `num` 不同的存档无法原地复用）。followers 更复杂：`HeroFollower.num` 是 readonly，列表长度/顺序变化时无法原地改 num，且 `removeAllFollowers` 会触发 `onRemoveFollower`（渲染端可能依赖）——保留实例与保留钩子是两件事，需要用户拍定取哪个组合。以上均属用户主导的接口/契约范畴，AI 不自行选择。
  </context>
  <options>
    <option id="q1-all">
      <name>Q1 选项 A：【建议默认】三个子系统全部保留实例（`equipStore` / `flag` / `followers` 都按同引用原则改）</name>
      <pros>与 07-09 在 `HeroAttribute` 上确立的原则一致；三个缺陷同根因，一次收口；外部持有者（含渲染端与 legacy 侧）不需要任何重取逻辑</pros>
      <cons>三处 `loadState` 都要从「整表替换」改为「逐项合并」，需要同时定义「多余项」的处置与错误分支；实现面比逐个替换大</cons>
    </option>
    <option id="q1-partial">
      <name>Q1 选项 B：只保留部分子系统（由用户在记录中点名哪几个保留、哪几个维持现状并登记为已接受契约）</name>
      <pros>把改动面压到用户认为真正有外部持有者的子系统</pros>
      <cons>未保留的子系统保留「脱钩」契约，必须在 jsDoc 与 SUMMARY 明写；同一原则在不同容器上口径不一致，后续容易再次踩坑</cons>
    </option>
    <option id="q1-none">
      <name>Q1 选项 C：三个都维持现状，只登记为已知契约（`loadState` 会替换容器内实例）</name>
      <pros>零改动、零风险</pros>
      <cons>与「本轮把这 15 项纳入修复」的指令冲突（清单第 7–9 条）；与 07-09 刚确立的同引用原则自相矛盾</cons>
    </option>
    <option id="q2-index-inplace">
      <name>Q2 选项 A：【建议默认】followers 按索引对齐：同索引且 `num` 相同 → 原地 `loadState`（保留实例）；`num` 不同 → 该槽位替换并按既有钩子语义发 `onRemoveFollower`/`onAddFollower`；数量变化同样按替换 + 删除/新增处理</name>
      <pros>不改 `IHeroFollower`（`num` 维持 readonly）；最常见的「读同一存档」路径零脱钩；钩子语义与今日一致（渲染端不需要改）</pros>
      <cons>`num` 不同的槽位仍会换实例（外部持有该槽位的引用会脱钩）——需在 jsDoc 写明该边界</cons>
    </option>
    <option id="q2-mutable-num">
      <name>Q2 选项 B：把 `num` 改为可变（`IHeroFollower.num` 去掉 readonly + 内部更新方法），任何情况都原地复用实例，load 期间不发 remove/add 钩子</name>
      <pros>实例身份在所有情形下都保留；读档不再触发跟随者增删副作用</pros>
      <cons>公开契约变更（`readonly num` → 可写）；渲染端若依赖 `onRemoveFollower`/`onAddFollower` 重同步，读档后将不再收到通知，需要另定同步方式</con>
    </option>
    <option id="q2-no-preserve">
      <name>Q2 选项 C：followers 维持 `removeAllFollowers()` + `addFollower()` 全量重建，只登记契约</name>
      <pros>不动 follower 相关代码</pros>
      <cons>`#06-17-6` 未修；与 Q1 选项 A 冲突（若 Q1 选 A，本选项只能用于 followers 例外）</cons>
    </option>
    <option id="q3-delete">
      <name>Q3 选项 A：【建议默认】以存档为准：内存中多出的装备实例 / flag 字段 / follower 一律删除（与今日 `clear()` 的可观测结果等价，只是不再波及存档里存在的项）</name>
      <pros>读档结果与今日完全一致（不会留下「存档里没有的旧数据」）；语义简单、易断言</pros>
      <cons>被删除实例的外部持有者仍然脱钩（但这与「该实例在存档点不存在」一致，属正确语义）</cons>
    </option>
    <option id="q3-keep">
      <name>Q3 选项 B：合并语义：存档中不存在的项**保留**（不删除），读档只覆盖存档里出现的项</name>
      <pros>外部持有的实例全都不会消失</pros>
      <cons>读档不再等价于「回到存档点」：存档里已不存在的装备/字段会残留，可能与后续存档产生漂移（`saveState` 会把它们重新写入）；语义风险明显，需要用户在记录中承担该后果</con>
    </option>
    <option id="q4-existing">
      <name>Q4 选项 A：不新增码，复用既有诊断——followers 用 91 / 142（既有「follower number 不存在」「id 解析失败」码），装备用 58/59，flag 无专用码则不打诊断（删除静默）</name>
      <pros>不动 `packages/common/src/logger.json`（延续 07-09「不新增错误码」惯例）</pros>
      <cons>91/142 的文案原意与本场景不完全相符（它们是「添加不存在的老鼠」/「id 解析失败」）；借用会让日志语义变模糊</cons>
    </option>
    <option id="q4-new">
      <name>Q4 选项 B：新增 replay 之外的通用 warn 码（当前最大 warn 码 178 之后的可用号）用于「读档时丢弃了存档外的实例/字段」，携带类别与标识</name>
      <pros>诊断精确、语义清晰；码唯一不复用</pros>
      <cons>需改 `packages/common/src/logger.json`；需要用户确认这条诊断值得进码表</cons>
    </option>
    <option id="q4-none">
      <name>Q4 选项 C：不打任何诊断（删除静默）</name>
      <pros>零码表改动、零日志噪音</pros>
      <cons>外部持有者被解绑时无任何线索；排查「读档后我的引用失效了」只能靠读代码</cons>
    </option>
    <option id="revise">
      <name>（兜底）修订方案后再执行</name>
      <pros>避免带错误契约落地</pros>
      <cons>按 D-09 退出本次修改、修订本 PLAN.md 后重新执行，不得自行另辟他法</cons>
    </option>
  </options>
  <record>
    待用户在关卡处逐条填写（Q1..Q4：勾选选项 id 或给出裁决文字）。
    **空的 `<record>` 表示尚未裁决**：执行者必须停在本任务，不得按「建议默认」推进。
    填写后 Task 1–4 一律按本记录执行；若 Q1 选了「部分保留」，请在记录中点名保留哪些子系统，Task 1/2/3 中被排除的子系统跳过实现、只保留登记。
  </record>
  <resume-signal>逐条给出 Q1..Q4 的裁决（例如「Q1=A, Q2=A, Q3=A, Q4=C」）；若要修订方案，回复修订点，执行者退出并按 D-09 修订本 PLAN.md</resume-signal>
  <action>向用户汇报上述 4 个契约点及其对三个子系统的影响（可连带汇报 Task 1–4 的实现路径，便于一次确认）。不得在裁决前修改任何文件。</action>
  <verify>
    <human-check>`<record>` 中 Q1..Q4 均有明确裁决（Q1 若为部分保留，已点名子系统），且与用户实际意图一致</human-check>
    <fails_when>记录为空或与用户实际意图不符 —— 必须暂停并请用户澄清，不得按「建议默认」自行推进</fails_when>
  </verify>
  <acceptance_criteria>
    - Q1（保留范围，逐子系统）、Q2（followers 保留口径与钩子语义）、Q3（存档缺失实例处置）、Q4（诊断码）均有用户裁决并写入 `<record>`
    - 裁决与 Task 1–4 的分支描述可对应；未在裁决前修改任何文件
  </acceptance_criteria>
  <done>Q1..Q4 裁决已完整写入 `<record>`；执行者据记录直接进入 Task 1，不再重复询问</done>
</task>

<task type="auto">
  <name>Task 1: `#06-17-4` —— `HeroEquipsStore.loadState` 保留实例（按 uid 复用 + 原地 `EquipmentState.loadState`），多余实例按裁决处置</name>
  <files>packages-user/data-base/src/hero/equipStore.ts, packages-user/data-base/src/hero/equipStore.test.ts, packages-user/data-base/src/hero/saveLoad.test.ts</files>
  <read_first>
    - packages-user/data-base/src/hero/equipStore.ts（:15-165 `EquipmentState` 的 `uid`/`item`/`loadState`、:167-289 `HeroEquipsStore` 的 `instanceMap`/`nextUid`/`add`/`get`/`instancesOf`/`saveState`/`loadState`（:267-288 缺陷本体））
    - packages-user/data-base/src/hero/equipStore.test.ts（现有 `HeroEquipsStore instances` / `EquipmentState modifiers` 用例夹具）
    - packages-user/data-base/src/hero/saveLoad.test.ts（三档压缩夹具与 `#06-09-1` 的加成恢复用例，注意不要弱化）
    - packages-user/data-base/src/hero/types.ts（`IHeroEquipsStore`、`IEquipmentState` 的公开契约与 jsDoc 位置）
  </read_first>
  <action>
    按 Task 0 的 Q1/Q3/Q4 裁决重写 `HeroEquipsStore.loadState`（`equipStore.ts:267-288`）。**若 Q1 判 equipStore 保留**（推荐默认）：
    (1) 遍历 `state.equipments`，对每条 `save`：`const raw = this.state.itemStore.getData(save.num)`；`raw` 为空时保持既有 `logger.error(59, …)` + `continue`；否则 `const existing = this.instanceMap.get(save.uid)`：若 `existing` 存在且 `existing.item.num === save.num` → `existing.loadState(save, compression)`（**不新建实例**）；否则 → 新建 `new EquipmentState(save.uid, raw, false)`（`buildModifier = false` 与现状一致）、`instance.loadState(save, compression)`、`this.instanceMap.set(save.uid, instance)`；
    (2) 按 Q3 裁决处置「存档中不存在的 uid」：选项 A（以存档为准）→ 收集存档 uid 集合，遍历 `this.instanceMap.keys()`，不在集合中的按裁决删除（Q4 选项 A 不打诊断 / B 打新码 / C 静默）；选项 B（合并）→ 不删除，并在 jsDoc 写明合并语义；
    (3) `nextUid` 段（`:282-287`）保持不变（`maxBy(state.equipments, 'uid')` → `nextUid = maxUid.uid + 1`，空存档走既有 `logger.error(58)` 分支）——IN-02 不在本计划范围，不得顺手改动；
    (4) 在 `IHeroEquipsStore.loadState` 的 jsDoc（`hero/types.ts`）补写「同一 uid 的实例在读档前后为同一实例；存档中不存在的实例按 {裁决} 处置」。
    **若 Q1 判 equipStore 不保留**：不修改行为，仅在 SUMMARY 登记「用户裁决维持现状」。
    回归用例：
    (a) `equipStore.test.ts`：`add(num)` → 取 `const before = store.get(uid)` → 改一下活实例（如 `before.getModifiers()` 对应的修饰器 `setValue`，或先 `loadState` 前修改活状态以便观察）→ `saveState(三档)` → 再改动 → `loadState(save, 三档)` → 断言 `store.get(uid) === before`（同实例）且实例上读到的状态等于存档点；
    (b) `saveLoad.test.ts`：`HeroState` 容器三档往返中，断言 `hero.items.equipment.get(uid)` 读档前后同实例；若 Task 0 裁决 Q3 为删除，另加一条「存档中不存在的 uid 读档后被移除（且按 Q4 裁决有/无诊断）」；
    (c) 断言 `EquipmentState` 的加成恢复（`#06-09-1` 既有语义）在保留实例路径下不回归。
    用例中文单行注释说明覆盖内容（`dev.md:85`）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/hero/equipStore.test.ts packages-user/data-base/src/hero/saveLoad.test.ts</automated>
    <fails_when>读档后 `store.get(uid)` 返回的不是读档前持有的实例（Q1 判保留时）；或三档压缩下装备加成/槽位恢复结果与既有断言不一致 —— 按 D-09 退出并修订计划，不得改用「读档后让调用方重取」的替代方案</fails_when>
  </verify>
  <acceptance_criteria>
    - 同一 uid 的 `EquipmentState` 在读档前后为同一实例（Q1 判保留时），且读档后的值与修饰器等于存档点
    - 存档中不存在的实例处置与 Q3/Q4 裁决一致并有断言
    - `nextUid` 段与 IN-02 未被改动；既有 `equipStore.test.ts` / `saveLoad.test.ts` 断言未弱化
  </acceptance_criteria>
  <done>`HeroEquipsStore.loadState` 不再让外部持有的 `EquipmentState` 脱钩，且恢复结果仍以存档为准（`#06-17-4` / 审计 D 关闭）</done>
</task>

<task type="auto">
  <name>Task 2: `#06-17-5` —— `FlagSystem.loadState` 保留字段实例（按 key 复用 + `fromStructured`），多余字段按裁决处置</name>
  <files>packages-user/data-base/src/flag/system.ts, packages-user/data-base/src/flag/system.test.ts</files>
  <read_first>
    - packages-user/data-base/src/flag/system.ts（:4-73 全文件，重点 :16-35 `getField`/`getOrInsert`/`getOrInsertComputed`、:57-63 `saveState`、:65-72 `loadState` 缺陷本体）
    - packages-user/data-base/src/flag/field.ts（`FlagCommonField` 的 `set`/`get`/`toStructured`/`fromStructured`）
    - packages-user/data-base/src/flag/system.test.ts（现有 `FlagSystem field container` / `FlagSystem value accessors` 用例与 `logger.catch` 口径）
    - packages-user/data-base/src/flag/saveLoad.test.ts（既有 flag 三档往返用例）
    - packages-user/data-base/src/flag/types.ts（`IFlagSystem` / `IFlagCommonField` / `IFlagSystemSave` 契约与 jsDoc 位置）
  </read_first>
  <action>
    按 Task 0 的 Q1/Q3/Q4 裁决重写 `FlagSystem.loadState`（`system.ts:65-72`）。**若 Q1 判 flag 保留**（推荐默认）：
    (1) 遍历 `state.fields`，对 `[key, data]`：`const field = this.fieldMap.get(key)`；若存在 → `field.fromStructured(data)`（原地写值，与 `saveState` 的 `toStructured` 对称）、`this.fieldMap.set(key, field)`；否则 → `const created = new FlagCommonField<unknown>(this, key, undefined); created.fromStructured(data); this.fieldMap.set(key, created)`；
    (2) 按 Q3 裁决处置「存档中不存在的 key」：选项 A → 收集存档 key 集合并按裁决删除（Q4 选项 A 无 flag 专用码时静默 / B 打新码 / C 静默）；选项 B → 保留（合并语义须写入 jsDoc）；
    (3) 在 `IFlagSystem.loadState` 的 jsDoc（`flag/types.ts`）补写同引用契约与多余字段处置。
    **若 Q1 判 flag 不保留**：不修改行为，仅在 SUMMARY 登记「用户裁决维持现状」。
    回归用例（`flag/system.test.ts`）：
    (a) 取字段引用 `const before = system.getField('score')`（或 `getOrInsert`）→ 写值 → `saveState()` → 再改值 → `loadState(save)` → 断言 `system.getField('score') === before`（同实例）且 `before.get()` 等于存档点的值（而不是改后的值）；
    (b) `getOrInsert` 在 load 覆盖后仍返回同一实例；
    (c) 按 Q3 裁决断言「存档外的字段」读档后存在/不存在，并按 Q4 裁决断言诊断（`logger.catch` 口径）或无诊断；
    (d) 既有 `flag/saveLoad.test.ts` 的三档往返断言保持通过（不改写）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/flag/system.test.ts packages-user/data-base/src/flag/saveLoad.test.ts</automated>
    <fails_when>读档后 `getField(key)` 返回的不是读档前持有的实例（Q1 判保留时）；或保留实例上的值不等于存档点（说明 `fromStructured` 未在原实例上生效）—— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - 同一 key 的 `FlagCommonField` 在读档前后为同一实例（Q1 判保留时），且其值等于存档点
    - 存档外字段的处置与 Q3/Q4 裁决一致并有断言
    - `IFlagSystem.loadState` 的 jsDoc 与最终语义一致；既有断言未弱化
  </acceptance_criteria>
  <done>`FlagSystem.loadState` 不再让外部持有的字段引用脱钩，读档值仍以存档为准（`#06-17-5` / 审计 E 关闭）</done>
</task>

<task type="auto">
  <name>Task 3: `#06-17-6` —— followers 保留式恢复（按 Q2 裁决：索引对齐原地 `loadState` / 槽位替换 + 钩子语义）</name>
  <files>packages-user/data-base/src/hero/state.ts, packages-user/data-base/src/hero/follower.ts, packages-user/data-base/src/hero/types.ts, packages-user/data-base/src/hero/follower.test.ts, packages-user/data-base/src/hero/saveLoad.test.ts</files>
  <read_first>
    - packages-user/data-base/src/hero/state.ts（:138-153 `loadState` 的 followers 段与 `void this.followers.removeAllFollowers()`）
    - packages-user/data-base/src/hero/follower.ts（`HeroFollower` 的 `num` readonly 与 `loadState`、`HeroFollowersController` 的 `followers` 私有数组、`addFollower`/`getFollower`/`getAllFollowers`/`removeFollower`/`removeAllFollowers` 及各自的钩子触发）
    - packages-user/data-base/src/hero/types.ts（`IHeroFollower`（`readonly num`）、`IHeroFollowersController`、`IHeroFollowersControllerHooks`（`onAddFollower`/`onRemoveFollower`））
    - packages-user/data-base/src/hero/follower.test.ts（现有 follower 用例夹具与钩子断言口径）
    - packages-user/data-base/src/hero/saveLoad.test.ts（`HeroState` 容器三档往返中的 followers 断言）
  </read_first>
  <action>
    按 Task 0 的 Q2 裁决实现保留式恢复。**若 Q2 = 选项 A（索引对齐）**（推荐默认）：
    (1) `HeroFollowersController` 增加一个保留式恢复入口（命名由实现者定，如 `restoreFollowers(saves, compression)`）：按索引遍历 `saves`，对第 i 项：若 `this.followers[i]` 存在且 `this.followers[i].num === save.num` → 调用其 `loadState(save, compression)`（**保留实例**，不发钩子）；否则 → 按既有顺序触发 `onRemoveFollower`（若该槽位原有 follower）、构造新的 `HeroFollower`（复用 `addFollower` 的定位/朝向初始化口径）、并触发 `onAddFollower`；`saves` 短于现有时，末尾多余项按 Q3 裁决删除；
    (2) `HeroState.loadState`（`state.ts:148-152`）改为调用该入口，删除 `void this.followers.removeAllFollowers()` 与 `addFollower` 循环；**保持同步**（`HeroState.loadState` 不得改为 async，A4）；
    (3) 契约声明放接口源头：若新增入口，则在 `hero/types.ts` 的 `IHeroFollowersController` 上补声明与中文 jsDoc（写清「同索引同 num 保留实例、否则替换，并说明钩子触发条件」）。
    **若 Q2 = 选项 B（num 可变）**：按裁决把 `IHeroFollower.num` 去掉 `readonly` 并允许原地更新（实现方式由记录指定），同时按裁决在 load 期间不发 remove/add 钩子；在 jsDoc 写明渲染端同步方式的变更。
    **若 Q2 = 选项 C（不保留）**：不修改行为，仅在 SUMMARY 登记「用户裁决维持现状」。
    回归用例：
    (a) `follower.test.ts`：`addFollower(num)` → 取引用 → `saveState` → 改位置/数量 → 恢复 → 断言保真口径（选项 A：同索引同 num 为同一实例；按 Q2/Q3 裁决断言 num/数量变化时的替换与钩子）；
    (b) `saveLoad.test.ts`：`HeroState` 三档往返中，跟随者数量与位置恢复；按裁决断言 `getAllFollowers()[0]` 读档前后同实例（同索引同 num 时）；
    (c) 断言 `HeroFollower.location` / `rendering` 的恢复仍走既有原地 `loadState`（不新增第三条读档路径）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/hero/follower.test.ts packages-user/data-base/src/hero/saveLoad.test.ts</automated>
    <fails_when>`HeroState.loadState` 仍然调用 `removeAllFollowers()` 全量重建（Q2 判保留时）；或读档后跟随者位置/数量与存档点不一致；或 `HeroState.loadState` 被改成 async —— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - 按 Q2 裁决：同索引同 `num` 的 follower 实例在读档前后为同一实例（选项 A/B），或明确维持现状（选项 C）
    - 钩子语义与 Q2 裁决一致并有断言（`onRemoveFollower`/`onAddFollower` 何时触发）
    - `HeroState.loadState` 仍为同步方法；`HeroFollower.loadState` 未被替换为新的读档路径
  </acceptance_criteria>
  <done>followers 不再因读档全量换实例（按裁决口径），位置/渲染恢复仍以存档为准（`#06-17-6` / 审计 F 关闭）</done>
</task>

<task type="auto">
  <name>Task 4: 顶层同引用集成断言 + D-12/D-44 门禁 + 全量套件 + 缺口登记与 Phase 7 验证失效说明</name>
  <files>packages-user/data-state/test/saveablesRoundTrip.test.ts, .planning/phases/07-data-fixes/07-11-SUMMARY.md</files>
  <read_first>
    - packages-user/data-state/test/saveablesRoundTrip.test.ts（`createCoreState`/`seedState`/`mutateState`/`assertRestored` 夹具与三档往返循环、码 178 相关既有断言）
    - packages-user/data-state/src/core.ts（:248-252 saveables 注册顺序、`loadState` 的 177/178 判定——本计划不改动，仅用于理解触发路径）
    - packages-user/data-base/src/hero/saveLoad.test.ts、flag/system.test.ts、hero/follower.test.ts、hero/equipStore.test.ts（Task 1–3 新增用例，用于避免重复设计）
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（D/E/F 行与「未确定项 2」——渲染端是否跨读档保留实例，本任务用顶层断言把该不确定性在数据端封口）
    - .planning/phases/07-data-fixes/07-VERIFICATION.md（头部 verified/status）
    - .planning/ROADMAP.md（Phase 7 段 Plans 与 Wave 列表）
    - .planning/WINDOWS.md（确认三条无入账 id）
  </read_first>
  <action>
    (1) **顶层集成断言**（`saveablesRoundTrip.test.ts`）：在既有三档循环里新增一组同引用断言——`CoreState` 初始化并装备/写 flag/加 follower 后，取 `state.hero.items.equipment.get(uid)`、`state.flags.getField(key)`（或既有 flag 访问入口）、`state.hero.followers.getFollower(0)` 三个引用；`saveState` → 改动活状态（装备修饰器/flag 值/follower 位置）→ `loadState` → 断言三个引用 `===` 读档前的实例（按 Q1 裁决的子系统），且其值/位置等于存档点。用例中文单行注释说明覆盖内容。
    (2) **门禁（D-12 / D-44 文件级）**：对 `files_modified` 中每个改动的源码/测试文件执行 `pnpm exec eslint --fix <文件>` 后 `pnpm exec eslint <文件>` 0 错误；**文件级类型门禁按 06-RESEARCH Pitfall 4 的判定方式**：`pnpm exec vue-tsc --noEmit 2>&1 | Select-String -Pattern "<改动文件相对路径>"` 无命中（**不得**用「退出码非 0」判定失败）；`pnpm test:ci` 全绿（对比基线：66 文件 / 690 通过 / 1 跳过，跳过项仍只能是 `equipment.test.ts:306` 码 147）。
    (3) **原子提交（D-13）**：Task 1 / 2 / 3 / 4 各自独立提交（`fix(07-11): …`，携带 `#06-17-4` / `#06-17-5` / `#06-17-6` 锚点）。
    (4) **缺口登记**：SUMMARY 的 findings/ledger 段落逐条标注 `#06-17-4/5/6`（审计 D/E/F）已闭合（附 commit），并登记 review 的 `IN-02`（空存档不重置 `nextUid`）为「本轮不修，保留在册」；`WINDOWS.md` 不新建、不 `fixed`/`waive`。
    (5) **Phase 7 验证失效**：SUMMARY 明写 `07-VERIFICATION.md` 已因 07-09 + 07-10..07-14 失效，结束后必须重跑 `/gsd-verify-work`（或 `gsd-verify-work 7`）；`ROADMAP.md` 的 Phase 7 计划数与 Wave 列表同步。
  </action>
  <verify>
    <automated>pnpm test:ci</automated>
    <fails_when>`pnpm test:ci` 出现新增失败或新增 `it.skip`；或顶层集成断言显示读档前后三个引用任一不是同一实例（Q1 判保留的子系统）—— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - 顶层 `CoreState` 三档往返中，按裁决保留的容器实例跨读档为同一实例且状态等于存档点
    - 改动文件 `eslint` 0 错误、`vue-tsc --noEmit` 该文件 0 类型错误、`pnpm test:ci` 全绿且跳过项仍仅 1 条
    - Task 1–4 各有独立提交，含对应锚点；SUMMARY 完成缺口登记（D/E/F 闭合 + IN-02 保留）与 Phase 7 验证失效说明
    - `WINDOWS.md` 未被本计划改动
  </acceptance_criteria>
  <done>顶层集成断言证明三个容器的外部引用跨读档仍然有效；D-44 三步门禁全绿、原子提交完成、缺口登记与验证失效说明写入 SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 存档数据 → 容器实例 | 读档是「外部数据定义内存状态」的边界；容器若整表替换，外部持有者的引用即脱钩 |
| 数据层 → 渲染端/legacy 持有者 | 渲染端与 legacy 侧持有 `IEquipmentState` / `IFlagCommonField` / `IHeroFollower` 引用，跨读档的引用有效性由本计划定义 |
| load 钩子 → 渲染端同步 | `onRemoveFollower`/`onAddFollower` 是数据端向渲染端广播增删的通道，保留式恢复可能改变其触发频率 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-25 | Tampering | `HeroEquipsStore.loadState`（`equipStore.ts:267-288`） | medium | mitigate | Task 1：按 uid 复用 `EquipmentState` 并原地 `loadState`；同 uid 但 `item.num` 不同时新建（`item` 为构造期 readonly），多余 uid 按裁决处置 |
| T-7-26 | Tampering | `FlagSystem.loadState`（`flag/system.ts:65-72`） | medium | mitigate | Task 2：按 key 复用 `FlagCommonField` 并 `fromStructured`（与 `toStructured` 对称），多余 key 按裁决处置 |
| T-7-27 | Tampering | `HeroState.loadState` followers 段（`state.ts:148-152`） | medium | mitigate | Task 3：索引对齐原地 `loadState`（或按裁决的等价口径），保留实例；`num`/数量变化按裁决替换并显式处理钩子 |
| T-7-28 | Repudiation | 读档丢弃/保留实例无诊断 | low | mitigate | Task 0 Q4 裁决决定是否诊断；若选新码则登记文案并带类别+标识，若静默则在 jsDoc 与 SUMMARY 明写 |
| T-7-29 | Elevation of Privilege | 保留实例后旧状态残留（合并语义） | medium | mitigate | Task 0 Q3 裁决；若选合并语义，必须在 jsDoc + SUMMARY 明写「读档不再等价于回到存档点」的后果，并有断言覆盖 |
| T-7-30 | Denial of Service | `HeroState.loadState` 改异步导致调用点行为变化 | low | mitigate | A4：保留同步；Task 3 `<fails_when>` 显式拦截 async 化 |
| T-7-SC | Tampering | npm/pnpm 依赖安装 | high | mitigate | 本计划不新增依赖、不改 `package.json`；出现安装需求即暂停并要求用户确认包合法性 |
</threat_model>

<verification>
1. `pnpm exec vitest run packages-user/data-base/src/hero/equipStore.test.ts packages-user/data-base/src/flag/system.test.ts packages-user/data-base/src/hero/follower.test.ts packages-user/data-base/src/hero/saveLoad.test.ts packages-user/data-state/test/saveablesRoundTrip.test.ts` 全绿。
2. `pnpm test:ci` 全绿：66 文件 / 690 通过 / 1 跳过（仅 `equipment.test.ts:306` 码 147）。
3. `pnpm exec eslint <files_modified 中每个改动文件>` 0 错误；`pnpm exec vue-tsc --noEmit` 输出不含这些文件的类型错误。
4. Task 0 的 `<record>` 中 Q1..Q4 均有裁决，代码与裁决逐条一致（SUMMARY 对照表）。
5. 无新增依赖、无新建源码文件、`WINDOWS.md` 未改动、`data-state/src/core.ts` 未改动（除集成断言所在测试文件外）。
6. Phase 7 验证失效与重跑要求已在 `07-11-SUMMARY.md` 明写，`ROADMAP.md` 已更新。
</verification>

<success_criteria>
- `#06-17-4/5/6`（审计 D/E/F）按用户裁决闭合：装备实例 / flag 字段 / follower 的外部引用跨读档不再脱钩（或按裁决明写为已接受契约），并有回归断言。
- 保留实例不改变恢复结果：三档压缩下的值、修饰器、槽位、位置仍等于存档点。
- 存档缺失实例的处置与诊断符合 Q3/Q4 裁决，且有断言与文档。
- `pnpm test:ci` 全绿、无新增 `it.skip`、既有断言未弱化；D-44 三步门禁全绿。
- Phase 7 验证失效与重跑要求已记录；`IN-02` 已登记为不在本轮修复。
</success_criteria>

<output>
Create `.planning/phases/07-data-fixes/07-11-SUMMARY.md` when done
</output>
