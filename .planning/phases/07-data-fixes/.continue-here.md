# Phase 7 — 接力说明（07-16 暂缓，等数据端改完再启动）

**更新:** 2026-09-17（晚）
**HEAD:** 见 `git log -1`（分支 `refactor/data`）

## 当前状态：**Phase 7 暂缓**

- `07-01..07-15` 全部已执行并提交。
- `07-16`（寻路重构后的测试对齐）**已完成规划，但暂缓（DEFERRED）** —— 用户仍在手工重构数据端，接口/形状还会继续变，测试会持续报错。

## 为什么暂缓（用户原话）
> 「数据端还有很多内容需要我手动修改，还会导致大量的测试文件报错。所以此计划暂缓，等待我把数据端全部修改完毕后再进行。」

## 恢复时的第一件事（重要）
1. **重新测量**：跑一次 `pnpm test:ci`，得到**最新**的失败清单。
2. **重新核对 / 重规划**：
   - `07-VERIFICATION.md` 的 `### Post-Refactor Test Breakage`（2026-09-17 快照，**很可能已过期**）
   - `07-16-PLAN.md`（很可能需要按新清单重规划，而不是直接执行）
3. 重规划走：`/gsd-plan-phase 7 --gaps`（必要时先更新缺口清单）。
4. 执行走：`/gsd-execute-phase 7 --gaps-only`（Task 0 的 Q1..Q4/R1..R3 需用户逐条裁决）。
5. 收尾走：`/gsd-verify-work 7` 重出验证。

## 已知的 07-16 规划要点（供重规划参考）
- 目标：把失败测试文件**对齐到已发布的接口**，**只改测试、不改生产代码**。
- 已查明的真实替代接口（规划时从源码读出，非猜测）：
  - 寻路：`useMapState` 已删除且无替代（宽度改由 `MapGraphBuilder.useMapLayer` → `indexer.setWidth(layer.width)`）；`useDirGroup(number)` → `useFaceHandler(IFaceHandler<number>)`（`Dir4FaceHandler`/`Dir8FaceHandler`）；builder `face` 默认改为 `null`，每个 `build()/find()` 需显式注入；`DefaultPassPredicateImpl(state.maps)` → `(state)`。
  - 录像：`ReplaySystem.array`；`onRecordCommand(code, array.length - 1, …)` → 首步索引期望 `1`→`0`；测试本地 `ReplaySystemStub` 的 `route` 需改 `array`。
  - 位置/楼层：`setFloor(map: IGameMap)` + 新增 `location.map`（`location.ts:24/49`）；`IHeroChangeFloorInfo.target: IGameMap`；16 处残留 `setFloor(<string>)`。
  - `CoreState.initMapState` 已全仓删除（仅 `coreEventLayer.test.ts` 引用）→ 需裁决「改指向 `MapState.fromRaw` 还是退役」。
- 07-16 初版 `files_modified`：17 个测试文件 + `data-state/test/fixtures/closed-loop.ts`（共 18 条，均已跟踪）。

## 仍待处理的遗留
1. **07-15 的 CR-01 见证仍红**：唯一原因是你 `a2a8e6e` 把 `ReplaySystem.route` 改名 `array` 时漏了 `hero/equipment.test.ts` 的本地 `ReplaySystemStub`。生产修复（`53f067a`）已提交并经反向验证正确。**07-16 恢复时会顺带修好**。
2. `07-VERIFICATION.md` 状态为 `gaps_found`（含已暂缓的测试缺口）→ Phase 7 最终必须 `/gsd-verify-work 7`。
3. `ROADMAP.md` 的 Phase 7 进度行为 `15/16 · 暂缓 (Deferred)`。

## 未提交的用户在途改动（**禁止纳入任何提交**）
- 执行 `git status --short` 查看；当前可能含 `packages-user/data-state/src/core.ts`、`packages/common/src/dirtyTracker.ts` 等。
- **提交时只 stage 计划声明的文件**，绝不 `git add -A` / `git commit -a` / `git stash`。

## 长期纪律（用户 2026-09-17）
- 并行改动导致的**类型错误**与**测试失败**，若不落在本计划声明的路径上：**不理会、不修、不登记**。
- 不得 amend 已推送提交。
- 任何方法/字段命名，执行前须经用户批准。
- 执行任何计划前，先向用户汇报计划内容并取得确认。
