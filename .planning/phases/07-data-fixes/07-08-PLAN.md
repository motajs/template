---
phase: 07-data-fixes
plan: 08
type: execute
wave: 8
depends_on:
  - 07-07
files_modified:
  - packages-user/data-state/test/replayPlayback.test.ts
autonomous: false
requirements:
  - FIX-01
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "core.ts 已提供 useMapState(this.maps) 与 usePassPredicate(new DefaultPassPredicateImpl(this.maps))，且 useMapLayer(null) 为有意设计（勇士初始不在任何楼层，待切层时再设置）"
    - "测试在楼层激活后把 map.eventLayer 绑定到 finder，顶层录像瞬移可解析出路径并到达 (2,0)（不再告警 173、不再下发 2005）"
    - "重命名后的用例不再以 it.skip 存在；wireFinder=true 的既有兄弟用例一字未改"
    - "pnpm test:ci 全绿，且本计划不新增任何 it.skip"
    - "AI 未修改任何生产源码；楼层切换重注入缺口作为用户负责项在 SUMMARY 登记，本阶段不修复"
  artifacts:
    - path: packages-user/data-state/test/replayPlayback.test.ts
      provides: "顶层录像瞬移回归见证（createSmallMapScene 返回 map、测试侧绑定 eventLayer、原 skip 用例重命名并转绿）"
    - path: packages-user/data-state/src/core.ts
      provides: "用户已产出的三处 finder 注入（:236 maps、:238 layer=null、:239-240 predicate）——本计划只读，不修改"
  key_links:
    - "测试侧 state.pathfinding.finder.useMapLayer(map.eventLayer) ↔ finder.find 的 layer 非空判定（finder.ts:210-215），使告警 173 早退分支不再命中"
    - "data-state/src/core.ts 的 finder 注入点 ↔ data-system/src/path/finder.ts:117-131 的注入 API ↔ data-state/src/hero/predicate.ts 的 DefaultPassPredicateImpl"
    - "createSmallMapScene 的返回值 ↔ 顶层录像瞬移用例的层绑定（对既有忽略返回值的调用方为加法式变更）"
  assumptions:
    - "FIX-01 探针未分类：本计划以显式假设承接，即 FIX-01 = 「登记缺陷的正确预期 skip 用例转绿且不新增跳过」"
    - "core.ts:238 useMapLayer(null) 属有意设计（源码注释：初始状态下勇士不在任何楼层，切换楼层后再具体设置）；本计划保持 core.ts 原样，由测试侧显式绑定事件层来验证真实装配"
    - "楼层切换时事件层会变化（gameMap.ts:43 的 eventLayer）；仓库内除测试外没有任何生产调用点重新调用 useMapLayer。该重注入缺口是已登记的用户负责后续项，不在本阶段（AI 不实现、不评价其设计）"
    - "路由修订（用户裁决）：D-09 首跑证实接线有效（不再告警 173），但 `finder.find` 对 `startIndex === targetIndex` 早退返回空数组（`finder.ts:150`）、`teleportTo` 据此返回 null 并下发 2005，致使 sandbox 挂起；故把瞬移目标由 `(1,0)` 改为 `(2,0)`，断言随之改为 `x=2`/`y=0`。该自目标空路径行为属未登记缺陷，用户裁决「仅登记不修」"
  prohibitions:
    - "AI 不修改 packages-user/data-state/src/core.ts 或任何生产源码；接线已是用户产出（提交 1ff22dd），本计划不追加、不调整、不重注入"
    - "不新增测试用例（D-10）：本次只取消一条既有 skip 并重命名，且只允许对 createSmallMapScene 做「返回 map」的加法式变更"
    - "不弱化断言，不改写为可跑绿假象；不删除或跳过任何其它用例"
    - "不修改渲染端 @user/client-* 与 legacy 渲染接线"
    - "不为 #06-07-1 新建 WINDOWS.md 账本条目"
    - "不引入新依赖、不新建文件"
---

<objective>
处置 path 系统的 `#06-07-1`（D-07 用户负责）：接线已由用户落地——`core.ts:236` `pathfinding.finder.useMapState(this.maps)`、`core.ts:238` `pathfinding.finder.useMapLayer(null)`、`core.ts:239-240` `usePassPredicate(new DefaultPassPredicateImpl(this.maps))`，其中 `useMapLayer(null)` 是**有意设计**（勇士初始不在任何楼层，切层后再具体设置），不是遗漏。因此仅取消 skip 仍会命中 `finder.ts:212-215` 的告警 173 → 空路径 → 2005。本计划据此改为：**由测试侧在楼层激活后显式绑定 `map.eventLayer`**，并把用例**重命名**为反映该事实的名字，取消 skip 后验证转绿；AI **不修改** `core.ts`。

Purpose: 这是 20 条中唯一的装配缺口（L3 顶层接线），其接线已由用户完成；测试侧的层绑定与重命名完成后，顶层录像瞬移链路才有一份真实、可跑的回归见证。

Output: 接线事实与「layer 有意为 null」的确认 + 测试侧绑定 `map.eventLayer` 并重命名用例后转绿 + 未新建账本条目的处置记录 + 楼层切换重注入缺口在 SUMMARY 登记为**用户负责后续项**（本阶段不修复）。

执行序：本计划在 07-07（save）之后串行执行。`depends_on: 07-07` 是**真实**依赖——`core.ts` 同时是 07-07 的修改目标（`loadState` 的 178 判定）与用户接线文件，两者必须串行以避免对同一文件并发编辑。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-fixes/07-CONTEXT.md
@.planning/phases/07-data-fixes/07-RESEARCH.md
@.planning/phases/07-data-fixes/07-PATTERNS.md
@.planning/phases/07-data-fixes/07-VALIDATION.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@.planning/WINDOWS.md
@dev.md
@packages-user/data-system/src/path/finder.ts
@packages-user/data-base/src/map/gameMap.ts
@packages-user/data-state/src/hero/predicate.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: D-07/D-09 确认 —— 接线已落地且 layer 有意为 null，用户授权测试侧绑定 + 重命名</name>
  <files>（只读确认，不修改任何文件）</files>
  <read_first>
    - packages-user/data-state/src/core.ts（:232-241 装配区域：:236 useMapState(this.maps)、:237-238 useMapLayer(null) 及其中文注释、:239-240 usePassPredicate(new DefaultPassPredicateImpl(this.maps))）
    - packages-user/data-state/test/replayPlayback.test.ts（:80-116 createSmallMapScene 与 wireFinder 注入、:66-78 withReplayDisabled 返回值、:287/:302/:320 忽略返回值的既有调用方、:360-373 目标 skip 用例）
    - packages-user/data-system/src/path/finder.ts（:117-131 注入 API、:205-233 maps/layer 为 null 时告警 173 并返回空数组）
    - packages-user/data-base/src/map/gameMap.ts（:43 eventLayer 字段，楼层切换时会变化）
    - .planning/phases/07-data-fixes/07-RESEARCH.md（§Per-Finding #06-07-1、§WINDOWS.md 收口映射）
  </read_first>
  <action>
执行前置（只读核对，勿写）：确认用户接线已在源码中落地——`core.ts:236` `pathfinding.finder.useMapState(this.maps)`、`core.ts:238` `pathfinding.finder.useMapLayer(null)`（其上中文注释：初始状态下勇士不在任何楼层，切换楼层后再具体设置）、`core.ts:239-240` `usePassPredicate(new DefaultPassPredicateImpl(this.maps))`。若三处中任一缺失，**必须停止本计划**并把缺失项与接线依据（`replayPlayback.test.ts:108-114` 的等价注入）汇报给用户；不得由 AI 代改 `core.ts`（D-07）。

核对通过后，向用户汇报以下内容并等待确认：
① 已确认的接线事实：三处注入的调用位置与实参如上；`useMapLayer` 传入 `null` 是**有意设计**（勇士初始不在任何楼层），不是遗漏。
② 由此的必然结果：`finder.find` 在 `layer` 为 `null` 时进入 `finder.ts:212-215` 的告警 173 早退分支并返回空数组，顶层录像瞬移随之下发 2005——仅取消 skip、不绑定事件层仍会红。
③ 用户已确认的处置方式（本计划据此执行）：**保持 `core.ts` 原样**；在**测试侧**于楼层激活后调用 `state.pathfinding.finder.useMapLayer(map.eventLayer)`，并把用例从 `plays a teleport step without manual finder wiring` **重命名为** `plays a teleport step after the event layer is bound on floor activation`（名字须反映其不再宣称「无需手动接线」）。
④ 需要登记的用户负责项（本阶段**不修复**）：`core.ts:238` 的 `useMapLayer(null)` 是一次性注入，楼层切换时事件层会变化（`gameMap.ts:43`），仓库内除测试外没有任何生产调用点在切层时重新注入；该缺口归用户所有，在 SUMMARY 登记为后续项，且不新建账本条目。
⑤ 验证方式与路由修订（D-09 首跑已证实）：接线有效（绑层后不再告警 173）；但原路由 `[Right, Teleport(1,0)]` 的瞬移目标与 `Right` 后勇士所在格 `(1,0)` 相同，`finder.find((1,0),(1,0))` 因 `startIndex === targetIndex` 早退返回空数组（`finder.ts:150`），`teleportTo` 据空路径返回 null（`system.ts:96-98`）→ 2005 → `ReplaySandbox.step` 返回 false、`ending` 未置位、`waitForEnded` 抛 `replay sandbox did not end`。故按用户裁决**改瞬移目标为 `(2,0)`**，断言改为 `x` 为 2、`y` 为 0。
⑥ 失败纪律：若绑定事件层后用例仍红，AI 必须**退出本计划**、如实汇报观测到的告警码与路径结果，并等待用户修订接线或修订本计划；**不得**自行修改 `core.ts`、不得改断言（D-07/D-09）。
⑦ 账本事实：`#06-07-1` 在 WINDOWS.md 中无对应条目，D-14 的 waive 没有可 waive 的对象；按用户确认不新建条目（避免抬高 `open_count` 门禁），改为在本计划 SUMMARY 记录用户负责项与验证结果。请用户确认。
  </action>
  <decision>用户是否确认「保持 core.ts 原样（layer 有意为 null）、由测试侧绑定 map.eventLayer 并重命名用例、取消 skip 验证、不新建账本条目」</decision>
  <context>
    `#06-07-1` 是唯一的顶层装配缺口，D-07 规定接线由用户完成。用户已完成接线并把事件层**有意**留为 `null`（勇士初始不在任何楼层），故「取消 skip」不再等价于「无需接线即可通过」：测试必须自己绑定事件层。楼层切换时的重注入是用户负责的后续缺口，本计划只登记、不实现、不评价。
  </context>
  <options>
    <option id="test-binds-layer">
      <name>（已确认）保持 core.ts 原样，测试侧绑定 map.eventLayer 并重命名用例，不新建账本条目</name>
      <pros>尊重用户对 core.ts 的设计（layer 有意为 null）；验证仍覆盖真实 maps/predicate 装配；账本 open_count 不变</pros>
      <cons>#06-07-1 的处置与切层重注入缺口只出现在 SUMMARY，不进入跨阶段账本</cons>
    </option>
    <option id="core-reinject-first">
      <name>用户先在 core.ts 补上切层重注入，再由 AI 执行本计划</name>
      <pros>若用户希望 layer 在切层时自动生效，可让测试绑定行为与生产行为一致</pros>
      <cons>超出本阶段范围（用户负责项）；AI 需退出本计划等待用户完成后再执行</cons>
    </option>
    <option id="not-wired">
      <name>接线已回退或缺失 —— 停止本计划</name>
      <pros>严格遵守 D-07，不越界修改生产代码</pros>
      <cons>本计划需等待用户恢复接线后重新执行；Phase 7 收尾延后</cons>
    </option>
    <option id="new-window-entry">
      <name>为 `#06-07-1` 新建账本条目并 waive</name>
      <pros>用户负责项进入跨阶段账本，后续阶段可见</pros>
      <cons>抬高 open_count，可能阻塞 /gsd-ship 的 windows_enforce 门禁</cons>
    </option>
    <option id="revise">
      <name>绑定方式、命名或验证方式需修订后再执行</name>
      <pros>避免带着错误方案落地</pros>
      <cons>退出本计划并修订 PLAN.md 后重新执行</cons>
    </option>
  </options>
  <resume-signal>默认按已确认的 test-binds-layer 继续；如需改为 core-reinject-first / not-wired / new-window-entry，或给出修订意见，请直接回复</resume-signal>
  <verify>
    <human-check>用户确认按 test-binds-layer 执行（或给出其它明确指示）</human-check>
    <fails_when>用户回复 not-wired / core-reinject-first / revise；或未回复即继续 —— 必须停止执行</fails_when>
  </verify>
  <acceptance_criteria>
    - `core.ts` 的装配区域存在 `useMapState`、`useMapLayer(null)`、`usePassPredicate` 三处调用（源码断言）
    - `useMapLayer(null)` 被记录为有意设计，且用户确认测试侧绑定事件层 + 重命名用例
    - 楼层切换重注入缺口被登记为用户负责后续项，且不新建账本条目
  </acceptance_criteria>
  <done>用户确认为 test-binds-layer，且 `core.ts` 三处注入在源码中可见（`:236`/`:238`/`:239-240`）</done>
</task>

<task type="auto">
  <name>Task 1: 测试侧绑定事件层、重命名用例并验证顶层录像瞬移转绿</name>
  <files>packages-user/data-state/test/replayPlayback.test.ts</files>
  <read_first>
    - packages-user/data-state/test/replayPlayback.test.ts（:80-116 createSmallMapScene/wireFinder 注入、:66-78 withReplayDisabled 返回 action() 的返回值、:287/:302/:320 忽略返回值的既有调用方、:360-373 目标 skip 用例）
    - packages-user/data-state/src/core.ts（:232-241 用户接线后的装配）
    - packages-user/data-system/src/path/finder.ts（:121-123 useMapLayer 接受 IMapLayer | null、:209-215 layer 为 null 时告警 173 并返回空数组）
  </read_first>
  <action>
仅改 `replayPlayback.test.ts` 一个文件，做四处加法式/最小变更，不动生产源码（`core.ts` 为用户产出，保持原样）。注意：D-09 首跑已证实——绑层后告警 173 消失（接线有效），但原路由 `[Right, Teleport(1,0)]` 的瞬移目标与 `Right` 后勇士所在格 `(1,0)` 相同，`finder.find((1,0),(1,0))` 因 `startIndex === targetIndex` 早退返回空数组（`finder.ts:150`），`teleportTo` 据空路径返回 null 并下发 2005，`sandbox.step()` 返回 false 致 `ended` 永假、`waitForEnded` 抛 `replay sandbox did not end`。故按用户裁决**改瞬移目标**（见 (b2)）：
(a) 让 `createSmallMapScene`（:85）**返回 `map`**：在函数体末尾（`resetHero(state)` 之后，且仍在 `if (!map || !map.eventLayer) throw` 造成的非空收窄范围内）加 `return map;`。既有调用方 :287/:302/:320 忽略返回值，不受影响；`withReplayDisabled`（:71-78）返回 `action()` 的返回值，故返回值可穿透该包装器。
(b) 在目标用例中捕获该 map 并在**楼层激活后**显式绑定事件层：把 `withReplayDisabled(state, () => createSmallMapScene(state, false));` 改为捕获返回值的形式（`const map = withReplayDisabled(state, () => createSmallMapScene(state, false));`），随后在 `runHeroStep`/`replay.record`/`playRoute` **之前**调用 `state.pathfinding.finder.useMapLayer(map.eventLayer);`。`wireFinder = false` 保持不变——该用例刻意不依赖 createSmallMapScene 内部的手动注入，只补上事件层。
(b2) 修正路由使瞬移目标 ≠ 勇士当前格：前置 `runHeroStep(state, FaceDirection.Right)` 后勇士位于 `(1,0)`，故把 `replay.record(ReplayCommandCode.Teleport, 1, 0)` 的目标改为 `(2,0)`（第二参数写 2），并把断言 `expect(state.hero.location.x).toBe(1)` 改为 `toBe(2)`（`y` 仍为 0）。这是用户裁决的「改瞬移目标」方案，用于规避 `finder.find` 的 `startIndex === targetIndex` 空路径早退（非 #06-07-1，属未登记的自目标行为）。
(c) 取消 skip 并重命名：`it.skip('plays a teleport step without manual finder wiring', ...)` 改为 `it('plays a teleport step after the event layer is bound on floor activation', ...)`；同步把 :360-361 的中文注释从「疑似缺陷 #06-07-1 …… 修复后取消 skip」改为中性描述（说明本用例在楼层激活后由测试侧绑定事件层、验证顶层录像瞬移仍可解析并到达目标），不得再出现「疑似 bug / 待修复」措辞。

断言随路由修订为 `expect(state.hero.location.x).toBe(2)` 与 `expect(state.hero.location.y).toBe(0)`（用户已授权「改瞬移目标」，用于规避自目标空路径；除此之外断言不再另改）；不改 :108-114 的 `wireFinder` 分支；不改该文件任何其它用例；不新增用例；不改任何生产源码；不引入新依赖、不新建文件。

若用例仍红：立即停止并按 Task 0 的第 ⑥ 条退出汇报（记录实际告警码、找到的路径与首个分歧），不得改断言、不得改 `core.ts`。

验证：`pnpm exec vitest run packages-user/data-state/test/replayPlayback.test.ts` 全绿 → D-44 门禁三步（本计划仅一个测试文件）。提交信息：`test(07-08): #06-07-1 bind the event layer in the teleport playback test`。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-state/test/replayPlayback.test.ts</automated>
    <fails_when>非零退出，或摘要行出现 "1 failed" / "failed"（仍告警 173 / 路径为空 / 下发 2005），或输出出现 "no tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `replayPlayback.test.ts` 中 `plays a teleport step without manual finder wiring` 已不存在；`it('plays a teleport step after the event layer is bound on floor activation', ...)` 不以 `.skip` 存在（源码断言：该行不含 `.skip`）
    - `createSmallMapScene` 返回 `map`，且目标用例在 `playRoute` 之前调用 `state.pathfinding.finder.useMapLayer(map.eventLayer)`
    - `core.ts` 保持原样（`:236` 提供 `useMapState`、`:239-240` 提供 `usePassPredicate`、`:238` 的 `useMapLayer(null)` 不变）
    - 目标用例的瞬移目标为 `(2,0)`（`replay.record(ReplayCommandCode.Teleport, 2, 0)`），断言为 `x=2`/`y=0`
    - `pnpm exec vitest run packages-user/data-state/test/replayPlayback.test.ts` 全绿
    - 本计划未修改任何生产源码（以本计划提交 sha 核验：`git show --name-only <本计划提交 sha>` 仅含该测试文件与 .planning 文档）
  </acceptance_criteria>
  <done>用例已重命名并在测试侧绑定事件层后转绿，三个 `wireFinder=true` 兄弟用例未改动，且未触碰任何生产源码；已一个原子提交入库</done>
</task>

<task type="auto">
  <name>Task 2: D-44 门禁、全量套件与用户负责项处置登记</name>
  <files>packages-user/data-state/test/replayPlayback.test.ts</files>
  <read_first>
    - .planning/phases/07-data-fixes/07-VALIDATION.md（§Manual-Only Verifications 的 #06-07-1 行）
    - .planning/phases/07-data-fixes/07-RESEARCH.md（§WINDOWS.md 收口映射「需澄清」、§Open Questions）
    - packages-user/data-state/src/core.ts（:232-241，供 SUMMARY 逐字记录接线实参）
  </read_first>
  <action>
对唯一改动文件串行执行 D-12（沿用 D-44 的文件级判定）三步门禁：`pnpm exec eslint --fix` 后 `pnpm exec eslint` 0 错误；`pnpm exec vue-tsc --noEmit` 后按该文件相对路径过滤输出 0 类型错误（不以整仓退出码判定）；`pnpm test:ci` 全绿。

账本：`#06-07-1` 在 WINDOWS.md 中无对应条目，**不新建条目**（Task 0 已确认 test-binds-layer）；若用户在 Task 0 改选 `new-window-entry`，则按用户指示执行 `windows` 的 append/waive 流程并在 SUMMARY 记录 `open_count` 变化。

在 SUMMARY 中记录以下四类内容：
① 已确认的接线事实（源码逐字）：`core.ts:236 pathfinding.finder.useMapState(this.maps)`、`:238 pathfinding.finder.useMapLayer(null)`（含中文注释原意：初始状态下勇士不在任何楼层，切换楼层后再具体设置）、`:239-240 usePassPredicate(new DefaultPassPredicateImpl(this.maps))`；用户接线提交 `1ff22dd`。
② `core.ts` 的 `layer` 为 `null` 是**有意设计**（非遗漏），因此单纯取消 skip 仍会命中 `finder.ts:212-215` 的告警 173 → 空路径 → 2005。
③ 本计划的测试侧处置：`createSmallMapScene` 返回 `map`（加法式）；目标用例在楼层激活后调用 `state.pathfinding.finder.useMapLayer(map.eventLayer)`；用例由 `plays a teleport step without manual finder wiring` 重命名为 `plays a teleport step after the event layer is bound on floor activation`；瞬移目标由 `(1,0)` 改为 `(2,0)`、断言改为 `x=2`/`y=0`（用户裁决的「改瞬移目标」）。
④ 剩余的用户负责缺口（本阶段**不修复**）：`core.ts:238` 的一次性 `useMapLayer(null)` 在楼层切换（`gameMap.ts:43` 的 `eventLayer` 变化）时不会被重新注入，仓库内无对应生产调用点；登记为用户负责后续项，D-14 账本无 `#06-07-1` 条目、不新建。
⑤ 未登记缺陷登记（本阶段**不修复**，仅登记）：D-09 首跑发现 `PathfindingSystem.teleportTo` 对「空路径 / 瞬移目标等于勇士当前格」返回 null（`system.ts:96-98`，源于 `finder.find` 的 `startIndex === targetIndex` 早退，`finder.ts:150`），进而下发 2005，并使 `ReplaySandbox.step` 返回 false、`ending` 未置位、`waitForEnded` 抛 `replay sandbox did not end`。该行为属未登记缺陷、超出 `#06-07-1` 范围，用户裁决为「仅登记不修」，登记于 SUMMARY。
  </action>
  <verify>
    <automated>pnpm test:ci</automated>
    <fails_when>非零退出，或摘要行出现 "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm exec eslint` 对该文件 0 错误
    - `pnpm exec vue-tsc --noEmit` 按该文件路径过滤后无命中
    - `pnpm test:ci` 全绿，且本计划未新增 `it.skip`
    - SUMMARY 含①接线事实（`1ff22dd` 与三处实参）、②layer 有意为 null、③测试侧绑定 `(2,0)` 目标与重命名、④楼层切换重注入的用户负责缺口登记、⑤`teleportTo` 自目标空路径缺陷登记（不修）
  </acceptance_criteria>
  <done>D-44 三步全过、全量套件绿、四类登记内容均写入 SUMMARY（提交粒度按 D-13 一个缺陷一个原子提交）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 顶层装配 → finder | `core.ts` 已注入 `maps` 与谓词，但 `layer` 有意为 `null`；测试必须自行绑定事件层，属装配边界而非安全边界 |
| 用户代码 → AI 范围 | 接线已由用户落地（D-07）；AI 只改测试侧，越界修改 `core.ts` 即违反协作边界 |
| 包管理器 → 仓库 | 本阶段无安装动作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-14 | DoS（功能性） | `path/finder.ts` 的 `layer = null` 早退路径（告警 173）与楼层切换重注入缺口 | low | accept | 接线由用户完成（D-07）；`layer = null` 为有意设计，由测试侧显式绑定 `map.eventLayer` 规避；切层重注入属用户负责后续项，本阶段登记于 SUMMARY 而不修改生产代码 |
| T-7-SC | Tampering | npm/pip/cargo 安装 | high | accept | 本阶段不安装任何包（`package.json`/`pnpm-lock.yaml` 不变）；若执行中出现安装需求，立即停止并汇报 |
</threat_model>

<verification>
- `core.ts` 中存在 `useMapState`（`:236`）/`useMapLayer(null)`（`:238`）/`usePassPredicate`（`:239-240`）三处注入（用户产出，本计划只读）
- `replayPlayback.test.ts` 的目标用例已重命名为 `plays a teleport step after the event layer is bound on floor activation` 且不以 `it.skip` 存在
- `createSmallMapScene` 返回 `map`；该用例在 `playRoute` 之前调用 `state.pathfinding.finder.useMapLayer(map.eventLayer)`
- 目标用例的瞬移目标为 `(2,0)`、断言为 `x=2`/`y=0`；SUMMARY 登记 `teleportTo` 自目标空路径缺陷（不修）
- 改动文件仅 `replayPlayback.test.ts`（AI 未触碰生产源码；`git show --name-only <本计划提交 sha>` 可核验）
- `pnpm exec vitest run packages-user/data-state/test/replayPlayback.test.ts` 全绿
- `pnpm test:ci` 全绿且本计划不新增 `it.skip`
</verification>

<success_criteria>
1. 确认 `core.ts` 的三处 finder 注入已由用户落地（D-07），且 `useMapLayer(null)` 为有意设计
2. 测试侧在楼层激活后绑定 `map.eventLayer`，瞬移目标改为 `(2,0)`，`#06-07-1` 用例重命名、取消 skip 后转绿
3. AI 未修改任何生产源码；失败时按 D-09 退出并汇报
4. 用户负责的楼层切换重注入缺口与账本处置（不新建条目）在 SUMMARY 登记
5. `teleportTo` 自目标空路径导致的 2005 / sandbox 挂起缺陷登记于 SUMMARY（本阶段不修）
</success_criteria>

## Artifacts this phase produces

| 类型 | 路径 | 内容 |
|------|------|------|
| 用户产出（AI 只读） | `packages-user/data-state/src/core.ts` | `pathfinding.finder` 的三处注入（`:236`/`:238`/`:239-240`）；`useMapLayer(null)` 有意为 null |
| 测试（修改） | `packages-user/data-state/test/replayPlayback.test.ts` | `createSmallMapScene` 返回 `map`；目标用例测试侧绑定 `map.eventLayer`、重命名并取消 skip |
| 摘要 | `.planning/phases/07-data-fixes/07-08-SUMMARY.md` | 接线事实、layer 有意为 null、测试侧绑定与重命名、切层重注入用户负责缺口登记、账本处置 |

<output>
Create `.planning/phases/07-data-fixes/07-08-SUMMARY.md` when done
</output>
