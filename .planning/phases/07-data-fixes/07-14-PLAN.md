---
phase: 07-data-fixes
plan: 14
type: execute
wave: 14
depends_on:
  - 07-13
files_modified:
  - packages-user/data-fallback/src/hero.ts
  - packages-user/data-fallback/src/hero.test.ts
autonomous: false
requirements:
  - FIX-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`core.status.hero` 代理的属性读写始终作用于**当前活属性**：按 Task 0 Q1 裁决，代理不再闭包持有 `getModifiableAttribute()` 的一次性结果（或按裁决在加载后重绑），并有回归断言"
    - "`patchHero` 的既有可观测语义不变：`set` 触发告警 56 并写入属性、`get` 触发告警 56 并返回 {Q2 裁决的} 属性值；`core.status.hero.flags` 仍被替换为拒绝写入/读取并告警 54 的代理，`window.flags` 同样指向它"
    - "`resetHero` 钩子触发时仍完成 patch 安装（既有行为不变），新增回归用例锚定「活属性跟随」"
    - "`pnpm test:ci` 全绿，不新增 `it.skip`；本计划为该包首个测试文件，不得因引入测试而改动其它包的既有断言"
  artifacts:
    - path: packages-user/data-fallback/src/hero.ts
      provides: "`core.status.hero` 代理按 Q1 裁决改为活属性读取/重绑；`patchHero` 的 jsDoc 写清代理的属性解析口径与 legacy 侧的约束"
    - path: packages-user/data-fallback/src/hero.test.ts
      provides: "该包首个测试文件：`resetHero` → 代理安装、活属性跟随（换属性实例/换 hero 后读写落到新实例）、告警 56/54 口径、`window.flags` 绑定"
  key_links:
    - "`patchHero` 的 `const attr = state.hero.getModifiableAttribute()`（`hero.ts:8`）↔ 代理的 `set`/`get` 陷阱（`:14`/`:20`）——闭包持有一次性结果是 `#06-17-7` 的根因"
    - "`hook.on('resetHero', …)`（`hero.ts:6`）↔ `GameEvent` 的 `resetHero: [hero: any]`（`data-base/src/game.ts:151-152`）与 `hook.on('loadData')`（`game.ts:149-150`，由 legacy `project/functions.js` 发出）——Q1 选项 B 的重绑触发点"
    - "`core.status.hero.flags` 代理（`hero.ts:26-36`）与 `window.flags`（`:37`）↔ 码 54 的拒绝语义"
    - "`state.hero.getModifiableAttribute()`（`data-base/src/hero/state.ts:77-79`，07-09 后返回 `readonly attribute` 同一实例）——数据侧已保证属性实例稳定，本计划处理的是「hero/属性实例被整体替换」的兜底"
  assumptions:
    - "A1（D-10 例外，本计划授权）：本计划为修复计划，为 `#06-17-7` 新增回归是必需项；同时为该包建立首个测试文件（此前 `packages-user/data-fallback` 零测试）"
    - "A2（不改数据侧）：数据侧 `HeroState`/`HeroAttribute` 的行为由 07-09 已定（属性原地读档、实例稳定），本计划**不**修改 `packages-user/data-base`/`data-state` 的任何文件；若实现中发现必须新增数据侧钩子，暂停并回到 Task 0 关卡追加裁决"
    - "A3（legacy 边界）：`data-fallback` 属数据端（`packages-user` 内、非渲染端），其 patch 面向仓外旧引擎；本计划不引入新依赖、不改 legacy 插件与渲染端"
    - "A4（测试环境）：该包测试需 stub 全局 `main`（`{ replayChecking: true }`，避免 `data-base/src/game.ts` 模块级 `GameListener` 触碰 `window`）与 `core`/`window`；`hook` 为 `@user/data-base` 导出的模块级单例（`game.ts:155`），可 `emit('resetHero', hero)`"
  prohibitions:
    - "不改 `packages-user/data-fallback/src/flag.ts`（`patchFlags` 闭包的是稳定的 `FlagSystem` 实例，审计未列为缺陷）；若 Q4 裁决要求一并处理，须在 `<record>` 明确授权后才可改"
    - "不改 `packages-user/data-fallback/src/index.ts` 的 `patchAll` 组成，除非 Q3 裁决明确要求"
    - "不改任何数据侧源码（`data-common`/`data-base`/`data-system`/`data-state`）与渲染端 `@user/client-*`"
    - "不引入新依赖（含测试框架依赖：复用仓库既有 vitest）、不新增 `import type`、不使用连续 `as` 断言、`package.json` 不改（vitest 默认含 `*.test.ts`，无需新增脚本）"
    - "不新增 `it.skip`；不改其它包的既有断言；不为转绿弱化断言"
---

<objective>
闭合审计条目 C（`#06-17-7`）：`packages-user/data-fallback/src/hero.ts` 的 `core.status.hero` 代理闭包持有 `state.hero.getModifiableAttribute()` 的一次性结果（`hero.ts:8`），旧引擎侧对 `core.status.hero` 的读写会落到**被替换掉的旧属性实例**上。

现状（锚点）：`patchHero`（`hero.ts:5-39`）在 `resetHero` 钩子里取 `const attr = state.hero.getModifiableAttribute()`（`:8`），代理的 `set`（`:14`）与 `get`（`:20`）都引用该闭包变量。07-09 已让 `HeroAttribute` 在自身实例上原地读档，因此**常态路径**下该闭包仍然有效；但若 `state.hero` 整体被替换（或仓外旧引擎在读档后未重发 `resetHero`），代理会持续读写孤儿属性——审计把该条判为「低-中，取决于仓外 `resetHero` 是否重发」，属未封口的兜底缺口。

Purpose: legacy 数据接口（编辑器与旧引擎样板）是数据端对外的一层薄壳，它的引用一旦脱钩不会报错、只会静默写错对象；把「每次访问都解析活属性」作为契约，可以让这层薄壳不依赖任何调用顺序假设，与 07-09/07-11 建立的同引用原则收尾一致。

Output: `hero.ts` 的代理解析口径调整（按 Task 0 Q1 裁决）+ 该包首个测试文件 `hero.test.ts`（活属性跟随回归 + 告警 54/56 口径）。

**Artifacts this phase produces（本计划产出物）**：修改 `packages-user/data-fallback/src/hero.ts`；**新建** `packages-user/data-fallback/src/hero.test.ts`（该包首个测试）；无新增依赖、无新增脚本。

**Phase 7 验证已失效**：`07-VERIFICATION.md`（2026-09-16T06:04:11Z）已因 07-09 与本轮 07-10..07-14 失效。本计划执行完毕后**必须重跑 `/gsd-verify-work`**（或 `gsd-verify-work 7`）重新出具验证结论。

Phase 7 裁决对应（逐条引用）：D-01: 本计划是 Phase 7 第 14 个计划，承接 `07-LOADSTATE-AUDIT.md` C 行（`#06-17-7`）。D-02: 属 legacy 兼容层（`packages-user/data-fallback`）。D-03/D-04/D-05/D-06: 其它系统契约裁决，不涉及。D-07: path 接线为用户负责项，不涉及（本计划不改 `path/system.ts`）。D-08: enemy，不涉及。D-09: 由 Task 0 关卡承载。D-10: 本计划为修复计划，回归用例为必需项（A1）。D-11: 代理契约变更需同步 `patchHero` 的 jsDoc。D-12: 沿用 06-CONTEXT D-44 文件级三步门禁。D-13: 单一根因，一个 fix commit + 一个 test commit（或合并为一个 `fix(07-14)` 提交，按 D-13 的原子粒度判断）。D-14: 该条无 `WINDOWS.md` 条目，只登记不新建。

执行序：`depends_on: 07-13`（Wave 14）。门禁型依赖（D-09 逐计划裁决关卡 + D-12 全量测试基线逐计划重录）；源码层面与 07-10..07-13 无耦合（本计划只碰 `data-fallback`）。

**基线（2026-09-16 实测）**：`pnpm test:ci` = 66 文件 / 690 通过 / 1 跳过（`equipment.test.ts:306` 码 147，D-06）。本计划执行后文件数应变为 67（新增 `packages-user/data-fallback/src/hero.test.ts`），通过数相应增加；这是唯一被允许的基线变化，必须在 SUMMARY 说明。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-fixes/07-CONTEXT.md
@.planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md
@.planning/phases/07-data-fixes/07-09-PLAN.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@.planning/codebase/TESTING.md
@dev.md
@packages-user/data-fallback/src/hero.ts
@packages-user/data-fallback/src/index.ts
@packages-user/data-fallback/src/flag.ts
@packages-user/data-base/src/game.ts
@packages-user/data-base/src/hero/state.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: 契约裁决关卡（D-09）——代理解析口径（每次读活属性 vs 加载后重绑 vs 仅文档）、`get` 返回基础属性还是最终属性、测试落点与测试入口、`patchFlags` 是否一并处理</name>
  <files>（只读汇报，不修改任何文件）</files>
  <read_first>
    - packages-user/data-fallback/src/hero.ts（全文件：:8 `attr` 闭包、:9-22 代理陷阱、:23 `core.status.hero` 赋值、:26-37 flags 代理与 `window.flags`）
    - packages-user/data-fallback/src/index.ts（`patchAll` 组成）
    - packages-user/data-fallback/src/flag.ts（`patchFlags` 闭包的是稳定 `FlagSystem` 实例；`switchName` 使用全局 `core`）
    - packages-user/data-fallback/package.json（依赖与脚本现状：无 test 脚本，由根 vitest 默认发现 `*.test.ts`）
    - packages-user/data-base/src/game.ts（:147-155 `loadData` / `resetHero` 事件声明与 `export const hook`；:166-184 `GameListener` 构造器对 `main`/`window` 的依赖）
    - packages-user/data-base/src/hero/state.ts（:77-79 `getModifiableAttribute`、:138-153 `loadState`——07-09 后属性实例原地读档）
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（C 行、触发序列、未确定项 1）
    - .planning/codebase/TESTING.md（vitest 约定、`logger.catch` 断言模式、`vi.hoisted` stub 全局的既有写法）
  </read_first>
  <decision>
    `#06-17-7` 的修复口径与测试落点：
    （Q1）代理的活属性解析方式（每次访问解析 vs 依赖 `loadData`/`resetHero` 重绑 vs 仅文档约束）；（Q2）`get` 陷阱返回基础属性还是最终属性；（Q3）回归测试的落点与入口（经真实 `patchHero` + 全局 stub，还是抽出具名工厂直接测）；（Q4）`patchFlags` 的同类闭包是否一并处理。
  </decision>
  <context>
    Q1 决定这层薄壳是否依赖调用顺序：选项 A 让代理在每次 `get`/`set` 时解析 `state.hero.getModifiableAttribute()`，任何替换都自动跟随，代价是每次访问多一次方法调用；选项 B 复用既有事件（`hook.on('loadData')` 由 legacy `project/functions.js` 发出 / `resetHero`）在加载后重绑，代价是依赖 legacy 侧真的发出该事件（审计的「未确定项 1」正是这一点无法在仓内确证）。
    Q2 是行为问题：今日 `get` 返回 `attr.getBaseAttribute(p)`（基础值），若改为 `getFinalAttribute`（含加成），旧引擎读到的数值会变化——必须由用户拍定。
    Q3 是工程问题：`data-fallback` 目前零测试，经真实 `patchHero` 测需要 stub 全局 `core`/`window`/`main` 并 `emit('resetHero')`（贴近真实装配路径）；抽出口工厂则测试更简单但会新增一个模块级导出（`dev.md` 的模块/导出原则）。
    Q4 是范围问题：`patchFlags` 闭包的是稳定的 `FlagSystem` 实例（审计判定安全），但其中 `switchName` 读全局 `core.status.floorId`，与本次同族。
  </context>
  <options>
    <option id="q1-live">
      <name>Q1 选项 A：【建议默认】代理陷阱内每次访问解析活属性（`const attr = state.hero.getModifiableAttribute()` 移入 `set`/`get` 内部）</name>
      <pros>不依赖任何调用顺序与仓外事件；`state.hero` 或属性实例被替换后自动跟随；改动极小（一处提取移动）且无新增导出；与 07-09/07-11 的同引用原则互补（数据侧保证不替换，兼容层不假设不替换）</pros>
      <cons>每次属性读写多一次 `getModifiableAttribute()` 调用（薄壳路径，非热点循环）；若未来 `state.hero` 在单次读写之间被替换，读写会落在不同实例上（语义上正是期望行为）</cons>
    </option>
    <option id="q1-rebind">
      <name>Q1 选项 B：保留闭包，但在 `hook.on('loadData')` / `resetHero` 时重建代理并重新赋值 `core.status.hero`</name>
      <pros>每次访问零额外开销；代理只在装配点创建一次</pros>
      <cons>依赖 legacy 侧真的发出 `loadData`（仓内无法确证，审计「未确定项 1」）；遗漏任一加载路径即回到原缺陷；`core.status.hero` 指向的对象会在加载时变化（外部若缓存过该代理会脱钩）</cons>
    </option>
    <option id="q1-document">
      <name>Q1 选项 C：不改行为，在 `patchHero` 的 jsDoc 写明「读档后必须重发 `resetHero`，否则代理指向旧属性」并登记为已接受契约</name>
      <pros>零代码风险、零测试环境成本</pros>
      <cons>把缺陷转为「调用方必须记住」的文档约定；与「本轮修复 15 项」的指令冲突（清单第 15 条，用户已把该条列入修复范围）</cons>
    </option>
    <option id="q2-base">
      <name>Q2 选项 A：【建议默认】保持 `get` 返回 `attr.getBaseAttribute(p)`（基础值）与 `set` 写 `attr.set(p, v)`，仅把解析目标改为活属性</name>
      <pros>行为完全不变（只修引用解析），既有 legacy 语义与数值口径零回归；测试只需断言「读写落在活属性上」</pros>
      <cons>旧引擎经该代理读不到装备/修饰器加成（现状即如此，属既有契约）</cons>
    </option>
    <option id="q2-final">
      <name>Q2 选项 B：`get` 改为返回 `attr.getFinalAttribute(p)`（含修饰器/装备加成）</name>
      <pros>旧引擎读到的数值与游戏内最终数值一致</pros>
      <cons>改变 legacy 可观测行为（旧引擎若用基础值做存档/编辑器换算会出现数值跳变）；需要额外的回归基线来确定哪些旧接口受影响</cons>
    </option>
    <option id="q3-real">
      <name>Q3 选项 A：【建议默认】新建 `packages-user/data-fallback/src/hero.test.ts`，经真实 `patchHero` 测试：`vi.hoisted` stub `main`/`location`，stub 全局 `core`（含 `status.hero` 与 `flags`）与 `window`，用 `@user/data-base` 导出的 `hook` 发出 `resetHero`</name>
      <pros>测的是真实装配路径（含 `hook` 订阅、`core.status.hero` 赋值、`window.flags` 绑定），回归价值最高；不改生产导出面</pros>
      <cons>需要构造若干全局桩；类型上对全局 `core`/`window` 的赋值可能需要 `// @ts-expect-error`（按 `dev.md:91` 说明原因，不使用连续 `as`）</cons>
    </option>
    <option id="q3-factory">
      <name>Q3 选项 B：从 `hero.ts` 抽出具名工厂（如同文件内导出的 `createHeroProxy(state, hero)`），`patchHero` 调用它，测试直接测工厂</name>
      <pros>测试无需全局桩，断言更聚焦；代理构造逻辑可复用</pros>
      <cons>新增模块级导出（`dev.md` 的模块/导出原则要求导出内容属于本包，技术上合规但扩大公开面）；测试不再覆盖 `hook`/`core`/`window` 装配路径</cons>
    </option>
    <option id="q3-both">
      <name>Q3 选项 C：既抽工厂又保留一条经真实 `patchHero` 的装配用例</name>
      <pros>覆盖最全</pros>
      <cons>文件与用例数量翻倍，超出本条缺陷需要的回归强度</cons>
    </option>
    <option id="q4-skip">
      <name>Q4 选项 A：【建议默认】`patchFlags` 不处理（其闭包指向稳定的 `FlagSystem` 实例，审计未列为缺陷）</name>
      <pros>严格守住清单边界（第 15 条只提 hero 代理）</pros>
      <cons>`switchName` 读全局 `core.status.floorId` 的耦合保留（不属本次缺陷）</cons>
    </option>
    <option id="q4-include">
      <name>Q4 选项 B：一并把 `patchFlags` 的 `state.flags` 解析改为活引用（若 `state.flags` 可被替换）</name>
      <pros>同族模式一并封口</pros>
      <cons>超出清单；`FlagSystem` 实例在 07-11 后本身是稳定对象（字段实例也可保留），改动收益低、回归面增加</cons>
    </option>
    <option id="revise">
      <name>（兜底）修订方案后再执行</name>
      <pros>避免带错误契约落地</pros>
      <cons>按 D-09 退出本次修改、修订本 PLAN.md 后重新执行，不得自行另辟他法</cons>
    </option>
  </options>
  <record>
    **裁决日期：2026-09-17（经用户裁定，D-09 关卡已闭合）。**

    **用户原话（逐字）**：「关于旧引擎的兼容部分不动，很快就要删除了，没必要改。」

    **逐条裁决（Q1..Q4）**：

    | 问题 | 裁决 | 说明 |
    | --- | --- | --- |
    | Q1 代理的活属性解析方式 | **不改动** | 选项 A（每次访问解析）/ 选项 B（加载后重绑）/ 选项 C（仅文档）**均不采用**——兼容层整体不动，不引入任何代理解析口径变更 |
    | Q2 `get` 返回基础属性还是最终属性 | **不改动** | 保持现状（`attr.getBaseAttribute(p)`），不改变 legacy 可观测数值口径 |
    | Q3 回归测试的落点与入口 | **不新建测试** | 不创建 `packages-user/data-fallback/src/hero.test.ts`、不抽工厂；`packages-user/data-fallback` 维持零测试现状 |
    | Q4 `patchFlags` 是否一并处理 | **不改动** | `flag.ts` 与 `index.ts` 一行不动 |

    **裁决理由**：`packages-user/data-fallback` 是面向仓外旧引擎的**兼容层**，按规划**即将删除**，为很快消失的代码修复引用口径并新建测试不具价值。审计条目 C（`#06-17-7`）的严重度本身为「低-中，取决于仓外 `resetHero` 是否重发」，风险随兼容层删除而自然消解。

    **处置结论**：`#06-17-7`（审计条目 C）判定为 **不修复（wontfix）／经用户裁定不修复**；本计划因此**产出零代码改动、零新增测试、零新增依赖**。

    **本计划属 Phase 7 成功标准第 1 条的合法处置**：`ROADMAP.md` Phase 7 成功标准第 1 条为「06-TEST-FINDINGS.md 登记的数据端疑似缺陷全部处置完毕（**修复或经用户裁定改契约/不修复**）」——本裁决即其中「经用户裁定不修复」一支，条目已闭合而非遗留。

    **对 Task 1–2 的效力**：**Task 1 整体跳过**（不得修改 `hero.ts`、不得新建 `hero.test.ts`、不得触碰 `flag.ts`/`index.ts`）；**Task 2** 仅执行「门禁/基线」与「SUMMARY 登记」部分——因无改动文件，文件级 `eslint`/`vue-tsc` 门禁为空集（**vacuous clean**），以 `pnpm test:ci` 作为 Phase 7 收口基线；SUMMARY 须明写本裁决、零改动事实与「计划声明的 `hero.ts` 修改 / `hero.test.ts` 未产出」为**已登记偏差**（而非执行不完整）。
  </record>
  <resume-signal>逐条给出 Q1..Q4 的裁决（例如「Q1=A, Q2=A, Q3=A, Q4=A」）；若要修订方案，回复修订点，执行者退出并按 D-09 修订本 PLAN.md</resume-signal>
  <action>向用户汇报上述 4 个契约点，重点说明 Q2 对 legacy 可观测数值的影响、Q1 对「是否依赖仓外事件」的取舍、Q3 对测试环境（全局 stub）的影响。不得在裁决前修改任何文件。</action>
  <verify>
    <human-check>`<record>` 中 Q1..Q4 均有明确裁决，且与用户实际意图一致</human-check>
    <fails_when>记录为空或与用户实际意图不符 —— 必须暂停并请用户澄清，不得按「建议默认」自行推进</fails_when>
  </verify>
  <acceptance_criteria>
    - Q1（解析口径）、Q2（`get` 语义）、Q3（测试落点与入口）、Q4（`patchFlags` 是否处理）均有用户裁决并写入 `<record>`
    - Q3 已明确测试文件路径与全局 stub 范围
    - 未在裁决前修改任何文件
  </acceptance_criteria>
  <done>Q1..Q4 裁决已完整写入 `<record>`；执行者据记录直接进入 Task 1，不再重复询问</done>
</task>

<task type="auto">
  <name>Task 1: `#06-17-7` —— `core.status.hero` 代理跟随活属性（按 Q1/Q2/Q3 裁决实现）+ 该包首个回归测试文件</name>
  <files>packages-user/data-fallback/src/hero.ts, packages-user/data-fallback/src/hero.test.ts</files>
  <read_first>
    - packages-user/data-fallback/src/hero.ts（全文件）
    - packages-user/data-base/src/game.ts（:147-155 `loadData`/`resetHero` 事件与 `export const hook`、:166-184 `GameListener` 对 `main`/`window` 的依赖）
    - packages-user/data-base/src/hero/state.ts（:77-79 `getModifiableAttribute` 返回类型 `IHeroAttribute<THero>`）
    - packages-user/data-base/src/hero/types.ts（`IHeroAttribute` 的 `getBaseAttribute`/`getFinalAttribute`/`set` 签名；`IReadonlyHeroAttribute`）
    - .planning/codebase/TESTING.md（`vi.hoisted` stub 全局、`logger.catch` 断言模式的既有写法）
    - 参考既有测试的 stub 写法：packages-user/data-common/src/replay/array.test.ts:7-25、packages-user/data-system/src/combat/mapDamage.test.ts:19-43
  </read_first>
  <action>
    (1) **按 Q1 裁决修改 `hero.ts`**：选项 A → 删除 `:8` 的 `const attr = state.hero.getModifiableAttribute()` 闭包，改为在 `set`（`:10-16`）与 `get`（`:17-21`）陷阱体内各自 `const attr = state.hero.getModifiableAttribute();` 后使用（保持告警 56 的调用与 `set` 的 `target[p] = newValue` 顺序不变）；选项 B → 保留代理构造，按裁决监听 `hook.on('loadData')`（或 `resetHero`）重建并重新赋值 `core.status.hero`；选项 C → 不改行为，把契约写进 jsDoc 并在 SUMMARY 登记；
    (2) **按 Q2 裁决**：`get` 返回 `attr.getBaseAttribute(p)` 或 `attr.getFinalAttribute(p)`，并同步 `patchHero` 的中文 jsDoc（写在函数上方，说明该代理解析的是活属性、以及返回值口径）；
    (3) 其余行为逐条保持：`:11` 与 `:18` 的 `logger.warn(56, 'core.status.hero', 'state.hero')`、`:12` 的 `target[p] = newValue`、`:23` 的 `core.status.hero = proxy`、`:26-37` 的 flags 代理与 `window.flags` 绑定（按 Q4 裁决决定是否触碰 `flag.ts`，默认不动）；
    (4) **新建 `hero.test.ts`**（按 Q3 裁决；文件头用中文单行注释说明覆盖内容，与仓库其它测试一致）：
      - `vi.hoisted(() => { vi.stubGlobal('main', { replayChecking: true }); vi.stubGlobal('location', { origin: 'http://localhost' }); })` + `afterAll(() => vi.unstubAllGlobals())`，与既有测试同款；
      - 构造假 `core`（`{ status: { hero: { flags: {} } } }` 形态）与假 `window` 并 `vi.stubGlobal`，取 `hook`（`@user/data-base` 导出）后 `hook.emit('resetHero', fakeHero)` 触发 `patchHero` 的装配；
      - 假 `state`：`{ hero: { getModifiableAttribute: () => currentAttr } }`（`currentAttr` 可切换），断言：
        (a) 装配后 `core.status.hero` 是代理对象且 `core.status.hero.flags` 为拒绝写入/读取的代理、`window.flags` 指向同一对象；
        (b) **活属性跟随**：切换 `currentAttr`（模拟属性实例被替换）后经 `core.status.hero.xxx` 读写 → 断言落在**新**属性实例上（旧实例零改动），并按 Q2 裁决断言 `get` 的返回值口径；
        (c) 告警口径：`logger.catch(() => { core.status.hero.atk = 1; })` 含 56；对 flags 代理的读写含 54；
      - 类型处理遵循 `dev.md`：不使用连续 `as`；无法避免时用 `// @ts-expect-error` 并写明原因；
      - 若 Q3 选了 B/C（抽工厂），则按裁决同时/改为直接测工厂，并保留装配路径用例（若裁决要求）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-fallback/src/hero.test.ts</automated>
    <fails_when>切换活属性实例后经代理读写仍落在旧属性上（Q1 选 A 时）；或告警 54/56 口径变化；或 `core.status.hero.flags` / `window.flags` 绑定丢失 —— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - 代理按 Q1 裁决跟随活属性（选项 A）或按裁决重绑/文档化（B/C），并有断言
    - `get` 返回值口径与 Q2 裁决一致
    - `hero.test.ts` 覆盖：装配、活属性跟随、告警 56、flags 代理与 `window.flags`（告警 54）
    - 仅改动 `hero.ts` 与本计划新增的 `hero.test.ts`；`flag.ts`/`index.ts`/数据侧文件零改动（除非 Q4 授权）
  </acceptance_criteria>
  <done>`core.status.hero` 代理不再闭包过期属性，读写始终作用于活属性；该包首个测试文件锚定装配与跟随语义（`#06-17-7` / 审计 C 关闭）</done>
</task>

<task type="auto">
  <name>Task 2: D-12/D-44 门禁 + 全量套件（含新增测试文件后的基线核对）+ 缺口登记与 Phase 7 验证失效说明</name>
  <files>.planning/phases/07-data-fixes/07-14-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（C 行与处置段）
    - .planning/phases/07-data-fixes/07-VERIFICATION.md（头部 verified/status）
    - .planning/ROADMAP.md（Phase 7 段 Plans 与 Wave 列表）
    - .planning/WINDOWS.md（确认该条无入账 id）
    - .planning/codebase/TESTING.md（`pnpm test:ci` 口径与文件数说明）
  </read_first>
  <action>
    (1) **门禁（D-12 / D-44 文件级）**：对 `hero.ts` 与新增的 `hero.test.ts` 执行 `pnpm exec eslint --fix <文件>` 后 `pnpm exec eslint <文件>` 0 错误；**文件级类型门禁按 06-RESEARCH Pitfall 4 的判定方式**：`pnpm exec vue-tsc --noEmit 2>&1 | Select-String -Pattern "packages-user/data-fallback/src/hero"` 无命中（**不得**用「退出码非 0」判定失败）；`pnpm test:ci` 全绿——**基线与执行前对比**：文件数由 66 变为 67（新增该包首个测试文件），通过数相应增加，跳过项仍只能是 `equipment.test.ts:306` 码 147；若新增文件未被 vitest 发现（分母仍为 66），记录原因并确认该用例确实被根 vitest 默认 include 覆盖（必要时在 SUMMARY 说明）。
    (2) **原子提交（D-13）**：`hero.ts` 的实现与 `hero.test.ts` 可合并为一个 `fix(07-14): …`（单一根因）分别提交亦可；提交信息携带 `#06-17-7` 锚点。
    (3) **缺口登记**：SUMMARY 逐条标注 `#06-17-7`（审计 C）已闭合（附 commit）与 Q4 的 `patchFlags` 处置；同时说明审计「未确定项 1（仓外 `resetHero` 是否重发）」在本次修复后不再影响正确性（选项 A）或仍依赖该事件（选项 B，须明写）。`WINDOWS.md` 不新建、不 `fixed`/`waive`。
    (4) **Phase 7 验证失效**：SUMMARY 明写 `07-VERIFICATION.md` 已因 07-09 + 07-10..07-14 失效，结束后必须重跑 `/gsd-verify-work`（或 `gsd-verify-work 7`）；`ROADMAP.md` 的 Phase 7 计划数与 Wave 列表同步；这是 C–H + CR-01/CR-02 全部收口后的**最后一个**计划，SUMMARY 应给出本轮 07-10..07-14 的完整清单与逐条闭合状态，便于 `07-VERIFICATION.md` 重跑核对。
  </action>
  <verify>
    <automated>pnpm test:ci</automated>
    <fails_when>`pnpm test:ci` 出现新增失败或新增 `it.skip`；或新增测试文件未被纳入运行；或 `vue-tsc --noEmit` 输出含本计划改动文件的类型错误 —— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - `hero.ts`/`hero.test.ts` 的 `eslint` 0 错误、`vue-tsc --noEmit` 0 类型错误、`pnpm test:ci` 全绿且跳过项仍仅 1 条
    - 基线变化（文件数 66→67 与通过数）已在 SUMMARY 说明
    - SUMMARY 完成 `#06-17-7` 闭合登记、Q4 处置、"未确定项 1" 结论，以及 07-10..07-14 的逐条闭合清单与 Phase 7 验证失效说明
    - `WINDOWS.md` 未被本计划改动
  </acceptance_criteria>
  <done>D-44 三步门禁全绿、提交完成、`#06-17-7` 闭合登记与 Phase 7 验证失效说明写入 SUMMARY，本轮 5 个计划有完整清单</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仓外旧引擎 → 数据端 | `core.status.hero` 是 legacy 侧唯一入口，其代理的后端引用必须始终有效 |
| 数据端 → 全局对象 | `patchHero` 写入全局 `core.status.hero` 与 `window.flags`，装配时机由 `resetHero` 决定 |
| 测试环境 → 生产代码路径 | 新增测试需 stub `main`/`core`/`window`，stub 不当会掩盖真实装配差异 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-43 | Tampering | `patchHero` 的 `attr` 闭包（`hero.ts:8`） | medium | mitigate | Task 1 按 Q1 裁决：每次访问解析活属性（或按裁决重绑），并有「切换实例后读写落到新实例」的断言 |
| T-7-44 | Repudiation | legacy 读到的数值口径（基础值 vs 最终值） | low | mitigate | Task 0 Q2 裁决 + Task 1 断言；`patchHero` jsDoc 写明口径 |
| T-7-45 | Tampering | flags 代理（`hero.ts:26-37`）与 `window.flags` 绑定 | low | mitigate | Task 1 断言 flags 代理拒绝读写且发码 54、`window.flags` 指向同一代理（既有语义不回归） |
| T-7-46 | Information Disclosure | 告警 56 携带的字段名 | low | accept | 码 56 的既有文案只携带 `'core.status.hero'` 与 `'state.hero'` 两个常量标签，不输出用户数据 |
| T-7-47 | Denial of Service | 测试 stub 泄漏到其它测试文件 | low | mitigate | Task 1 使用 `vi.hoisted` + `afterAll(() => vi.unstubAllGlobals())`（与既有测试同款），`hook` 的单例订阅需注意跨文件隔离（在 SUMMARY 记录） |
| T-7-SC | Tampering | npm/pnpm 依赖安装 | high | mitigate | 本计划不新增依赖、不改 `package.json`；出现安装需求即暂停并要求用户确认包合法性 |
</threat_model>

<verification>
1. `pnpm exec vitest run packages-user/data-fallback/src/hero.test.ts` 全绿。
2. `pnpm test:ci` 全绿：67 文件（+1 为本计划新增测试）/ 通过数与执行前基线一致地增加 / 1 跳过（仅 `equipment.test.ts:306` 码 147）。
3. `pnpm exec eslint packages-user/data-fallback/src/hero.ts packages-user/data-fallback/src/hero.test.ts` 0 错误；`pnpm exec vue-tsc --noEmit` 输出不含这两个文件的类型错误。
4. Task 0 的 `<record>` 中 Q1..Q4 均有裁决，代码与裁决逐条一致（SUMMARY 对照表）。
5. 仅 `hero.ts` 与新增 `hero.test.ts` 被改动；`flag.ts`/`index.ts`/`package.json`/数据侧包零改动。
6. Phase 7 验证失效与重跑要求已在 `07-14-SUMMARY.md` 明写，`ROADMAP.md` 已更新，且 SUMMARY 含 07-10..07-14 的完整闭合清单。
</verification>

<success_criteria>
- `#06-17-7`（审计 C）按 Q1 裁决闭合：`core.status.hero` 代理的属性读写始终落在活属性上（或按裁决明写契约），并有回归断言。
- `get` 语义与 Q2 裁决一致；告警 54/56 与 `window.flags` 绑定等既有语义零回归。
- `packages-user/data-fallback` 拥有首个测试文件，覆盖装配路径与活属性跟随。
- `pnpm test:ci` 全绿、无新增 `it.skip`、其它包断言未改；D-44 三步门禁全绿；基线变化已说明。
- Phase 7 验证失效与重跑要求已记录，且 SUMMARY 给出 07-10..07-14 的完整闭合清单。
</success_criteria>

<output>
Create `.planning/phases/07-data-fixes/07-14-SUMMARY.md` when done
</output>
