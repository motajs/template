---
phase: 07-data-fixes
plan: 13
type: execute
wave: 13
depends_on:
  - 07-12
files_modified:
  - packages-user/data-system/src/combat/mapDamage.ts
  - packages-user/data-system/src/combat/mapDamage.test.ts
  - packages-user/data-system/src/combat/types.ts
  - packages-user/data-base/src/map/mapLayer.ts
  - packages-user/data-base/src/map/mapLayer.test.ts
  - packages-user/data-base/src/map/saveLoad.test.ts
autonomous: false
requirements:
  - FIX-01
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "怪物来源的地图伤害在来源消失或范围收缩后不再残留：`markEnemyDirty` 路径下 `removeEnemyAffecting` 触达过的每个坐标索引都被标脏并按现有 `refreshIndex` 重新聚合（CR-02 关闭），`getReducedDamage`/`getSeparatedDamage` 不再返回幽灵伤害"
    - "转换视图集变为空时（伤害来源被移除，如光环/特殊属性消失）仍完成缓存失效，且该怪物的反向索引被显式登记，后续 `markEnemyDirty` 不走整表 `refreshAll()`（按 Task 0 Q1 裁决）"
    - "范围收缩（同一怪物产生的伤害点位减少）时，跌出范围的坐标不再保留包含已移除贡献的缓存值"
    - "`MapLayer.setMapRef` 的失效契约按 Task 0 Q2 裁决明确：`getMapRef()` 的持有者能据 `expired`/对象身份判断何时重取（`#06-17-8` / 审计 G 关闭或明写契约）"
    - "`MapLayer.loadDynamics` 不再让旧动态图块累积：读档后该图层的动态图块集合与存档一致（按 Task 0 Q3 裁决：清空旧块 / 只清存档外坐标 / 明写合并语义）"
    - "`pnpm test:ci` 全绿，不新增 `it.skip`、不弱化既有断言（既有 `mapDamage.test.ts` 的 102/103/104、`deleteEnemy`、`markEnemyDirty` 用例全部保持通过）"
  artifacts:
    - path: packages-user/data-system/src/combat/mapDamage.ts
      provides: "`removeEnemyAffecting` 返回/登记被移除的坐标索引并逐个 `markDirtyIndex`；`refreshEnemyAndClearCache` 在视图集为空时仍完成失效与反向索引登记（按 Q1 裁决）"
    - path: packages-user/data-system/src/combat/types.ts
      provides: "`IMapDamage.markEnemyDirty`/`deleteEnemy` 的失效契约 jsDoc（何时保证缓存失效、空视图集语义）"
    - path: packages-user/data-system/src/combat/mapDamage.test.ts
      provides: "CR-02 回归：来源消失（空视图集）与范围收缩两条路径的幽灵伤害断言"
    - path: packages-user/data-base/src/map/mapLayer.ts
      provides: "`setMapRef`/`loadState`/`loadDynamics` 的失效与旧动态块处置（按 Q2/Q3 裁决）"
    - path: packages-user/data-base/src/map/mapLayer.test.ts
      provides: "`#06-17-8` 回归：`setMapRef` 的 `expired`/对象身份契约；旧动态块不再累积（实例级）"
    - path: packages-user/data-base/src/map/saveLoad.test.ts
      provides: "`#06-17-8` 回归：`MapLayer.loadState` 三档往返后动态图块集合等于存档（不累积）"
  key_links:
    - "`removeEnemyAffecting`（`mapDamage.ts:240-256`）↔ `markDirtyIndex`（`:126-129`）——移除端不标脏是 CR-02 的根因；`refreshEnemyAndClearCache`（`:290-333`）与 `refreshEnemy`（`:338-372`）都经由它"
    - "`refreshEnemyAndClearCache` 的 `if (set.size === 0) return;`（`:300`）↔ `enemyStore.delete(view)`（`:255`）—— 视图集为空时不登记反向索引，后续 `markEnemyDirty`（`:135-148`）会因 `enemyStore.get(view)` 为空而走 `refreshAll()`（`:142`）"
    - "`refreshIndex`（`:395-427`）↔ `viewStore.get(store.sourceView)`（`:406-407`）——重新聚合只在 `viewStore` 仍登记的视图上重建，故被移除视图的贡献随 `point.affectedBy.delete(viewItem)`（`:249`）自然消失"
    - "`MapLayer.setMapRef`（`mapLayer.ts:371-391`）↔ `getMapRef()`（`:393-395`）/`MapLayerHookController.getMapData`（`:950-952`）——旧 `mapData` 被标 `expired` 后整对象被替换（`:380-386`）"
    - "`MapLayer.loadDynamics`（`:841-849`）↔ `iterateDynamicTiles`（`:512-514`）/`tilePosMap`/`posTileMap`（`:48-51`）/`removeTile`（`:118-124`）——读档只创建不清理，旧动态块累积"
    - "`MapLayer.deleteDynamic`（`:498-507`）的 `syncStaticEvent(tile, false)` + `removeTile` + `onDeleteDynamic` 钩子 ↔ 读档清理是否复用同一路径（决定静态事件同步与渲染端通知是否触发）"
  assumptions:
    - "A1（D-10 例外，本计划授权）：本计划为修复计划，为 CR-02 与 `#06-17-8` 新增回归用例是必需项；既有断言不得弱化（`mapDamage.test.ts` 的 102/103/104/deleteEnemy/markEnemyDirty 用例、`map/saveLoad.test.ts` 的三档往返用例全部保持）"
    - "A2（不引入新状态语义）：CR-02 的修法只在既有结构（`dirtyIndexes`/`reducedCache`/`viewStore`/`damageStore`/`enemyStore`）内完成，不新增缓存层、不改 `IMapDamageReducer`/`IMapDamageConverter` 契约"
    - "A3（项目未发布，与 07-09 的 A2 同口径）：`IMapLayerSave`/存档形状不因本计划变更；动态块清理属读档行为而非格式变更"
    - "A4（范围守卫）：仅修 CR-02 与 `#06-17-8`；`07-REVIEW.md` 的 IN-01（删除后 `point.damages`/`affectedBy` 残留、`sourcelessDamage` 空点残留）与 IN-04/IN-05 不在本轮 15 项清单，只在 Task 0 作为可选项列出、默认登记不修"
  prohibitions:
    - "不改 `packages-user/data-base/src/map/gameMap.ts` / `mapState.ts`（审计判定 `GameMap`/`MapState.loadState` 为原地读档、安全）"
    - "不改 `packages-user/data-base/src/map/dynamicTile.ts`（07-05 已修 num 恢复）"
    - "不改 `packages-user/data-system/src/combat/context.ts` 的 `refreshEnemy`/`setEnemyAt` 调用点（只在 `mapDamage.ts` 内修复）"
    - "不改渲染端 `@user/client-*` 与 legacy 接线；不新增依赖、不新建源码文件、不新增 `import type`、不使用连续 `as` 断言"
    - "不新增 `it.skip`；不为转绿改写既有断言"
---

<objective>
闭合地图侧两条缺陷：`MapDamage` 的幽灵伤害（CR-02）与 `MapLayer` 的读档失效/旧动态块契约（`#06-17-8` / 审计 G）。

现状（逐条锚点）：
1. **CR-02（critical，`data-system/src/combat/mapDamage.ts:240-256, 290-333`）** `refreshEnemyAndClearCache()` 先 `removeEnemyAffecting(view)`（`:294`）——它删除该怪物的伤害对象并 `enemyStore.delete(view)`（`:255`），但**从不把被移除的坐标索引标脏、也不删对应 `reducedCache`**；随后只对新登记的索引做失效（`:329-332`），且视图集为空时在 `:300` 提前 `return`。后果：怪物光环/特殊属性消失（视图集变空）或范围收缩时，旧坐标的 `reducedCache` 永久停留在「含已移除贡献」的合并值——`getReducedDamage()`/`getSeparatedDamage()` 因索引不在 `dirtyIndexes` 而永不重算。`deleteEnemy()`（`:150-167`）标脏掩盖了整怪删除路径，`markEnemyDirty()`（`:135-148`，由 `EnemyContext.refreshEnemy()`/`setEnemyAt()` 触达）是真实可达路径。
2. **`#06-17-8`（低，审计 G）** `MapLayer.setMapRef()`（`mapLayer.ts:371-391`）把旧 `mapData` 标 `expired` 后整对象替换、清 `staticTileCache`；`loadDynamics()`（`:841-849`）只按存档 `createDynamic` 新块、从不清理既有动态块——同一图层多次读档或读一份动态块更少的存档时旧块累积；`expired` 契约（持有者何时重取 `getMapRef()`）未成文。

Purpose: 地图伤害是「怪物在哪里、谁该受伤」的唯一事实源，缓存残留会让玩家在光环消失后仍持续受伤（或显示已不存在的伤害）；动态块累积会让读档后的地图与存档点不一致。两者都在「读档/刷新」的失效边界上，属同一主题。

Output: `mapDamage.ts`（失效补全）+ `combat/types.ts`（失效契约 jsDoc）+ `mapDamage.test.ts`（CR-02 回归）；`mapLayer.ts`（`setMapRef`/`loadDynamics` 按裁决）+ `mapLayer.test.ts` + `saveLoad.test.ts`（`#06-17-8` 回归）。

**Artifacts this phase produces（本计划产出物）**：修改文件见 frontmatter `files_modified`；回归用例见 `must_haves.artifacts`；无新建源码文件、无新增依赖。

**Phase 7 验证已失效**：`07-VERIFICATION.md`（2026-09-16T06:04:11Z）已因 07-09 与本轮 07-10..07-14 失效。本计划执行完毕后**必须重跑 `/gsd-verify-work`**（或 `gsd-verify-work 7`）重新出具验证结论。

Phase 7 裁决对应（逐条引用）：D-01: 本计划是 Phase 7 第 13 个计划，承接 `07-REVIEW.md` CR-02 与 `07-LOADSTATE-AUDIT.md` G 行（`#06-17-8`）。D-02: 属 map 系统（`data-base/src/map`）与 combat 系统（`data-system/src/combat`）的地图伤害子系统，按严重度递减排序。D-03: `#06-01-3` before 语义，不涉及。D-04: `#06-06-1` 越图码 128，不涉及（本计划不改 `transferToDynamic`/`transferToStatic` 的诊断码）。D-05: 码 178 语义，不涉及。D-06: 码 147 保留，不涉及。D-07: path 接线，不涉及。D-08: enemy 复用映射，不涉及。D-09: 由 Task 0 关卡承载。D-10: 本计划为修复计划，回归用例为必需项（A1）。D-11: 失效契约与 `setMapRef` 语义变更需同步 jsDoc。D-12: 沿用 06-CONTEXT D-44 文件级三步门禁。D-13: 按缺陷原子提交（Task 1 / Task 2）。D-14: 两条均无 `WINDOWS.md` 条目，只登记不新建。

执行序：`depends_on: 07-12`（Wave 13）。门禁型依赖（D-09 逐计划裁决关卡 + D-12 全量测试基线逐计划重录）；源码层面与 07-12、07-11 无耦合，但因 `pnpm test:ci` 基线需逐计划重录，不并波执行。

**基线（2026-09-16 实测）**：`pnpm test:ci` = 66 文件 / 690 通过 / 1 跳过（`equipment.test.ts:306` 码 147，D-06）。执行前重录当日基线并与本行对比。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-fixes/07-CONTEXT.md
@.planning/phases/07-data-fixes/07-REVIEW.md
@.planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@dev.md
@packages-user/data-system/src/combat/mapDamage.ts
@packages-user/data-system/src/combat/mapDamage.test.ts
@packages-user/data-base/src/map/mapLayer.ts
@packages-user/data-base/src/map/saveLoad.test.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: 契约裁决关卡（D-09）——CR-02 失效补全口径（标脏 + 空视图集是否登记）、`setMapRef` 失效契约（替换对象 vs 原地变更）、读档旧动态块处置、IN-01 是否纳入</name>
  <files>（只读汇报，不修改任何文件）</files>
  <read_first>
    - packages-user/data-system/src/combat/mapDamage.ts（:58-72 内部结构、:114-133 `deleteMapDamage`/`markDirtyIndex`/`markDirty`、:135-148 `markEnemyDirty`、:150-167 `deleteEnemy`、:169-222 读取与合并缓存、:224-256 `clearSourceState`/`removeEnemyAffecting`、:258-285 `registerSourcedDamage`、:290-333 `refreshEnemyAndClearCache`、:338-372 `refreshEnemy`、:374-390 `refreshAll`、:395-427 `refreshIndex`）
    - packages-user/data-system/src/combat/types.ts（`IMapDamage` 的 `markEnemyDirty`/`deleteEnemy` 声明与 jsDoc 位置）
    - packages-user/data-system/src/combat/mapDamage.test.ts（:1-2 文件头覆盖说明、:19-90 夹具、后半段 `deleteEnemy`/`markEnemyDirty` 用例，注意文件头是否需同步更新）
    - packages-user/data-base/src/map/mapLayer.ts（:44-51 私有映射、:100-124 `removeTileFromPosMap`/`removeTile`、:371-395 `setMapRef`/`getMapRef`、:418-433 `createDynamic`、:461-507 删除动态块（含 `deleteDynamic` 的 `syncStaticEvent`/钩子）、:512-514 `iterateDynamicTiles`、:600-681 `isEqualToRef`/`compareWith`/`resize`/`resize2`（另两处 `mapData.expired`）、:841-849 `loadDynamics`、:855-931 三档 `load*`）
    - packages-user/data-base/src/map/mapLayer.test.ts、packages-user/data-base/src/map/saveLoad.test.ts（:176-220 `MapLayer save and load round trips` 与 124 告警用例）
    - .planning/phases/07-data-fixes/07-REVIEW.md（CR-02 与 IN-01 原文、建议修法）
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（G 行与「未确定项 2」）
  </read_first>
  <decision>
    地图侧两条修复的契约点：
    （Q1）CR-02 的失效补全口径（是否标脏；视图集为空时是否登记反向索引以免触发 `refreshAll()`）；（Q2）`MapLayer.setMapRef` 的失效契约（替换 `mapData` 对象 + 旧对象标 `expired`，还是原地变更）；（Q3）读档时旧动态块的处置（全清 / 只清存档外坐标 / 明写合并）；（Q4）`IN-01`（删除后的点残留与 `sourcelessDamage` 空点）是否纳入本计划。
  </decision>
  <context>
    两个决定都直接改变对外可观测行为：
    `refreshEnemyAndClearCache` 的 `set.size === 0` 早退（`:300`）与 `enemyStore.delete(view)`（`:255`）组合出一个隐式后果——**视图集为空的怪物在下次 `markEnemyDirty` 时会走 `refreshAll()` 整表重算**（`:142`）。修复 CR-02 时是否顺手登记空视图集（`enemyStore.set(view, new Set())`）会决定这是「按需重算」还是「整表重算」。
    `setMapRef` 现在把旧 `mapData` 标 `expired` 再整对象替换（`:380-386`）：这是外部持有者唯一的失效信号，但持有者拿到的对象上 `array` 仍是旧缓冲。是否改成原地变更（同引用原则的另一种形式）会决定外部持有者是「必须重取」还是「自动看到新缓冲」，两者不能同时成立。
    动态块清理涉及是否复用 `deleteDynamic` 的 `syncStaticEvent(tile, false)` 与 `onDeleteDynamic` 钩子（渲染端可能在监听），故必须由用户点名。
  </context>
  <options>
    <option id="q1-mark-dirty">
      <name>Q1 选项 A：【建议默认】`removeEnemyAffecting` 收集被移除的坐标索引并逐个 `markDirtyIndex(index)`；`refreshEnemyAndClearCache`（与 `refreshEnemy`）在视图集为空时**仍登记 `enemyStore.set(view, emptySet)`** 并完成失效，不再提前 `return` 跳过登记</name>
      <pros>与 `07-REVIEW.md` 的建议修法一致；幽灵伤害在两条路径（来源消失 / 范围收缩）上一次消除；空视图集被登记后，后续 `markEnemyDirty` 走局部刷新而不是 `refreshAll()` 整表重算（避免性能与行为双重抖动）</pros>
      <cons>`enemyStore` 会开始保存「空集合」条目，`markEnemyDirty`/`deleteEnemy` 的读法需相应容错（`Set.size === 0` 分支）；需在 jsDoc 写明空集语义</cons>
    </option>
    <option id="q1-cache-only">
      <name>Q1 选项 B：只在移除端删除 `reducedCache`（不标脏、不登记空集），让 `getReducedDamage` 下次按剩余伤害重算</name>
      <pros>改动最小（一行 `reducedCache.delete` 的循环），不动 `dirtyIndexes`/`enemyStore` 结构</pros>
      <cons>`refreshIndex` 的重新聚合不再执行，`point.damages` 中的其它陈旧项不会被清理；`enemyStore` 为空的行为（下次 `markEnemyDirty` → `refreshAll()`）保持不变；CR-02 的「范围收缩」场景只被间接修复</cons>
    </option>
    <option id="q1-document">
      <name>Q1 选项 C：不改行为，只把「`markEnemyDirty` 之后旧坐标缓存可能残留」写进 `IMapDamage` 契约并登记</name>
      <pros>零风险</pros>
      <cons>CR-02 是 review 判定的 correctness 缺陷（幽灵伤害可被玩家观测），与「本轮修复 15 项」的指令冲突（清单第 13 条）</cons>
    </option>
    <option id="q2-replace">
      <name>Q2 选项 A：【建议默认】保持「旧 `mapData` 标 `expired` + 替换为新对象」的既有契约，并把该契约写进 `IMapLayerData`/`getMapRef` 的 jsDoc（持有者见 `expired === true` 必须重取；`array` 引用可能失效）</name>
      <pros>零行为变更、零风险；`expired` 语义与 `resize`/`resize2`（`:625`/`:668`）保持一致（三处都是替换 + 标旧）</pros>
      <cons>持有者必须主动轮询/检查 `expired`，契约靠文档而非类型约束</cons>
    </option>
    <option id="q2-inplace">
      <name>Q2 选项 B：改为原地变更（`this.mapData.expired = false; this.mapData.array = this.mapArray`），不再替换对象</name>
      <pros>外部持有者的引用始终有效、自动看到新缓冲（同引用原则的又一处应用）</pros>
      <cons>`expired` 失效信号将**永不生效**（没有可观测的过期窗口），现有依赖该信号重取的调用方必须改成其它判据；`resize`/`resize2` 需一并改，改动面扩大</cons>
    </option>
    <option id="q3-clear-all">
      <name>Q3 选项 A：【建议默认】`loadState`（三档）在读入动态块之前**清空本图层全部既有动态块**（复用内部 `removeTile` 解除两张映射，并按裁决决定是否 `syncStaticEvent(tile, false)` 与触发 `onDeleteDynamic`），使读档后动态块集合等于存档</name>
      <pros>读档语义与「回到存档点」一致；多次读档不再累积；与 `MapLayer` 其它读档字段（静态块原地 `loadState`、矩阵 `setMapRef`）口径统一</pros>
      <cons>读档会销毁旧动态图块实例（外部持有者脱钩——与「读档即替换」语义一致，但需在 jsDoc 写明）；若勾选触发 `onDeleteDynamic`，渲染端会在读档期收到一批删除通知</cons>
    </option>
    <option id="q3-clear-missing">
      <name>Q3 选项 B：只清理「存档中不存在」的坐标上的动态块（合并语义），存档里出现的坐标保留并复用</name>
      <pros>旧实例尽量保留，减少脱钩</pros>
      <cons>同一坐标上「存档块数 < 现有块数」的场景（如同一坐标叠了 3 块、存档只有 1 块）仍需逐块比对，逻辑复杂且容易出现半合并状态；实现成本明显高于收益</cons>
    </option>
    <option id="q3-document">
      <name>Q3 选项 C：不改行为，把「`loadState` 会与既有动态块合并/累积」写进 `IMapLayer`/`IMapLayerSave` 契约并登记</name>
      <pros>零代码风险</pros>
      <cons>`#06-17-8` 未修（审计 G 明确「旧动态块累积」）；读档结果与存档点不一致，属可观测的正确性问题</cons>
    </option>
    <option id="q4-include">
      <name>Q4 选项 A：把 `IN-01`（`deleteMapDamage`/`deleteEnemy` 后的 `point.damages`/`affectedBy` 残留与 `sourcelessDamage` 空点）纳入本计划一并清理</name>
      <pros>CR-02 的清理路径与 IN-01 相邻（都在 `removeEnemyAffecting`/`deleteEnemy` 的清理语句上），一次改动收敛</pros>
      <cons>超出用户点名的 15 项清单（属 review Info 级），需要明确授权；会扩大 CR-02 的回归面</cons>
    </option>
    <option id="q4-register">
      <name>Q4 选项 B：【建议默认】`IN-01` 只登记不修（本轮清单未含）</name>
      <pros>严格守住清单边界；CR-02 的改动面最小（只补失效，不动移除语义）</pros>
      <cons>`point.affectedBy`/`sourcelessDamage` 的空条目泄漏保留在册（review 已判定为 lazy cleanup 而非错误结果）</cons>
    </option>
    <option id="revise">
      <name>（兜底）修订方案后再执行</name>
      <pros>避免带错误契约落地</pros>
      <cons>按 D-09 退出本次修改、修订本 PLAN.md 后重新执行，不得自行另辟他法</cons>
    </option>
  </options>
  <record>
    待用户在关卡处逐条填写（Q1..Q4：勾选选项 id 或给出裁决文字）。
    **空的 `<record>` 表示尚未裁决**：执行者必须停在本任务，不得按「建议默认」推进。
    填写后 Task 1–3 一律按本记录执行；若裁决与 Task 1–3 的分支描述冲突，暂停并请用户澄清。
  </record>
  <resume-signal>逐条给出 Q1..Q4 的裁决（例如「Q1=A, Q2=A, Q3=A, Q4=B」）；若要修订方案，回复修订点，执行者退出并按 D-09 修订本 PLAN.md</resume-signal>
  <action>向用户汇报上述 4 个契约点，重点说明 Q1 对 `refreshAll()` 触发频率的影响、Q2 对 `getMapRef()` 持有者的影响、Q3 对渲染端 `onDeleteDynamic` 通知的影响。不得在裁决前修改任何文件。</action>
  <verify>
    <human-check>`<record>` 中 Q1..Q4 均有明确裁决，且与用户实际意图一致</human-check>
    <fails_when>记录为空或与用户实际意图不符 —— 必须暂停并请用户澄清，不得按「建议默认」自行推进</fails_when>
  </verify>
  <acceptance_criteria>
    - Q1（CR-02 失效口径）、Q2（`setMapRef` 失效契约）、Q3（旧动态块处置）、Q4（IN-01 纳入与否）均有用户裁决并写入 `<record>`
    - Q3 已明确是否复用 `deleteDynamic` 的 `syncStaticEvent`/`onDeleteDynamic` 行为
    - 未在裁决前修改任何文件
  </acceptance_criteria>
  <done>Q1..Q4 裁决已完整写入 `<record>`；执行者据记录直接进入 Task 1，不再重复询问</done>
</task>

<task type="auto">
  <name>Task 1: CR-02 —— `MapDamage` 失效补全（移除端标脏 + 空视图集登记），含「来源消失」与「范围收缩」两条幽灵伤害回归</name>
  <files>packages-user/data-system/src/combat/mapDamage.ts, packages-user/data-system/src/combat/types.ts, packages-user/data-system/src/combat/mapDamage.test.ts</files>
  <read_first>
    - packages-user/data-system/src/combat/mapDamage.ts（:126-148 `markDirtyIndex`/`markEnemyDirty`、:150-167 `deleteEnemy` 的标脏参照、:224-256 `clearSourceState`/`removeEnemyAffecting`、:290-333 `refreshEnemyAndClearCache`、:338-372 `refreshEnemy`、:395-427 `refreshIndex`）
    - packages-user/data-system/src/combat/types.ts（`IMapDamage` 方法声明与 jsDoc 位置）
    - packages-user/data-system/src/combat/mapDamage.test.ts（:1-2 文件头覆盖说明、夹具（假视图/转换器/reducer）、`deleteEnemy` 与 `markEnemyDirty` 现有用例）
  </read_first>
  <action>
    按 Task 0 的 Q1 裁决修复。**若 Q1 = 选项 A**：
    (1) `removeEnemyAffecting`（`:240-256`）：在内层 `store.damages.forEach((dam, index) => …)`（`:246-252`）中把 `index` 收集进一个 `Set<number>`；方法结束前对每个收集到的索引调用 `this.markDirtyIndex(index)`；返回类型与调用点按实现需要调整（两个调用点：`:294`、`:339`）；
    (2) `refreshEnemyAndClearCache`（`:290-333`）与 `refreshEnemy`（`:338-372`）：把 `if (set.size === 0) return;` 改为——**先** `this.enemyStore.set(view, set)`（空集也登记，使后续 `markEnemyDirty` 走局部路径而非 `refreshAll()`），再在 `set.size === 0` 时直接结束（无需继续遍历视图）；非空时保持既有登记与新索引失效（`:329-332` 的 `dirtyIndexes.delete`/`reducedCache.delete` 不变）；
    (3) `enemyStore` 的读法容错：`markEnemyDirty`（`:135-148`）与 `deleteEnemy`（`:150-167`）在 `store` 为**空集合**时不得视为「未登记」——`markEnemyDirty` 对空集合应直接结束（无视图可刷），`deleteEnemy` 对空集合应只做 `enemyStore.delete(view)`；`removeEnemyAffecting` 对空集合的遍历保持无副作用；
    (4) `IMapDamage`（`combat/types.ts`）的 `markEnemyDirty`/`deleteEnemy` jsDoc 补写失效契约：调用后该怪物此前影响的坐标索引缓存保证失效；该怪物的反向索引（含空集）保持登记，后续刷新为局部而非整表。
    **若 Q1 = 选项 B**：只在 `removeEnemyAffecting` 中收集索引并 `this.reducedCache.delete(index)`（不动 `dirtyIndexes`、不登记空集），并按裁决在 jsDoc 写明其局限。
    **若 Q1 = 选项 C**：不改行为，仅在 jsDoc 写明已知限制并在 SUMMARY 登记。
    回归用例（`mapDamage.test.ts`，中文单行注释；文件头覆盖说明同步更新）：
    (a) **来源消失**：注册带伤害的怪物视图 → `getReducedDamage(loc)` 预热缓存 → 使该怪物的转换视图集变为空（例如让假转换器返回空数组后调用 `refreshEnemy`/`markEnemyDirty`，或移除其特殊属性）→ 再次 `getReducedDamage(loc)` 断言为 `null`（或不再包含已移除贡献）、`getSeparatedDamage(loc)` 为空；
    (b) **范围收缩**：假视图的范围/命中点位在第二次转换时变小 → `markEnemyDirty(view)` 后断言跌出范围的坐标 `getReducedDamage` 不再包含已移除贡献（用「多点位 + 局部收缩」构造，避免与 (a) 重复）；
    (c) **整怪删除回归不破**：既有 `removes enemy-sourced damage when the enemy is deleted`（`:106-…` 附近）用例保持通过；
    (d) **不触发整表重算**（Q1 选项 A 时）：在空视图集场景下再次 `markEnemyDirty(view)`，断言行为仍为局部刷新（例如通过 reducer/转换器调用次数或 `enemyStore` 可观测状态断言，不使用对私有字段的反射）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-system/src/combat/mapDamage.test.ts</automated>
    <fails_when>来源消失或范围收缩后 `getReducedDamage` 仍返回含已移除贡献的缓存值（幽灵伤害）；或既有 `deleteEnemy`/102/103/104 用例出现回归 —— 按 D-09 退出并修订计划，不得改用「让调用方手动 markDirty」的替代方案</fails_when>
  </verify>
  <acceptance_criteria>
    - 移除端触达的每个坐标索引都被失效（Q1 选项 A：`markDirtyIndex`；选项 B：`reducedCache.delete`）
    - 视图集为空时按裁决完成失效与反向索引处置，并有 `markEnemyDirty` 重复调用不触发 `refreshAll()` 的断言（选项 A）
    - `IMapDamage` 的 jsDoc 与最终契约一致；既有 mapDamage 用例全部保持通过
  </acceptance_criteria>
  <done>怪物光环/特殊属性消失或范围收缩后不再有幽灵地图伤害，且失效路径保持局部刷新（CR-02 按裁决关闭）</done>
</task>

<task type="auto">
  <name>Task 2: `#06-17-8` —— `MapLayer.setMapRef` 失效契约 + 读档旧动态块处置（按 Q2/Q3 裁决）</name>
  <files>packages-user/data-base/src/map/mapLayer.ts, packages-user/data-base/src/map/mapLayer.test.ts, packages-user/data-base/src/map/saveLoad.test.ts</files>
  <read_first>
    - packages-user/data-base/src/map/mapLayer.ts（:44-51 私有映射、:100-124 `removeTileFromPosMap`/`removeTile`、:133-145 `syncStaticEvent`、:371-395 `setMapRef`/`getMapRef`、:418-433 `createDynamic`、:461-507 删除动态块、:512-514 `iterateDynamicTiles`、:600-681 `resize`/`resize2`（另两处 `expired`）、:814-822 `saveState`、:841-849 `loadDynamics`、:855-931 三档 `load*`）
    - packages-user/data-base/src/map/mapLayer.test.ts（现有动态块与 `setMapRef` 用例）
    - packages-user/data-base/src/map/saveLoad.test.ts（:176-220 `MapLayer save and load round trips` 与码 124 用例）
    - packages-user/data-base/src/map/types.ts（`IMapLayerData`/`IMapLayer`/`IMapLayerSave` 的 jsDoc 位置）
  </read_first>
  <action>
    (1) **Q2 裁决落地（`setMapRef` 契约）**：选项 A → 保持替换 + 标旧对象 `expired`（`:380-386`）的实现不变，在 `IMapLayerData`/`getMapRef()` 的 jsDoc 写明「持有者见 `expired === true` 必须重取；`array` 引用可能失效」，并确保 `resize`/`resize2`（`:625`/`:668`）的三处行为与文档一致（不修改其行为，只核对口径）；选项 B → 三处一并改为原地变更（`this.mapData.expired = false` + `this.mapData.array = this.mapArray`），并同步更新 jsDoc 与任何依赖 `expired` 信号的既有用例（逐条在 SUMMARY 说明）。
    (2) **Q3 裁决落地（`loadDynamics`/`loadState`）**：选项 A → 在 `loadDynamics`（`:841-849`）入口先清空本图层全部既有动态块：遍历 `iterateDynamicTiles()` 收集快照 → 逐个复用内部 `removeTile(tile)`（`:118-124`，解除 `tilePosMap`/`posTileMap`），并按裁决决定是否对每个块执行 `syncStaticEvent(tile, false)`；再按存档创建新块（既有逻辑）。**注意**：`setMapRef` 已清 `staticTileCache`，`syncStaticEvent` 在清理时是否有意义由裁决决定；若裁决要求触发 `onDeleteDynamic`，须用 `forEachHook` 与 `deleteDynamic`（`:498-507`）保持一致的参数顺序（`hook.onDeleteDynamic?.(tile)`）。选项 B → 只清理存档中不存在坐标上的动态块；选项 C → 不改行为，只在 jsDoc 写明合并/累积语义并登记。
    (3) **三档一致性**：清理逻辑放在 `loadDynamics` 内（三档共用 `:862-863`/`:886-887`/`:917-918` 三处调用点），避免三档行为分叉；若裁决要求「无 `dynamicBlocks` 字段时也清理」，则在三档 `load*` 的 `loadDynamics` 调用处按存档字段缺失情况补齐。
    回归用例：
    (a) `mapLayer.test.ts`：`createDynamic` 若干块 → 记录实例 → `setMapRef`（按 Q2 裁决断言 `expired`/对象身份/`array` 可见性）；读档前动态块数为 N（按 Q3 裁决断言清理后的集合与实例状态）；
    (b) `saveLoad.test.ts`：同一图层先创建多于存档的动态块 → `loadState`（三档各自循环）→ 断言 `[...iterateDynamicTiles()].length` 等于存档中的块数（不累积）；再 `loadState` 第二次（模拟重复读档）→ 数量仍等于存档块数；既有 `restores the matrix and point events across all compressions` 与码 124 用例保持通过。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/map/mapLayer.test.ts packages-user/data-base/src/map/saveLoad.test.ts</automated>
    <fails_when>重复读档（或读到动态块更少的存档）后 `iterateDynamicTiles()` 的块数大于存档块数（累积）；或 `setMapRef` 的失效契约与 Q2 裁决/文档不一致；或既有矩阵/事件/码 124 用例出现回归 —— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - Q2 裁决落地：`setMapRef`（及 `resize`/`resize2` 的口径）与 jsDoc 一致，并有 `getMapRef()` 失效行为的断言
    - Q3 裁决落地：读档后动态图块集合等于存档（不累积）并有断言；重复读档的断言通过
    - 三档压缩的清理行为一致；既有 `mapLayer.test.ts`/`saveLoad.test.ts` 断言未弱化
  </acceptance_criteria>
  <done>`MapLayer` 的失效契约成文且读档不再累积旧动态块（`#06-17-8` / 审计 G 按裁决关闭）</done>
</task>

<task type="auto">
  <name>Task 3: D-12/D-44 门禁 + 全量套件 + 缺口登记（CR-02 / `#06-17-8` / IN-01 处置）与 Phase 7 验证失效说明</name>
  <files>.planning/phases/07-data-fixes/07-13-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-data-fixes/07-REVIEW.md（CR-02 与 IN-01 编号）
    - .planning/phases/07-data-fixes/07-LOADSTATE-AUDIT.md（G 行）
    - .planning/phases/07-data-fixes/07-VERIFICATION.md（头部 verified/status）
    - .planning/ROADMAP.md（Phase 7 段 Plans 与 Wave 列表）
    - .planning/WINDOWS.md（确认两条无入账 id）
  </read_first>
  <action>
    (1) **门禁（D-12 / D-44 文件级）**：对 `files_modified` 中每个改动的文件执行 `pnpm exec eslint --fix <文件>` 后 `pnpm exec eslint <文件>` 0 错误；**文件级类型门禁按 06-RESEARCH Pitfall 4 的判定方式**：`pnpm exec vue-tsc --noEmit 2>&1 | Select-String -Pattern "<改动文件相对路径>"` 无命中（**不得**用「退出码非 0」判定失败）；`pnpm test:ci` 全绿（对比基线 66 文件 / 690 通过 / 1 跳过，跳过项仍只能是 `equipment.test.ts:306` 码 147）。
    (2) **原子提交（D-13）**：Task 1（CR-02）与 Task 2（`#06-17-8`）各自独立提交（`fix(07-13): …`）。
    (3) **缺口登记**：SUMMARY 逐条标注 CR-02 与 `#06-17-8` 已闭合（附 commit）；`IN-01` 按 Q4 裁决标注「已修」或「保留在册」；`IN-04`/`IN-05` 标注「本轮清单外，保留在册」。`WINDOWS.md` 不新建、不 `fixed`/`waive`。
    (4) **Phase 7 验证失效**：SUMMARY 明写 `07-VERIFICATION.md` 已因 07-09 + 07-10..07-14 失效，结束后必须重跑 `/gsd-verify-work`（或 `gsd-verify-work 7`）；`ROADMAP.md` 的 Phase 7 计划数与 Wave 列表同步。
  </action>
  <verify>
    <automated>pnpm test:ci</automated>
    <fails_when>`pnpm test:ci` 出现新增失败或新增 `it.skip`；或 `vue-tsc --noEmit` 输出含本计划改动文件的类型错误 —— 按 D-09 退出并修订计划</fails_when>
  </verify>
  <acceptance_criteria>
    - 改动文件 `eslint` 0 错误、`vue-tsc --noEmit` 该文件 0 类型错误、`pnpm test:ci` 全绿且跳过项仍仅 1 条
    - Task 1/2 各有独立提交并含对应锚点
    - SUMMARY 完成缺口登记（CR-02、`#06-17-8`、IN-01/IN-04/IN-05 处置）与 Phase 7 验证失效说明
    - `WINDOWS.md` 未被本计划改动
  </acceptance_criteria>
  <done>D-44 三步门禁全绿、原子提交完成、缺口登记与 Phase 7 验证失效说明写入 SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 怪物状态变更 → 地图伤害缓存 | 光环/特殊属性增删与范围变化由战斗侧驱动，缓存失效不全即产生与真实状态不一致的伤害 |
| 存档数据 → 地图图层 | 读档决定矩阵、静态块、动态块；清理不全即与存档点不一致 |
| 图层内部状态 → 外部持有者 | `getMapRef()` / `iterateDynamicTiles()` / 图块实例被外部（渲染端、事件层）持有 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-37 | Tampering | `MapDamage.removeEnemyAffecting`（`mapDamage.ts:240-256`） | high | mitigate | Task 1：收集被移除索引并逐个 `markDirtyIndex`，使旧坐标按 `refreshIndex` 重新聚合（CR-02 主修复） |
| T-7-38 | Tampering | `refreshEnemyAndClearCache` 的空视图集早退（`:300`） | high | mitigate | Task 1：空集也登记 `enemyStore.set(view, set)` 并完成失效，避免缓存永久残留与 `refreshAll()` 级联 |
| T-7-39 | Repudiation | 空集登记后的 `markEnemyDirty`/`deleteEnemy` 读法 | low | mitigate | Task 1：`markEnemyDirty`/`deleteEnemy`/`removeEnemyAffecting` 对空集合显式容错，并有重复调用断言 |
| T-7-40 | Tampering | `MapLayer.loadDynamics` 不清理旧动态块（`:841-849`） | medium | mitigate | Task 2：按裁决在读入前清理既有动态块（复用内部 `removeTile`），三档一致，并有重复读档断言 |
| T-7-41 | Repudiation | `setMapRef` 的失效信号 `expired` 无成文契约（`:371-391`） | low | mitigate | Task 2 按 Q2 裁决：保持替换+标过期并写入 jsDoc，或原地变更并同步全部依赖点（逐条登记） |
| T-7-42 | Denial of Service | 清空动态块时触发大量 `onDeleteDynamic`/`syncStaticEvent` | low | mitigate | Task 0 Q3 已把「是否复用 `deleteDynamic` 的同步与钩子」列为裁决点；实现按裁决执行并在测试中断言通知口径 |
| T-7-SC | Tampering | npm/pnpm 依赖安装 | high | mitigate | 本计划不新增依赖、不改 `package.json`；出现安装需求即暂停并要求用户确认包合法性 |
</threat_model>

<verification>
1. `pnpm exec vitest run packages-user/data-system/src/combat/mapDamage.test.ts packages-user/data-base/src/map/mapLayer.test.ts packages-user/data-base/src/map/saveLoad.test.ts` 全绿。
2. `pnpm test:ci` 全绿：66 文件 / 690 通过 / 1 跳过（仅 `equipment.test.ts:306` 码 147）。
3. `pnpm exec eslint <files_modified 中每个改动文件>` 0 错误；`pnpm exec vue-tsc --noEmit` 输出不含这些文件的类型错误。
4. Task 0 的 `<record>` 中 Q1..Q4 均有裁决，代码与裁决逐条一致（SUMMARY 对照表）。
5. 无新增依赖、无新建源码文件、`WINDOWS.md` 未改动、`gameMap.ts`/`mapState.ts`/`dynamicTile.ts`/`context.ts` 未改动。
6. Phase 7 验证失效与重跑要求已在 `07-13-SUMMARY.md` 明写，`ROADMAP.md` 已更新。
</verification>

<success_criteria>
- CR-02 按 Q1 裁决闭合：来源消失与范围收缩两条路径都不再有幽灵伤害，且失效路径保持局部刷新。
- `#06-17-8` 按 Q2/Q3 裁决闭合：失效契约成文；读档后动态块集合等于存档（不累积），三档一致。
- `IN-01`/`IN-04`/`IN-05` 按裁决登记或修复，并在 SUMMARY 逐条记录。
- `pnpm test:ci` 全绿、无新增 `it.skip`、既有断言未弱化；D-44 三步门禁全绿。
- Phase 7 验证失效与重跑要求已记录。
</success_criteria>

<output>
Create `.planning/phases/07-data-fixes/07-13-SUMMARY.md` when done
</output>
