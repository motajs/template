---
phase: 02-pathfinding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages-user/data-common/src/common/mover.ts
  - packages-user/data-common/src/common/mover.test.ts
  - packages-user/data-system/src/pathfinding/types.ts
  - packages-user/data-system/src/pathfinding/graph.ts
  - packages-user/data-system/src/pathfinding/system.ts
  - packages-user/data-system/src/pathfinding/index.ts
  - packages-user/data-system/src/pathfinding/graph.test.ts
  - packages-user/data-system/src/pathfinding/system.test.ts
autonomous: true
requirements: [PATH-01, PATH-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "mover.ts:651 坐标回写条件按用户拍板结果修复，L0 回归 4 用例（x 正交/y 正交/斜向/传送）全部翻绿 (D-07)"
    - "有向图构建正确：边 = outPass(source) 含方向位且 inPass(dest) 含反向位，单向门 A→B 可行而 B→A 不可行有测试 (D-02)"
    - "最小损失搜索满足 D-01：默认每格损失 1，可注入自定义损失函数，返回两点间损失最小路径 (D-01, D-02)"
    - "仅取路径 API 存在且不移动任何对象 (D-06)"
    - "不可达目标按 D-08 情形 2 返回空数组且不移动 (D-08)"
    - "回退策略槽位存在：策略函数接收路径及每步到达位置并决策移动方式（瞬移或退为逐步）(D-04, D-05)"
    - "绑定面为 IMapLayer 与 IObjectMovable 注入槽位，L2 未引用 L3 任何符号 (D-03)"
  artifacts:
    - "packages-user/data-common/src/common/mover.ts"
    - "packages-user/data-common/src/common/mover.test.ts"
    - "packages-user/data-system/src/pathfinding/types.ts"
    - "packages-user/data-system/src/pathfinding/graph.ts"
    - "packages-user/data-system/src/pathfinding/system.ts"
    - "packages-user/data-system/src/pathfinding/index.ts"
    - "packages-user/data-system/src/pathfinding/graph.test.ts"
    - "packages-user/data-system/src/pathfinding/system.test.ts"
  key_links:
    - "graph.ts 消费注入的通行性谓词而非 import L3，谓词复刻 moverImpl canPass 的掩码语义（事件层恒参与，其余层仅 onlyEvents 参与）"
    - "system.ts 的损失/回退/谓词槽位由 02-03 的 L3 接线注入"
  assumptions:
    - "PATH-01（flagged assumption，spec-less probe 未分类）：本计划交付自动寻路的纯计算核心（图构建 + 最小损失搜索 + 仅取路径），Node 可验证"
    - "PATH-02（flagged assumption，spec-less probe 未分类）：本计划提供的仅取路径 API 即渲染端 Phase 4 将调用的数据端入口之一，点击接线不在本阶段 (D-11)"
  prohibitions:
    - "不得为寻路引入任何新 npm 依赖（Dijkstra 自写，零新包）"
    - "不得把渲染端点击处理接进数据端代码 (D-11)"
    - "接口签名必须与用户拍板后的草案逐字一致，不得自行更名或改层 (D-07)"
---

<objective>
按用户拍板结果修复 L0 mover.ts:651 坐标回写缺陷并翻绿回归测试，然后在 data-system（L2）落地寻路核心：有向图构建（注入谓词）、最小损失搜索（自定义损失、默认每格 1）、仅取路径 API、D-08 情形 2 空数组语义、回退策略槽位与移动方式决策。

Purpose: 这是全部寻路行为的纯逻辑地基——不移动任何对象，可在 Node 下完整验证；L0 缺陷不修则 02-03 的多步逐步移动必然走崩（RESEARCH P1）。
Output: 修复后的 mover.ts + 绿色 L0 回归；pathfinding/{types,graph,system,index}.ts 及 co-located 测试。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pathfinding/02-CONTEXT.md
@.planning/phases/02-pathfinding/02-RESEARCH.md
@.planning/phases/02-pathfinding/02-PATTERNS.md
@.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md

# 草案与拍板记录是本计划的接口事实源
@packages-user/data-common/src/common/mover.ts
@packages-user/data-common/src/store/types.ts
@packages-user/data-base/src/map/types.ts
@packages-user/data-base/src/map/mapLayer.ts
@packages-user/data-state/src/hero/moverImpl.ts
@packages-user/data-system/src/event/system.ts
@packages-user/data-system/src/combat/mapDamage.ts
@dev.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 按拍板结果修复 L0 坐标回写缺陷并翻绿回归测试</name>
  <files>packages-user/data-common/src/common/mover.ts, packages-user/data-common/src/common/mover.test.ts</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-common/src/common/mover.ts, packages-user/data-common/src/common/mover.test.ts</read_first>
  <action>依据 02-01 拍板记录执行。若 P1 获 go：将 mover.ts `moveProgress` 内 651 行的回写条件按拍板语义修改（拍板为「确认为缺陷改为 ||」时，条件由两轴同时变化才回写改为任一轴变化即回写；若用户给出其他替代语义，逐字应用之）。移除 02-01 铺设的 4 个回归用例的 skip 标记，使 x 正交/y 正交/斜向/传送步的坐标回写断言全部执行并通过。若用户 no-go 并提供替代回写路径：按用户语义实现替代路径，并按其语义改写回归断言。本任务是本计划唯一触碰 L0 共享代码的改动；除该行条件（或用户指定的替代实现）外不得改动 mover.ts 其他逻辑（`start` 返回 null、`stop` 兑现时机等语义是 02-03 打断机制的依赖事实，per RESEARCH Pitfall 5）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-common/src/common/mover.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码；或输出出现 "skipped" 计数大于 0（说明 skip 标记未移除，回归未真正执行）</fails_when>
  </verify>
  <acceptance_criteria>
    - mover.ts 回写条件与拍板记录逐字一致（行为断言）
    - 4 个回归用例全部以 passed 状态出现在 vitest 输出（行为断言）
    - mover.ts 中除拍板授权的改动外无其他 diff：`git diff packages-user/data-common/src/common/mover.ts` 仅含回写条件一处变更（CLI 断言）
  </acceptance_criteria>
  <done>L0 回归 4/4 绿，多步移动自此能以真实坐标计算后续步骤。</done>
</task>

<task type="auto">
  <name>Task 2: 有向图构建（注入谓词 + 终端节点 + 边界守卫）</name>
  <files>packages-user/data-system/src/pathfinding/graph.ts, packages-user/data-system/src/pathfinding/types.ts, packages-user/data-system/src/pathfinding/graph.test.ts</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-base/src/map/mapLayer.ts, packages-user/data-base/src/map/types.ts, packages-user/data-common/src/store/types.ts, packages-user/data-system/src/combat/mapDamage.ts, dev.md</read_first>
  <action>创建 types.ts 与 graph.ts（签名与命名以拍板草案为唯一事实源，D-07）。types.ts：损失函数、回退策略、通行性谓词的函数类型别名；图节点/邻接的 interface；全部 jsDoc 中文注释、对象类型单独 interface、禁 `import type`（dev.md）。graph.ts：把 `IMapLayer` 转换为有向图——节点为 inMap 内图块；从 A 向方向 d 的边成立当且仅当通行性谓词(source, d, dest) 为真（D-02 有向图要求，单向门由掩码不对称产生）；谓词为 `useXxx()` 注入槽位（PATTERNS Shared Pattern 1 同款），L2 默认实现逐字复刻 moverImpl.ts:85-104 + 107-122 的掩码语义（事件层恒参与判定；其余层仅当 `pass.onlyEvents` 为真参与；方向→PassBit 映射照 moverImpl.ts:40-53 语义），L3 可注入复用 `DefaultHeroMoveTopImpl` 判定的谓词。终端节点：canPass 为真但 `eventPass` 为假的图块标记为仅可作路径终点、不可作中间节点（RESEARCH Pitfall 3）。边界守卫：构建与查询入口统一 `IMapLayer.inMap` + `isNil` 判空，非法输入 `logger.warn` 新码（warn ≥173，本任务在 packages/common/src/logger.json 登记首个本阶段码）后返回空图/空结果。graph.test.ts 用 eventDispatch.test.ts:105-179 的 fixture 模式（TileStore.addTile 位掩码 + FaceManager.register + MapState.fromRaw），必含用例：A→B 可行且 B→A 不可行的单向门（Pitfall 4）、eventPass=false 终端节点、越界坐标守卫告警。禁 import L3 任何符号（防循环依赖）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-system/src/pathfinding/graph.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——单向门、终端节点或边界守卫断言未通过</fails_when>
  </verify>
  <acceptance_criteria>
    - 单向门用例：同一条边从 A→B 判定可行、从 B→A 判定不可行（行为断言）
    - eventPass=false 的可通行图块出现在路径终点时合法、出现在中间节点时被拒绝（行为断言）
    - 越界/空数据输入触发 logger.warn 新码并得到空结果而非异常（行为断言）
    - graph.ts 与 types.ts 中无任何来自 data-state 的引入：对两文件运行来源断言找不到 data-state 引用语句（源断言）
  </acceptance_criteria>
  <done>有向图构建在 Node 下全绿，边语义与 canPass 判定同源。</done>
</task>

<task type="auto">
  <name>Task 3: 最小损失搜索 + 仅取路径 API + 回退策略槽位（L2 核心）</name>
  <files>packages-user/data-system/src/pathfinding/system.ts, packages-user/data-system/src/pathfinding/types.ts, packages-user/data-system/src/pathfinding/index.ts, packages-user/data-system/src/pathfinding/system.test.ts</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-system/src/pathfinding/graph.ts, packages-user/data-system/src/event/system.ts, packages-user/data-system/src/combat/mapDamage.ts, packages-user/data-common/src/common/mover.ts, dev.md</read_first>
  <action>创建 system.ts（`PathfindingSystem` 类，骨架照 GameEventSystem 形态：构造器持 state、可空成员显式类型、`useXxx()` 注入方法，per PATTERNS）与 index.ts barrel（照 event/index.ts 三行形态，并在 data-system/src/index.ts 追加 pathfinding 导出）。实现：(1) 自写 Dijkstra（O(V²)，约 40 行，零依赖）在 Task 2 的有向图上求两点间损失最小路径，满足 D-01/D-02 语义；(2) 损失函数注入槽位，默认每格损失 1（D-01）；损失守卫——函数返回非有限数或负数时 `logger.warn` 新码并按默认损失 1 处理（威胁 T-02-02）；(3) 仅取路径方法：绑定地图与 `IObjectMovable` 后可调用，返回坐标路径数组，不调用任何移动方法（D-06，D-03 绑定槽位）；(4) 不可达语义：目标不可达且不属于「no-pass 目标 + 可达相邻格」情形时返回空数组、不移动（D-08 情形 2；情形 1 的相邻格解析留给 02-03，本任务先暴露图上「目标四邻中存在可达格」的查询以便 L3 判定——具体 API 形态以拍板草案为准）；(5) 回退策略槽位：策略函数入参为完整路径及每步到达位置，返回是否退回逐步方式（D-05）；移动方式决策——策略判回退则走逐步，否则允许瞬移（D-04；瞬移的实际执行在 02-03，本任务只产出决策结果）；每次寻路动态构建图、不缓存（CONTEXT 酌情项默认建议，数据端状态可变）。system.test.ts 覆盖：默认损失 1 的最短路径、自定义损失改变选路（构造「步数少但损失高」的岔路）、不可达返回空数组、回退策略被以（路径，逐步到达位置序列）调用并生效、未绑定地图/对象时 logger.warn + 空结果（mapDamage.ts:169-173 守卫形态）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-system/src/pathfinding/system.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——最小损失、自定义损失、空数组语义或回退策略断言未通过</fails_when>
  </verify>
  <acceptance_criteria>
    - 默认损失下返回格数最少路径；注入自定义损失后选择损失更小的岔路（行为断言）
    - 不可达目标返回空数组且移动对象坐标不变（行为断言）
    - 回退策略收到路径与每步到达位置并改变决策结果（行为断言）
    - 未绑定依赖时 warn 码输出且返回空结果不抛异常（行为断言）
    - `pnpm exec vitest run "packages-user/data-system/src/pathfinding"` 全目录绿（CLI 断言）
  </acceptance_criteria>
  <done>L2 寻路核心在 Node 下全绿：图构建、最小损失、仅取路径、空数组语义、回退决策齐备。</done>
</task>

</tasks>

<threat_model>
## Artifacts this phase produces

- 接口/类型（命名以拍板草案为唯一事实源）：损失函数类型（默认每格 1）、回退策略函数类型（入参路径 + 每步到达位置）、通行性谓词函数类型、图节点/邻接 interface（`packages-user/data-system/src/pathfinding/types.ts`）
- 类：`PathfindingSystem`（L2，绑定 `IMapLayer`/`IObjectMovable` 注入槽位，D-03）
- 函数/方法：有向图构建（graph.ts）、自写 Dijkstra 最小损失搜索、仅取路径 API（D-06）、移动方式决策（D-04/D-05 槽位）、方向→PassBit 映射（谓词默认实现内）
- 修改：`packages-user/data-common/src/common/mover.ts`（mover.ts:651 回写条件，按拍板）、`packages/common/src/logger.json`（新 warn 码 ≥173）、`packages-user/data-system/src/index.ts`（barrel 追加）
- 测试：`mover.test.ts`（翻绿）、`graph.test.ts`、`system.test.ts`

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 地图/存档数据 → 图构建 | 畸形图层/掩码/越界坐标进入 graph.ts |
| 调用方注入的损失函数 → Dijkstra | 非有限/负损失值破坏搜索不变式 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/DoS | graph.ts 构建与查询入口 | medium | mitigate | `IMapLayer.inMap` + `isNil` 守卫，非法输入 logger.warn 新码后返回空图/空路径（Task 2） |
| T-02-02 | Tampering | 自定义损失函数 → Dijkstra | medium | mitigate | 损失值守卫：非有限数/负数 → logger.warn 新码并按默认损失 1 处理（Task 3） |
| T-02-03 | DoS | 搜索迭代规模 | low | mitigate | 图节点受 width×height 网格上限天然约束；Dijkstra 主循环以节点数上界终止，不存在无限循环路径 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装对象） |
</threat_model>

<verification>
- `pnpm exec vitest run "packages-user/data-common/src/common/mover.test.ts"` 4/4 passed
- `pnpm exec vitest run "packages-user/data-system/src/pathfinding"` 全绿
- `pnpm check:type 2>&1 | Select-String -Pattern "pathfinding|mover\.ts"` 无输出（Phase 01 同款过滤式类型门，仓库既有诊断不在本计划范围）
- `pnpm check:circular` 输出中不含 pathfinding 相关路径（eventStore 既有循环为 Phase 01 基线，维持不变）
</verification>

<success_criteria>
L0 缺陷按拍板修复且有回归护栏；L2 寻路核心全部行为（有向边、单向门、终端节点、最小损失、自定义损失、仅取路径、空数组、回退决策）在 Node 下测试验证通过。
</success_criteria>

<output>
Create `.planning/phases/02-pathfinding/02-02-SUMMARY.md` when done
</output>
