---
phase: 02-pathfinding
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - packages-user/data-state/src/path/heroPathfinding.ts
  - packages-user/data-state/src/path/heroPathfinding.test.ts
  - packages-user/data-state/src/hero/moverImpl.ts
  - packages-user/data-base/src/hero/mover.ts
  - packages-user/data-state/src/core.ts
  - packages-user/data-state/src/index.ts
  - packages/common/src/logger.json
autonomous: true
requirements: [PATH-01, PATH-02]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "DefaultHeroMoveTopImpl 通行性判断已重构为 IPassPredicate：predicate() 返回谓词实现，hero mover onStepStart 经 predicate().canPass/shouldHit（IPassCheckHandler: currLoc/nextLoc/direction/floorId）判定；Phase 1 enter/leave/hit 事件派发行为不变（D-07, D-08, D-09）"
    - "逐步自动寻路端到端可用：hero mover 的 predicate() 经 usePassPredicate 注入 L2，`find` 取得的 IPathfindingStep 步骤序列翻译为 Dir 步骤队列并复用 `DefaultHeroMoveTopImpl` 执行，每步走 enter/leave/hit hooks，途经事件自然触发 (D-09)"
    - "瞬移执行可用：决策允许瞬移时经 `ObjectMoveType.Teleport` 到达目标；`PathFallbackPolicy(path) => boolean` 判定需回退（true = 必须逐步）时自动退为逐步寻路，拍板默认策略「路径上存在事件即回退」在 L3 注入 (D-04, D-05)"
    - "D-08 情形 1：no-pass 目标存在可达相邻格时，移动到该相邻格、按拍板方案派发目标 OnTouch、勇士面朝目标 (D-08)"
    - "自动寻路途中被新寻路调用或显式打断入口（interrupt()）打断后，新寻路从最新位置接管 (D-10)"
    - "数据端移动入口已在 data-state barrel 导出，Node 冒烟测试可调用；渲染端点击接线不在本阶段 (D-11)"
    - "CoreState 构造器完成寻路系统接线（照 useTopImplementation 同处接线模式）(D-09)"
  artifacts:
    - "packages-user/data-state/src/path/heroPathfinding.ts"
    - "packages-user/data-state/src/path/heroPathfinding.test.ts"
    - "packages-user/data-state/src/hero/moverImpl.ts"
    - "packages-user/data-base/src/hero/mover.ts"
    - "packages-user/data-state/src/core.ts"
    - "packages-user/data-state/src/index.ts"
    - "packages/common/src/logger.json"
  key_links:
    - "heroPathfinding.ts 将 state.hero mover 的 predicate()（moverImpl 重构产物）注入 02-02 的 usePassPredicate，并注入损失/回退槽位、绑定 hero mover（L3 → L2 合法方向，反向即循环依赖）"
    - "data-base/src/hero/mover.ts 的 onStepStart 判定改经 topImpl.predicate().canPass/shouldHit（IPassCheckHandler 构造），与 moverImpl 重构为同一接口契约的两端"
    - "D-08 OnTouch 派发走 Phase 1 source-aware 执行链（executor.execute，env 形态复刻 moverImpl commonTrigger）"
    - "打断接管经 `IMoverController.stop()`（先停旧再起新，mover.start 移动中返回 null）"
  assumptions:
    - "PATH-01（flagged assumption，spec-less probe 未分类）：自动寻路以「fixture 地图上勇士沿最小损失路径移动且事件触发」的 Node 测试等效验证"
    - "PATH-02（flagged assumption，spec-less probe 未分类）：本计划交付数据端移动入口并在 Node 下冒烟验证；移动端点击触发按 D-11 延迟至 Phase 4 渲染适配验证"
  prohibitions:
    - "不得为寻路引入任何新 npm 依赖（零依赖约束）"
    - "不得把渲染端点击处理接进数据端代码 (D-11)"
    - "D-08 OnTouch 派发必须按用户拍板方案实现，不得静默替换 (D-07, D-08)"
    - "path/types.ts（用户已提交）不得重写；moverImpl 重构仅提取 predicate()，Phase 1 事件派发语义不得变更 (D-07, D-09)"
---

<objective>
在 data-state（L3）完成寻路系统的移动执行接线：先把 moverImpl 通行性判断重构为用户新接口要求的 `IPassPredicate`（`predicate()`），再落地逐步寻路（复用 hero mover 与 Phase 1 事件链）、瞬移与拍板默认回退策略、D-08 情形 1 相邻格 + OnTouch + 面朝目标、打断接管，以及 CoreState/barrel 装配、logger 码登记与阶段门禁。接口签名以用户已提交的 `packages-user/data-system/src/path/types.ts`（commit 7a011b2）为唯一事实源。

Purpose: 让寻路从「算得出」变成「走得通」——勇士真实移动、事件真实触发、输入真实接管；这是 PATH-01 的最终形态与 PATH-02 的数据端入口。
Output: moverImpl predicate 重构 + heroPathfinding.ts + 接线与测试；更新 core.ts、双 barrel、logger.json；全阶段门禁通过。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pathfinding/02-CONTEXT.md
@.planning/phases/02-pathfinding/02-RESEARCH.md
@.planning/phases/02-pathfinding/02-PATTERNS.md
@.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md
@.planning/phases/02-pathfinding/02-02-SUMMARY.md

# L2 寻路核心（用户 types.ts 为接口事实源）与 L3 既有接线点
@packages-user/data-system/src/path/system.ts
@packages-user/data-system/src/path/types.ts
@packages-user/data-base/src/map/types.ts
@packages-user/data-state/src/hero/moverImpl.ts
@packages-user/data-state/src/core.ts
@packages-user/data-state/src/index.ts
@packages-user/data-system/src/index.ts
@packages-user/data-common/src/common/mover.ts
@packages-user/data-base/src/hero/mover.ts
@packages-user/data-base/src/map/types.ts
@packages/common/src/logger.json
@dev.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: moverImpl 通行性判断重构为 IPassPredicate（predicate()）+ hero mover 调用点改接</name>
  <files>packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-base/src/hero/mover.ts</files>
  <read_first>packages-user/data-base/src/hero/types.ts, packages-user/data-base/src/map/types.ts, packages-user/data-base/src/hero/mover.ts, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-state/src/coreEventLayer.test.ts, packages-user/data-common/src/common/mover.ts, dev.md</read_first>
  <action>用户的接口重构（commit 7a011b2）将 `IHeroMoveTopImpl` 的 `canPass(handler)/shouldHit(handler)` 移除、新增 `predicate(): IPassPredicate`（`@user/data-base` map/types.ts §通行检测：`IPassPredicate.canPass/shouldHit(IPassCheckHandler)`，handler 为 `{ currLoc, nextLoc, direction, floorId }`），现有 moverImpl.ts 不再满足接口。本任务做行为保持型重构：(1) 在 `DefaultHeroMoveTopImpl` 内把现 `canPass`（moverImpl.ts:63-125，含四角朝向直通、事件层掩码、其余层 onlyEvents 掩码）与 `shouldHit`（moverImpl.ts:127-141，eventPass 判定）的判定逻辑提取为一个 `IPassPredicate` 实现类；谓词不再依赖 `IHeroMoveTopHandler.face`（`IPassCheckHandler` 无 face 字段）——反向掩码位由 `FaceDirection` 的反向映射直接得出（语义与 moverImpl.ts:85-87 经 face.opposite 等价）；实现 `predicate(): IPassPredicate` 返回该实例；类内原 `canPass/shouldHit` 公有方法移除。(2) 改接 `packages-user/data-base/src/hero/mover.ts` 的调用点：`onStepStart` 内 mover.ts:183 `this.topImpl.canPass(handler)` 与 mover.ts:185 `this.topImpl.shouldHit(handler)` 改为从 `this.topImpl.predicate()` 取谓词，并以 `IPassCheckHandler` 形态（currLoc/nextLoc/direction/floorId，从现有 handler 字段提取）调用 `canPass/shouldHit`；HeroMoveCode 分支结构（Step/Hit/CannotMove）不变。(3) 保持 Phase 1 事件派发行为完整不变：enter/leave/hit → OnEnter/OnLeave/OnTouch（D-08 语义 per 拍板记录——hit/OnTouch 仅在掩码允许到达且目标 no-pass 时触发，掩码导致的不可达走 CannotMove 永不触发 OnTouch）。D-08 拍板语义映射：`canPass=true` 且 `shouldHit=true` → HeroMoveCode.Hit → OnTouch；`canPass=false` → CannotMove（不触发）。全部 jsDoc 中文注释、禁 `as`、禁 `import type`（dev.md）。回归护栏：coreEventLayer.test.ts 与全量既有测试必须全绿（行为保持证明）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/coreEventLayer.test.ts"; if ($?) { pnpm exec vitest run "packages-user/data-base/src" }</automated>
    <fails_when>任一 vitest 输出出现 "failed" 字样或非零退出码——moverImpl 重构改变了 Phase 1 事件派发或 hero 移动判定行为</fails_when>
  </verify>
  <acceptance_criteria>
    - DefaultHeroMoveTopImpl 满足新 IHeroMoveTopImpl：含 predicate(): IPassPredicate，无 canPass/shouldHit 公有方法（源断言）
    - data-base/src/hero/mover.ts 中无 `topImpl.canPass(`/`topImpl.shouldHit(` 直接调用，改经 predicate()（源断言）
    - coreEventLayer.test.ts 与 data-base 既有测试全绿，Phase 1 OnEnter/OnLeave/OnTouch 行为不变（行为断言）
    - 掩码语义保持：单向门（outPass/inPass 方向位）、事件层恒参与、其余层仅 onlyEvents 参与的行为与重构前等价（行为断言，由既有测试覆盖）
  </acceptance_criteria>
  <done>moverImpl 满足用户新接口且行为保持：predicate() 可被 L3 注入 usePassPredicate，hero 移动判定与事件派发零回归。</done>
</task>

<task type="tracer">
  <name>Task 2: 逐步自动寻路端到端接线（CoreState + usePassPredicate 注入 + 路径翻译 + hero mover 执行）</name>
  <files>packages-user/data-state/src/path/heroPathfinding.ts, packages-user/data-state/src/path/heroPathfinding.test.ts, packages-user/data-state/src/core.ts, packages-user/data-state/src/index.ts</files>
  <read_first>packages-user/data-system/src/path/types.ts, packages-user/data-system/src/path/system.ts, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-state/src/core.ts, packages-user/data-base/src/hero/mover.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-system/src/event/eventDispatch.test.ts, dev.md</read_first>
  <action>创建 L3 `HeroPathfinding` 类于 `packages-user/data-state/src/path/heroPathfinding.ts`（imports 骨架照 moverImpl.ts:1-32：从 `@user/data-system` 引入 L2 寻路系统——此方向合法）。职责：构造时绑定 `IObjectMovable`（hero mover）到 02-02 `PathfindingSystem` 的 `useMovable` 槽位、事件层 `IMapLayer` 到 finder 的 `useMapLayer` 槽位（D-03）；将 Task 1 重构所得 hero mover 顶层实现的 `predicate()` 经 `usePassPredicate` 注入 L2（D-03/D-09 关键接线）；注入拍板默认回退策略（「路径上存在事件即回退」，Task 3 实现细节，本任务先接策略槽位）。实现逐步执行：调用 `finder.find`/`system.getPath` 取得 `IPathfindingStep[]` 最小损失步骤序列后，将步骤的 `dir`（`FaceDirection`）翻译为 `ObjectMoveType.Dir` 步骤队列（每步一步，保证 canPass 逐格判定，per RESEARCH Code Examples 语义），经 mover `push` + `start` 启动（D-09）；heroPathfinding 的公开入口与 `IPathfindingSystem` 的 `moveTo/teleportTo/getPath/interrupt` 对齐（签名以用户 types.ts 为唯一事实源，D-07）。core.ts 在 `//#endregion 勇士顶层初始化` region 内、`useTopImplementation` 之后新增寻路接线（照 core.ts:235-237 模式）；data-state/src/index.ts 在 `export * from './hero';` 之后追加 path 接线导出（dev.md：不转发导出）。data-system/src/index.ts 的 path 导出已由 02-02 Task 3 落地（该文件的唯一归属任务），本任务只验证其存在且可被 `@user/data-system` 引入，不重复写入该行。heroPathfinding.test.ts 用 eventDispatch.test.ts:105-179 fixture 模式：勇士在 fixture 地图上自动寻路走两步，断言终点坐标、leave/enter 事件按 Phase 1 source-aware 顺序触发、途经带事件的图块事件被执行。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/path/heroPathfinding.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——勇士未到达目标、事件未按序触发或翻译步骤数与路径不符</fails_when>
  </verify>
  <acceptance_criteria>
    - 勇士沿最小损失路径逐步移动并停在目标格，坐标断言通过（行为断言）
    - 途经图块的 leave/enter 钩子触发且 env 的 heroLocator 与实际位置一致（行为断言）
    - hero 的 predicate() 已注入 L2 usePassPredicate（L2 未注入谓词时不可行，注入后图可达——行为断言）
    - core.ts 含寻路接线且位于勇士顶层初始化 region（源断言）
    - 两个 barrel 均导出寻路内容，`pnpm check:type 2>&1 | Select-String -Pattern "heroPathfinding|src[\\/]path[\\/]|core\.ts"` 无输出（CLI 断言）
  </acceptance_criteria>
  <done>勇士能在 fixture 地图上自动寻路移动到指定格，途经事件自然触发——PATH-01 的核心闭环成立。</done>
</task>

<task type="auto">
  <name>Task 3: 瞬移执行 + 拍板默认回退策略 + D-08 情形 1（相邻格 + OnTouch + 面朝）</name>
  <files>packages-user/data-state/src/path/heroPathfinding.ts, packages-user/data-state/src/path/heroPathfinding.test.ts</files>
  <read_first>packages-user/data-system/src/path/types.ts, packages-user/data-state/src/path/heroPathfinding.ts, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-common/src/event/types.ts, packages-user/data-base/src/map/types.ts, .planning/phases/02-pathfinding/02-RESEARCH.md</read_first>
  <action>扩展 heroPathfinding.ts：(1) 瞬移执行——`PathFallbackPolicy(path) => boolean`（path 为 `IPathfindingStep[]` 步骤序列）决策允许瞬移（返回 false）时以 `ObjectMoveType.Teleport` 步骤到达目标（D-04）；(2) 拍板默认回退策略——「路径上存在事件即回退」：沿步骤序列逐格用 moverImpl.ts:169-204 同款方式探测事件（点事件 + 静态/动态图块事件），命中即策略返回 true（必须逐步、禁止瞬移），L3 将此策略经 `useFallbackPolicy` 注入 L2（D-05；L2 的 null 默认为必定逐步，本策略为游戏默认，per 用户 types.ts + 拍板）；(3) D-08 情形 1——目标为 no-pass 图块且四邻存在可达格时（相邻格可达性以 `finder.find(heroPos, 相邻格)` 判定）：寻路至该相邻格并移动到达，按用户拍板方案派发目标 OnTouch（拍板方案 A：直接构造 `IGameEventInvocation`，env 逐字复刻 moverImpl.ts:206-222 `commonTrigger` 形态——state、type 按目标事件来源、trigger=`EventTrigger.OnTouch`、heroLocator=相邻格、triggerLocator=目标格、tile/layer/map 按 env 接口可空语义填实，调 `executor.execute`；拍板方案 B：按用户拍板的替代语义实现）；随后以纯转向步或 `setFaceDir` 使勇士面朝目标（D-08）。测试补充：瞬移在无事件路径上直达目标；路径含事件时默认策略退为逐步且途经事件触发；no-pass 目标带 OnTouch 事件时勇士停于相邻格、面朝目标、mock 事件以 OnTouch 触发且 env.triggerLocator 为目标格（mock 事件照 eventDispatch.test.ts:181-211 addEvent 模式）；无相邻可达格的不可达目标保持空数组不移动（D-08 情形 2 回归）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/path/heroPathfinding.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——瞬移未直达、事件路径未回退逐步、OnTouch 未按拍板方案触发或面朝方向错误</fails_when>
  </verify>
  <acceptance_criteria>
    - 无事件路径瞬移一步到达目标，坐标断言通过（行为断言）
    - 含事件路径在默认策略（返回 true）下退为逐步且途经事件触发（行为断言）
    - no-pass 目标：勇士位于相邻格、朝向指向目标、OnTouch 事件以拍板方案派发且 triggerLocator 为目标格（行为断言）
    - 四邻皆不可达的 no-pass 目标：路径空数组、勇士不动（行为断言）
  </acceptance_criteria>
  <done>D-04/D-05/D-08 全部语义在 L3 落地且测试验证。</done>
</task>

<task type="auto">
  <name>Task 4: 打断接管 + logger 码登记 + 阶段门禁</name>
  <files>packages-user/data-state/src/path/heroPathfinding.ts, packages-user/data-state/src/path/heroPathfinding.test.ts, packages/common/src/logger.json</files>
  <read_first>packages-user/data-system/src/path/types.ts, packages-user/data-state/src/path/heroPathfinding.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-base/src/hero/mover.ts, packages/common/src/logger.json, .planning/phases/02-pathfinding/02-RESEARCH.md</read_first>
  <action>实现打断接管（D-10）：寻路控制器持有当前 `IMoverController` 引用（即 02-02 返回的 `IPathfindingController.controller`）；L2 `interrupt(): Promise<void>` 的语义在此接线——新寻路调用或显式 `interrupt()` 入口到达时，按拍板时序选项 1（stop 后 await 兑现，再起新寻路）停止旧移动，从最新坐标起算并启动新路径——注意 mover.start 移动中返回 null（mover.ts:669-670），必须先完成旧移动的停止（await 兑现）再 start；识别 HeroMover.onStepEnd 在 CannotMove/Stop/Hit 时的自然终止，避免对已终止移动重复 stop（RESEARCH Pitfall 5）。在 logger.json 集中登记本阶段全部新码（warn ≥173、error ≥65，与 02-02 已登记码合并查重，RESEARCH Pitfall 7）。测试补充：移动中途发起新寻路，旧路径中止且新路径从最新位置起算到达新目标；显式 interrupt() 入口等效。最后跑阶段门禁并修复本阶段文件暴露的问题。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/path/heroPathfinding.test.ts"; if ($?) { pnpm check:type 2>&1 | Select-String -Pattern "src[\\/]path[\\/]|mover\.ts|core\.ts" }</automated>
    <fails_when>vitest 输出出现 "failed" 字样，或 Select-String 对本阶段文件输出任何类型诊断行（命中即存在计划内文件的类型错误）</fails_when>
  </verify>
  <acceptance_criteria>
    - 移动中发起新寻路：旧路径停止、新路径自最新坐标起算并到达新目标（行为断言）
    - 显式打断后 mover 处于可再次 start 的干净状态（行为断言）
    - logger.json 新码无撞码：warn 新码 ≥173、error 新码 ≥65 且与既有键位不重复（CLI 断言：node 枚举 logger.json 键位核对）
    - `pnpm check:circular` 输出不含 path 相关路径；`pnpm lint:user` 对本阶段文件无报错（CLI 断言）
  </acceptance_criteria>
  <done>打断接管按拍板时序（stop 兑现后起新）可用；全量测试绿、循环依赖与 lint 门禁对本阶段文件清洁。</done>
</task>

</tasks>

<threat_model>
## Artifacts this phase produces

- 类：`HeroPathfinding`（L3 接线类，`packages-user/data-state/src/path/heroPathfinding.ts`）
- 类/方法（重构）：`DefaultHeroMoveTopImpl` 的 `predicate(): IPassPredicate`（通行性掩码逻辑提取为 `IPassPredicate` 实现，行为保持）；`data-base/src/hero/mover.ts` onStepStart 改经 predicate().canPass/shouldHit
- 方法：路径步骤序列→`ObjectMoveType.Dir` 步骤翻译、逐步执行（复用 hero mover 事件链，D-09）、瞬移执行（D-04）、拍板默认回退策略「路径上存在事件即回退」经 useFallbackPolicy 注入（D-05）、D-08 情形 1 相邻格 + OnTouch + 面朝、打断接管入口（D-10）
- 修改：`packages-user/data-state/src/core.ts`（CoreState 寻路接线）、`packages-user/data-state/src/index.ts`（barrel）、`packages/common/src/logger.json`（集中登记码位）；`packages-user/data-system/src/index.ts` 的 path 导出已由 02-02 Task 3 落地（该文件唯一归属在 02-02），本计划仅验证不修改
- 测试：`heroPathfinding.test.ts`（端到端逐步/瞬移/回退/D-08 双语义/打断）；moverImpl 重构以既有 coreEventLayer.test.ts 与 data-base 测试为回归护栏

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 地图/存档数据 → OnTouch env 构造 | 目标格/图层可能为空或畸形，进入 `IBlockEventEnv` 构造 |
| 玩家输入时序 → 移动队列 | 打断与新寻路竞态可能使 mover 队列处于不一致状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | D-08 OnTouch env 构造 | low | mitigate | env 各字段按 `IBlockEventEnv` 可空语义用 `isNil` 判空填实，非法来源不构造 invocation（Task 2，复刻 commonTrigger 形态） |
| T-02-05 | DoS | 打断接管竞态 | low | mitigate | 单一 controller 所有权：先按拍板时序停旧移动、确认终止后起新路径；识别自然终止避免重复 stop（Task 3） |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装对象） |
</threat_model>

<verification>
- `pnpm exec vitest run "packages-user/data-state/src/path/heroPathfinding.test.ts"` 全绿
- `pnpm exec vitest run "packages-user/data-state/src/coreEventLayer.test.ts"` 全绿（moverImpl 重构回归护栏）
- `pnpm exec vitest run` 全量通过（Phase 02 + Phase 01 既有测试无回归）
- `pnpm check:type 2>&1 | Select-String -Pattern "src[\\/]path[\\/]|mover\.ts|core\.ts"` 无输出
- `pnpm check:circular` 输出不含 path 相关路径（eventStore 既有循环维持 Phase 01 基线）
- `pnpm lint:user` 无新报错
</verification>

<success_criteria>
moverImpl 满足用户重构后的 IHeroMoveTopImpl（predicate(): IPassPredicate）且 Phase 1 行为零回归；勇士在 fixture 地图上可自动寻路（逐步/瞬移/回退/不可达双语义/打断接管全部符合拍板契约）；数据端入口经 barrel 导出且 Node 可验证；阶段门禁全绿。PATH-01 交付完成，PATH-02 以数据端入口形态交付（点击接线属 Phase 4，per D-11）。
</success_criteria>

<output>
Create `.planning/phases/02-pathfinding/02-03-SUMMARY.md` when done
</output>
