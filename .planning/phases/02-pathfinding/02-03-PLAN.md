---
phase: 02-pathfinding
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - packages-user/data-state/src/pathfinding/heroPathfinding.ts
  - packages-user/data-state/src/pathfinding/heroPathfinding.test.ts
  - packages-user/data-state/src/core.ts
  - packages-user/data-state/src/index.ts
  - packages-user/data-system/src/index.ts
  - packages/common/src/logger.json
autonomous: true
requirements: [PATH-01, PATH-02]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "逐步自动寻路端到端可用：路径翻译为 Dir 步骤队列并复用 `DefaultHeroMoveTopImpl` 执行，每步走 enter/leave/hit hooks，途经事件自然触发 (D-09)"
    - "瞬移执行可用：决策允许瞬移时经 `ObjectMoveType.Teleport` 到达目标；拍板的回退策略判定需回退时自动退为逐步寻路，默认策略为路径上存在事件即回退 (D-04, D-05)"
    - "D-08 情形 1：no-pass 目标存在可达相邻格时，移动到该相邻格、按拍板方案派发目标 OnTouch、勇士面朝目标 (D-08)"
    - "自动寻路途中被新寻路调用或显式打断入口打断后，新寻路从最新位置接管 (D-10)"
    - "数据端移动入口已在 data-state barrel 导出，Node 冒烟测试可调用；渲染端点击接线不在本阶段 (D-11)"
    - "CoreState 构造器完成寻路系统接线（照 useTopImplementation 同处接线模式）(D-09)"
  artifacts:
    - "packages-user/data-state/src/pathfinding/heroPathfinding.ts"
    - "packages-user/data-state/src/pathfinding/heroPathfinding.test.ts"
    - "packages-user/data-state/src/core.ts"
    - "packages-user/data-state/src/index.ts"
    - "packages-user/data-system/src/index.ts"
    - "packages/common/src/logger.json"
  key_links:
    - "heroPathfinding.ts 注入 02-02 的损失/回退/谓词槽位并绑定 hero mover（L3 → L2 合法方向，反向即循环依赖）"
    - "D-08 OnTouch 派发走 Phase 1 source-aware 执行链（executor.execute，env 形态复刻 moverImpl commonTrigger）"
    - "打断接管经 `IMoverController.stop()`（先停旧再起新，mover.start 移动中返回 null）"
  assumptions:
    - "PATH-01（flagged assumption，spec-less probe 未分类）：自动寻路以「fixture 地图上勇士沿最小损失路径移动且事件触发」的 Node 测试等效验证"
    - "PATH-02（flagged assumption，spec-less probe 未分类）：本计划交付数据端移动入口并在 Node 下冒烟验证；移动端点击触发按 D-11 延迟至 Phase 4 渲染适配验证"
  prohibitions:
    - "不得为寻路引入任何新 npm 依赖（零依赖约束）"
    - "不得把渲染端点击处理接进数据端代码 (D-11)"
    - "D-08 OnTouch 派发必须按用户拍板方案实现，不得静默替换 (D-07, D-08)"
---

<objective>
在 data-state（L3）完成寻路系统的移动执行接线：逐步寻路复用 hero mover 与 Phase 1 事件链、瞬移与默认回退策略、D-08 情形 1 相邻格 + OnTouch + 面朝目标、打断接管，以及 CoreState/barrel 装配、logger 码登记与阶段门禁。

Purpose: 让寻路从「算得出」变成「走得通」——勇士真实移动、事件真实触发、输入真实接管；这是 PATH-01 的最终形态与 PATH-02 的数据端入口。
Output: heroPathfinding.ts + 接线与测试；更新 core.ts、双 barrel、logger.json；全阶段门禁通过。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pathfinding/02-CONTEXT.md
@.planning/phases/02-pathfinding/02-RESEARCH.md
@.planning/phases/02-pathfinding/02-PATTERNS.md
@.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md
@.planning/phases/02-pathfinding/02-02-SUMMARY.md

# L2 寻路核心与 L3 既有接线点
@packages-user/data-system/src/pathfinding/system.ts
@packages-user/data-system/src/pathfinding/types.ts
@packages-user/data-state/src/hero/moverImpl.ts
@packages-user/data-state/src/core.ts
@packages-user/data-state/src/index.ts
@packages-user/data-system/src/index.ts
@packages-user/data-common/src/common/mover.ts
@packages-user/data-base/src/hero/mover.ts
@packages-user/data-base/src/map/types.ts
@packages/common/src/logger.json
@dev.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 逐步自动寻路端到端接线（CoreState + 路径翻译 + hero mover 执行）</name>
  <files>packages-user/data-state/src/pathfinding/heroPathfinding.ts, packages-user/data-state/src/pathfinding/heroPathfinding.test.ts, packages-user/data-state/src/core.ts, packages-user/data-state/src/index.ts, packages-user/data-system/src/index.ts</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-state/src/core.ts, packages-user/data-base/src/hero/mover.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-system/src/event/eventDispatch.test.ts, dev.md</read_first>
  <action>创建 L3 `HeroPathfinding` 类（imports 骨架照 moverImpl.ts:1-32：从 `@user/data-system` 引入 L2 寻路系统——此方向合法）。职责：构造时绑定 `IObjectMovable`（hero mover）与 `IMapLayer`（event 层）到 02-02 的 `PathfindingSystem` 注入槽位（D-03）；注入默认通行性谓词（复刻 `DefaultHeroMoveTopImpl` canPass 语义）与回退策略槽位。实现逐步执行：调用寻路系统取得最小损失路径后，将路径翻译为 `ObjectMoveType.Dir` 步骤队列（相邻坐标增量 → `FaceDirection`，每次一步保证 canPass 逐格判定，per RESEARCH Code Examples 语义），经 mover `push` + `start` 启动（D-09）。core.ts 在 `//#endregion 勇士顶层初始化` region 内、`useTopImplementation` 之后新增寻路接线（照 core.ts:235-237 模式）；data-state/src/index.ts 在 `export * from './hero';` 之后追加寻路导出，data-system/src/index.ts 追加 pathfinding 导出（dev.md：不转发导出）。heroPathfinding.test.ts 用 eventDispatch.test.ts:105-179 fixture 模式：勇士在 fixture 地图上自动寻路走两步，断言终点坐标、leave/enter 事件按 Phase 1 source-aware 顺序触发、途经带事件的图块事件被执行。签名与命名以拍板草案为唯一事实源（D-07）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/pathfinding/heroPathfinding.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——勇士未到达目标、事件未按序触发或翻译步骤数与路径不符</fails_when>
  </verify>
  <acceptance_criteria>
    - 勇士沿最小损失路径逐步移动并停在目标格，坐标断言通过（行为断言）
    - 途经图块的 leave/enter 钩子触发且 env 的 heroLocator 与实际位置一致（行为断言）
    - core.ts 含寻路接线且位于勇士顶层初始化 region（源断言）
    - 两个 barrel 均导出寻路内容，`pnpm check:type 2>&1 | Select-String -Pattern "heroPathfinding|pathfinding|core\.ts"` 无输出（CLI 断言）
  </acceptance_criteria>
  <done>勇士能在 fixture 地图上自动寻路移动到指定格，途经事件自然触发——PATH-01 的核心闭环成立。</done>
</task>

<task type="auto">
  <name>Task 2: 瞬移执行 + 默认回退策略 + D-08 情形 1（相邻格 + OnTouch + 面朝）</name>
  <files>packages-user/data-state/src/pathfinding/heroPathfinding.ts, packages-user/data-state/src/pathfinding/heroPathfinding.test.ts</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-state/src/pathfinding/heroPathfinding.ts, packages-user/data-state/src/hero/moverImpl.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-common/src/event/types.ts, packages-user/data-base/src/map/types.ts, .planning/phases/02-pathfinding/02-RESEARCH.md</read_first>
  <action>扩展 heroPathfinding.ts：(1) 瞬移执行——移动方式决策允许瞬移时以 `ObjectMoveType.Teleport` 步骤到达目标（D-04）；(2) 默认回退策略——按拍板草案实现「路径上存在事件即回退」：沿路径用 moverImpl.ts:169-204 同款方式探测事件（点事件 + 静态/动态图块事件），命中即退为逐步执行（D-05）；策略判不需回退时才瞬移；(3) D-08 情形 1——目标为 no-pass 图块且四邻存在可达格时：寻路至该相邻格并移动到达，按用户拍板方案派发目标 OnTouch（拍板方案 A：直接构造 `IGameEventInvocation`，env 逐字复刻 moverImpl.ts:206-222 `commonTrigger` 形态——state、type 按目标事件来源、trigger=`EventTrigger.OnTouch`、heroLocator=相邻格、triggerLocator=目标格、tile/layer/map 按 env 接口可空语义填实，调 `executor.execute`；拍板方案 B：按用户拍板的替代语义实现）；随后以纯转向步或 `setFaceDir` 使勇士面朝目标（D-08）。测试补充：瞬移在无事件路径上直达目标；路径含事件时默认策略退为逐步且途经事件触发；no-pass 目标带 OnTouch 事件时勇士停于相邻格、面朝目标、mock 事件以 OnTouch 触发且 env.triggerLocator 为目标格（mock 事件照 eventDispatch.test.ts:181-211 addEvent 模式）；无相邻可达格的不可达目标保持空数组不移动（D-08 情形 2 回归）。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/pathfinding/heroPathfinding.test.ts"</automated>
    <fails_when>vitest 输出出现 "failed" 字样或非零退出码——瞬移未直达、事件路径未回退逐步、OnTouch 未按拍板方案触发或面朝方向错误</fails_when>
  </verify>
  <acceptance_criteria>
    - 无事件路径瞬移一步到达目标，坐标断言通过（行为断言）
    - 含事件路径在默认策略下退为逐步且途经事件触发（行为断言）
    - no-pass 目标：勇士位于相邻格、朝向指向目标、OnTouch 事件以拍板方案派发且 triggerLocator 为目标格（行为断言）
    - 四邻皆不可达的 no-pass 目标：路径空数组、勇士不动（行为断言）
  </acceptance_criteria>
  <done>D-04/D-05/D-08 全部语义在 L3 落地且测试验证。</done>
</task>

<task type="auto">
  <name>Task 3: 打断接管 + logger 码登记 + 阶段门禁</name>
  <files>packages-user/data-state/src/pathfinding/heroPathfinding.ts, packages-user/data-state/src/pathfinding/heroPathfinding.test.ts, packages/common/src/logger.json</files>
  <read_first>.planning/phases/02-pathfinding/02-INTERFACE-DRAFT.md, packages-user/data-state/src/pathfinding/heroPathfinding.ts, packages-user/data-common/src/common/mover.ts, packages-user/data-base/src/hero/mover.ts, packages/common/src/logger.json, .planning/phases/02-pathfinding/02-RESEARCH.md</read_first>
  <action>实现打断接管（D-10）：寻路控制器持有当前 `IMoverController` 引用；新寻路调用或显式打断入口到达时，按拍板的时序（stop 后 await 兑现，或 onEnd 回调驱动）停止旧移动，从最新坐标起算并启动新路径——注意 mover.start 移动中返回 null（mover.ts:669-670），必须先完成旧移动的停止再 start；识别 HeroMover.onStepEnd 在 CannotMove/Stop/Hit 时的自然终止，避免对已终止移动重复 stop（RESEARCH Pitfall 5）。在 logger.json 集中登记本阶段全部新码（warn ≥173、error ≥65，与 02-02 已登记码合并查重，RESEARCH Pitfall 7）。测试补充：移动中途发起新寻路，旧路径中止且新路径从最新位置起算到达新目标；显式打断入口等效。最后跑阶段门禁并修复本阶段文件暴露的问题。</action>
  <verify>
    <automated>pnpm exec vitest run "packages-user/data-state/src/pathfinding/heroPathfinding.test.ts"; if ($?) { pnpm check:type 2>&1 | Select-String -Pattern "pathfinding|mover\.ts|core\.ts" }</automated>
    <fails_when>vitest 输出出现 "failed" 字样，或 Select-String 对本阶段文件输出任何类型诊断行（命中即存在计划内文件的类型错误）</fails_when>
  </verify>
  <acceptance_criteria>
    - 移动中发起新寻路：旧路径停止、新路径自最新坐标起算并到达新目标（行为断言）
    - 显式打断后 mover 处于可再次 start 的干净状态（行为断言）
    - logger.json 新码无撞码：warn 新码 ≥173、error 新码 ≥65 且与既有键位不重复（CLI 断言：node 枚举 logger.json 键位核对）
    - `pnpm check:circular` 输出不含 pathfinding 相关路径；`pnpm lint:user` 对本阶段文件无报错（CLI 断言）
  </acceptance_criteria>
  <done>打断接管按拍板时序可用；全量测试绿、循环依赖与 lint 门禁对本阶段文件清洁。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 地图/存档数据 → OnTouch env 构造 | 目标格/图层可能为空或畸形，进入 `IBlockEventEnv` 构造 |
| 玩家输入时序 → 移动队列 | 打断与新寻路竞态可能使 mover 队列处于不一致状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | D-08 OnTouch env 构造 | low | mitigate | env 各字段按 `IBlockEventEnv` 可空语义用 `isNil` 判空填实，非法来源不构造 invocation（Task 2，复刻 commonTrigger 形态） |
| T-02-05 | DoS | 打断接管竞态 | low | mitigate | 单一 controller 所有权：先按拍板时序停旧移动、确认终止后起新路径；识别自然终止避免重复 stop（Task 3） |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | 本计划零新依赖（RESEARCH Package Legitimacy Audit：无安装对象） |
</threat_model>

<verification>
- `pnpm exec vitest run "packages-user/data-state/src/pathfinding/heroPathfinding.test.ts"` 全绿
- `pnpm exec vitest run` 全量通过（Phase 02 + Phase 01 既有测试无回归）
- `pnpm check:type 2>&1 | Select-String -Pattern "pathfinding|mover\.ts|core\.ts"` 无输出
- `pnpm check:circular` 输出不含 pathfinding 相关路径（eventStore 既有循环维持 Phase 01 基线）
- `pnpm lint:user` 无新报错
</verification>

<success_criteria>
勇士在 fixture 地图上可自动寻路（逐步/瞬移/回退/不可达双语义/打断接管全部符合拍板契约）；数据端入口经 barrel 导出且 Node 可验证；阶段门禁全绿。PATH-01 交付完成，PATH-02 以数据端入口形态交付（点击接线属 Phase 4，per D-11）。
</success_criteria>

<output>
Create `.planning/phases/02-pathfinding/02-03-SUMMARY.md` when done
</output>
