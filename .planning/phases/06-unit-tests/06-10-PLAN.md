---
phase: 06-unit-tests
plan: 10
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - packages-user/data-system/src/combat/context.test.ts
  - packages-user/data-system/src/combat/mapDamage.test.ts
  - packages-user/data-state/test/enemyCombination.test.ts
  - .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TEST-01 由数据端行为单测满足，门禁为固定非 watch 命令 `pnpm test:ci` 全绿（D-08）。"
    - "G-06-01-A：一只怪物同时受 光环基础效果（`onApply`）+ 常规查询效果（`registerCommonQueryEffect`）+ 特殊查询效果（`registerSpecialQueryEffect`）+ final 效果（`registerFinalEffect`）作用，显式断言四阶段顺序 `special → base → query → final`（后两阶段内部次序按实现可取子序列断言），并断言 buildup 完成后计算后怪物的最终 atk/def/hp 与特殊属性（D-25 深化）。"
    - "G-06-01-B：多怪跨施加的嵌套光环 —— m1 产生光环 A1 施加到 m2；m2 因此获得特殊属性并生成光环 A2；A2 反向施加 m1 与 m2；m3 在 A1 范围外、在 A2 范围内。断言 m1/m2/m3 各自计算后最终属性、m2 持有被转换特殊属性而 m1/m3 不持有、转换器对代码 20 与 21 均被调用、A1 范围边界可观测（m1/m3 不受 A1 影响）。"
    - "G-06-01-C：系统层同点多来源地图伤害叠加 —— 有来源+有来源、无来源+无来源、多来源（>2，且有来源与无来源混合）三种组合；断言 `getSeparatedDamage` 条数与 `getReducedDamage` 的 damage 求和 / type 取最大 / extra 并集符合语义。"
    - "G-06-07-A：构建「尽可能多流水线同时生效」的单怪（顶层多特殊属性 + 光环 25 / 支援 26 + 常规查询 + 特殊查询 + final 效果 + 真实 `MainEnemyFinalEffect`），经真实 `MainDamageCalculator` 断言唯一精确的最终 `{damage, turn}`（仅含 damage 与 turn 两字段）。"
    - "接口覆盖：本计划只经既有公开接口驱动（`EnemyContext`、`MapDamage`、`MainDamageCalculator`、`createCoreState`），不新增生产接口、不 mock 数据层接口本身；名称含 legacy 的接口/方法一律排除（D-30）。"
    - "code 覆盖（模块可达）：本计划不引入新码；新增用例复用并再确认本模块既有可达码（如 102/103/104）的覆盖不被削弱（D-31）。"
    - "测试形态（D-01）：阶段 1/2 为纯系统层单测（`data-system/src/combat`，fake/显式依赖注入，延续 D-21）；阶段 3 经 `CoreState` 集成路径（`data-state/test`）。"
    - "阶段化（D-43）：计划按 构件→组合/流水线→完整/集成 三阶段组织，取消 tracer-first；每阶段跑绿后才进入下一阶段；某阶段发现阻断性 bug 时暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。"
    - "质量门禁（D-44）：每个计划执行后、提交前，改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误（全局既有无关错误不在范围）；`pnpm test:ci` 全绿；任一不通过不得提交。"
    - "执行节奏（D-33）：本计划执行前先向用户确认；执行完成后暂停，用中文分条简要汇报验证结果，详细结果写入共用的 `06-TEST-FINDINGS.md`。"
    - "增量补测（D-46）：只新增/扩展 `*.test.ts`，不改动已执行的 06-01..06-09 计划与任何生产/核心源码；fixture 文件内联（D-02/D-03），异步路径只用真实计时器（D-04）。"
    - "疑似缺陷按正确预期编写并标记 `it.skip`/`it.todo`，并按 D-06 格式登记到共用的 `06-TEST-FINDINGS.md`（本计划预期无需新增 skip）。"
  artifacts:
    - path: packages-user/data-system/src/combat/context.test.ts
      provides: G-06-01-A 四类效果同时生效的最终属性断言、G-06-01-B 多怪跨施加嵌套光环最终属性断言
    - path: packages-user/data-system/src/combat/mapDamage.test.ts
      provides: G-06-01-C 同点多来源地图伤害叠加的 separated/reduced 断言
    - path: packages-user/data-state/test/enemyCombination.test.ts
      provides: G-06-07-A 最大流水线组合单怪最终 {damage,turn} 断言
    - path: .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
      provides: 阶段级 code → 模块 → 用例 映射表（本计划 create-or-append 06-10 小节）
  key_links:
    - "context.test.ts 用测试用 fake `IAuraConverter`/`IAuraView` 手动装配（`new EnemyContext()` + `resize` + `bindHero` + `registerAuraConverter` + `registerCommonQueryEffect` + `registerSpecialQueryEffect` + `registerFinalEffect` + `setEnemyAt` + `buildup`）驱动，绝不引用/重测顶层真实实现（D-21）"
    - "context.test.ts 的 G-06-01-B 复用既有 `createContextFixture(w,h)`、`FakeAura`（priority/range/param/couldApplyBase/couldApplySpecial/onApply/onApplySpecial）、`FakeConverter(codes, factory)` 与 `modules.RectRange`/`FullRange` 参数形状（Rect：`{x,y,w,h}`；Manhattan：`{cx,cy,radius}`；Full：`undefined`）"
    - "mapDamage.test.ts 复用既有 `FakeView(value, indexes)`/`FakeConverter(views)`/`createInfo(damage)` 与 `MapLocIndexer` 夹具，新增内联 reducer 实现「damage 求和 / type 取最大 / extra 并集」语义，经 `getSeparatedDamage`/`getReducedDamage` 观测"
    - "enemyCombination.test.ts 复用 `createCoreState`、`createContext`（真实 `CommonAuraConverter`/`GuardAuraConverter`/`MainEnemyFinalEffect`）、`createEnemy`、`createHero`/`createContextHandler`、`MainDamageCalculator` 与 `createSpecial`"
    - "`.planning/phases/06-unit-tests/06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写 06-10 小节前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本小节，绝不重写他节"
  prohibitions:
    - "不创建或修改任何生产/核心源码，只新增/扩展 `*.test.ts` 与规划产物"
    - "不改动 06-01..06-09 的 PLAN.md"
    - "不测试 `saveState`/`loadState`（集中到 06-13）（D-32）"
    - "不安装覆盖率工具（D-09）"
    - "不建立跨文件共享 fixture/factory helper，fixture 全部文件内联（D-02/D-03）"
    - "异步路径只用真实计时器，禁用 `vi.useFakeTimers()`（D-04）"
    - "疑似 bug 不得弱化为通过；按正确预期编写并标记 `it.skip`/`it.todo`（D-05）"
---

<objective>
按 D-43 阶段化（构件 → 组合/流水线 → 完整/集成）补齐 06-01（战斗系统）与 06-02（顶层战斗实现）人工评审发现的覆盖缺口（D-46）：

- **G-06-01-A** 四类效果同时生效后的最终属性未断言（`context.test.ts`）
- **G-06-01-B** 多怪跨施加的嵌套光环未覆盖（`context.test.ts`）
- **G-06-01-C** 地图伤害叠加场景未覆盖（`mapDamage.test.ts`）
- **G-06-07-A** 缺少「尽可能多流水线组合」的单怪最终伤害断言（`enemyCombination.test.ts`）

Purpose: 现有用例覆盖了单效果、单光环、阶段顺序与系统层流水线，但缺少「多来源/多效果同时叠加后唯一的最终结果」与「跨怪嵌套光环」的组合断言。本计划只补测试，不改任何生产代码。

Output: 3 个既有 `*.test.ts` 的扩展与 `06-COVERAGE-MAP.md` 的 06-10 小节。

阶段结构（D-43）：阶段 1 构件级（G-06-01-C 同点多来源地图伤害）→ 阶段 2 组合/流水线（G-06-01-A 四类效果同时生效 + G-06-01-B 多怪嵌套光环）→ 阶段 3 完整/集成（G-06-07-A 最大流水线组合单怪最终伤害）。每阶段跑绿后再进入下一阶段。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-unit-tests/06-CONTEXT.md
@.planning/phases/06-unit-tests/06-COVERAGE-GAPS.md
@.planning/phases/06-unit-tests/06-PATTERNS.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@.planning/codebase/TESTING.md
@dev.md
@packages-user/data-system/src/combat/context.ts
@packages-user/data-system/src/combat/mapDamage.ts
@packages-user/data-system/src/combat/types.ts
@packages-user/data-system/src/combat/context.test.ts
@packages-user/data-system/src/combat/mapDamage.test.ts
@packages-user/data-state/test/enemyCombination.test.ts
@packages-user/data-state/src/enemy/calculator.ts
@packages-user/data-state/src/enemy/aura.ts
@packages-user/data-state/src/enemy/final.ts
@packages/common/src/logger.json
</context>

<execution_rules>
- 只写测试，绝不修改 `packages-user/*/src` 下的非 `*.test.ts` 文件。
- 每个 `it(...)` 之前必须有一行中文注释说明覆盖内容（dev.md 注释规范），测试范围变化时同步更新。
- 异步动作（mover/战斗长动作）用真实计时器 + await 控制器完成，禁用 `vi.useFakeTimers()`（D-04）。
- fixture 内联且最小（D-02/D-03），沿用目标文件既有 helper，不新造跨文件 helper。
- 警告/错误路径经 `logger.catch` / `vi.spyOn(logger, ...)`。
- 疑似 bug：按正确预期编写、标记 `it.skip`/`it.todo`、中文注释锚定 `#06-XX-N` 并登记 `06-TEST-FINDINGS.md`；`pnpm test:ci` 必须保持全绿（D-05）。
- 本计划不测 `saveState`/`loadState`（D-32）。
- `06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写入前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本计划小节，绝不重写他节。
- **执行协议（D-33）：执行本计划前必须先向用户确认；执行完成后暂停，用中文分条简要汇报本计划验证结果；详细结果写入共用的 `06-TEST-FINDINGS.md`。**
- **阶段门禁（D-43）：本计划按 构件 → 组合/流水线 → 完整/集成 三阶段顺序执行，每阶段跑绿后才进入下一阶段；若某阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。**
- **质量门禁（D-44，每个计划执行后、提交前必过）**：对改动文件运行 `eslint --fix`，并确保 `eslint <改动文件>` **0 错误**；对**本计划改动的测试文件**运行 `pnpm exec vue-tsc --noEmit`，并按输出文件路径过滤，确保这些文件路径下 **0 类型错误**（全局既有无关错误，如 `client-modules`/`legacy-plugin-data`，不在本门禁范围）；`pnpm test:ci` **全绿**。任一不通过则**不得提交**。
</execution_rules>

<tasks>

<task type="auto">
  <name>阶段 1（构件级）：G-06-01-C 同点多来源地图伤害的 separated/reduced 叠加断言</name>
  <files>packages-user/data-system/src/combat/mapDamage.test.ts</files>
  <read_first>
    packages-user/data-system/src/combat/mapDamage.ts（`addMapDamage`/`deleteMapDamage`/`getSeparatedDamage`/`getReducedDamage`/`useConverter`/`useReducer`/`markDirty`）
    packages-user/data-system/src/combat/mapDamage.test.ts（既有 `createInfo`/`FakeView`/`FakeConverter`/`FakeReducer`/`createFixture`）
    packages-user/data-system/src/combat/types.ts（`IMapDamageInfo` 的 damage/type/extra 字段；`IMapDamageReducer.reduce`）
    packages-user/data-state/src/enemy/mapDamage.ts（`MainMapDamageReducer` 的「求和 / 取最大 type / 并集 extra」参考语义，**只读参考、不导入**）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-01-C）
    .planning/phases/06-unit-tests/06-PATTERNS.md（A 节 mapDamage + 共享样板 4）
  </read_first>
  <action>
    在 `mapDamage.test.ts` 内联新增一个语义 reducer 与三类叠加用例，扩展既有夹具（`createFixture` 提供 `MapLocIndexer`、`FakeView`、`FakeConverter`、`FakeReducer`）。
    1) 新增内联 helper `createTypedInfo(damage, type, catchIds, repulseIds)`（返回 `IMapDamageInfo`，`extra.catch`/`extra.repulse` 为 `Set`）与 `createSemanticReducer()`（`reduce` 对 iterable 做 damage 求和、type 取最大、`extra.catch`/`extra.repulse` 并集，返回 `createTypedInfo(...)`）。
    2) 用例「有来源 + 有来源 同点叠加」：`useReducer(createSemanticReducer())` + `useConverter(new FakeConverter([new FakeView(7, [idx]), new FakeView(4, [idx])]))`（同一 `MapLocIndexer` 索引）；断言 `getSeparatedDamage(locator)` 长度 2、`getReducedDamage(locator).damage === 11`。
    3) 用例「无来源 + 无来源 同点叠加」：`useReducer` 后对同一 locator `addMapDamage(locator, createTypedInfo(3, ...))` 与 `addMapDamage(locator, createTypedInfo(5, ...))`（`FakeConverter([])`）；断言 separated 长度 2、reduced.damage === 8。
    4) 用例「多来源（>2，有来源与无来源混合）」：converter 产出两条 sourced（如 7 与 4，type 分别 0/2，extra 分别含不同 catch/repulse 标记），再 `addMapDamage` 两条 sourceless（如 3 与 2，type 1）；断言 separated 长度 4、reduced.damage === 16、reduced.type === 最大 type（2）、`reduced.extra.catch`/`extra.repulse` 为各来源并集（按标记集合显式断言）。
    5) 每个 `it` 前写一行中文注释；只经公开方法断言，不直接读内部 `ArrayBuffer`。若某组合在实现下出现与语义不符的结果，按 D-05 写正确预期 + `it.skip` 并在 `06-TEST-FINDINGS.md` 登记 `#06-10-N`，不得弱化为通过。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-system/src/combat/mapDamage.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 G-06-01-C 的三种叠加组合中任一缺少 `getSeparatedDamage` 条数断言或 `getReducedDamage` 的 damage/type/extra 断言，或现有 102/103/104 用例回归失败</fails_when>
  </verify>
  <acceptance_criteria>
    - `mapDamage.test.ts` 零非跳过失败。
    - 新增用例覆盖 G-06-01-C 三种组合（sourced+sourced、sourceless+sourceless、多来源混合），均断言 separated 条数与 reduced 的 damage/type/extra。
    - 新增 reducer/信息构造 helper 为文件内联，未建立跨文件 helper（D-03）。
    - 每个新增 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 1 的 G-06-01-C 覆盖全部跑绿后才进入阶段 2。若发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 2（组合/流水线）：G-06-01-A 四类效果同时生效的最终属性 + G-06-01-B 多怪跨施加嵌套光环</name>
  <files>packages-user/data-system/src/combat/context.test.ts</files>
  <read_first>
    packages-user/data-system/src/combat/context.ts（`registerAuraConverter`/`registerCommonQueryEffect`/`registerSpecialQueryEffect`/`registerFinalEffect`/`addAura`/`setEnemyAt`/`bindHero`/`buildup`/`getEnemyByLoc`）
    packages-user/data-system/src/combat/context.test.ts（既有 `createContextFixture`/`FakeAura`/`FakeConverter`/`createEnemy`/`createSpecial`，以及 L1046 四阶段用例、L895 两层嵌套用例）
    packages-user/data-system/src/combat/types.ts（`IEnemyCommonQueryEffect`/`IEnemySpecialQueryEffect`/`IEnemyFinalEffect`/`IAuraView`）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-01-A、G-06-01-B）
    .planning/phases/06-unit-tests/06-PATTERNS.md（A 节 context + 共享样板 4）
  </read_first>
  <action>
    在 `context.test.ts` 的 aura/阶段顺序分区新增两条用例，全部复用既有 fake 装配（`createContextFixture` + `FakeAura` + `FakeConverter`），不引用顶层真实实现（D-21）。
    1) G-06-01-A「四类效果同时生效」：`createContextFixture()`；`bindHero`；e1（`createEnemy('e1')`，基础 atk 2/def 0/hp 10）加 `createSpecial(20)` 并 `setEnemyAt({x:0,y:0}, e1)`；`registerAuraConverter(new FakeConverter([20], () => aura))`，其中 aura = `new FakeAura({ priority: 1, range: new modules.FullRange(), param: undefined, couldApplyBase: true, couldApplySpecial: true, onApply: h => { order.push('base'); h.enemy.addAttribute('atk', 5); }, onApplySpecial: () => { order.push('special'); return { add: () => [createSpecial(21)], delete: () => [], modify: () => false }; } })`；`registerCommonQueryEffect(20, { priority: 1, apply: h => { order.push('query'); h.enemy.addAttribute('def', 2); } })`；`registerSpecialQueryEffect({ priority: 1, for: () => ({ shouldQuery: h => h.enemy.hasSpecial(21), add: () => [], delete: () => [], modify: () => { order.push('special-query'); return true; } }) })`；`registerFinalEffect({ priority: 1, apply: h => { order.push('final'); h.enemy.addAttribute('hp', 4); } })`；`buildup()`。断言：`order` 以 `'special'` 开头且 `indexOf('special') < indexOf('base') < indexOf('query') < indexOf('final')`（`'special-query'` 允许落在 query 阶段内任意位置，用子序列断言，不硬编码其在 query 前后）；最终计算后怪物 `getAttribute('atk') === 7`、`getAttribute('def') === 2`、`getAttribute('hp') === 14`、`hasSpecial(21) === true`。
    2) G-06-01-B「多怪跨施加嵌套光环」：`createContextFixture(5, 3)`；`bindHero`；m1(0,0) atk 2 + `createSpecial(20)`，m2(1,0) atk 4，m3(4,0) atk 6，均 `setEnemyAt`；`registerAuraConverter(new FakeConverter([20, 21], code => code === 20 ? a1 : a2))`，其中 a1 = `new FakeAura({ priority: 10, range: new modules.RectRange(), param: { x: 1, y: 0, w: 1, h: 1 }, couldApplyBase: true, couldApplySpecial: true, onApply: h => h.enemy.addAttribute('atk', 100), onApplySpecial: () => ({ add: () => [createSpecial(21)], delete: () => [], modify: () => false }) })`（只覆盖 m2），a2 = `new FakeAura({ priority: 5, range: new modules.FullRange(), param: undefined, onApply: h => h.enemy.addAttribute('atk', 3) })`；`buildup()`。断言：`converter.convertCalls` 同时包含 20 与 21；m2 `hasSpecial(21) === true` 而 m1/m3 `hasSpecial(21) === false`（A1 范围边界可观测）；最终 atk `m1 === 5`、`m2 === 107`、`m3 === 9`（A1 只加成 m2，A2 加成三只）。
    两条用例前各写一行中文注释。若任一断言与实现不符，按 D-05 将该项改为正确预期的 `it.skip` 并在 `06-TEST-FINDINGS.md` 登记 `#06-10-N`（注明最小复现与疑似原因），不得弱化为通过。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-system/src/combat/context.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 G-06-01-A 未断言四阶段顺序/最终 atk/def/hp/special，或 G-06-01-B 未断言三只怪各自最终属性与 A1 范围边界，或既有 97–101/110/四阶段/嵌套用例回归失败</fails_when>
  </verify>
  <acceptance_criteria>
    - `context.test.ts` 零非跳过失败。
    - G-06-01-A：四类效果（光环基础 + 常规查询 + 特殊查询 + final）同时生效，四阶段顺序与最终属性均有显式断言。
    - G-06-01-B：m1→A1→m2→A2→m1/m2 的跨施加链与 m3 的 A1 范围外/A2 范围内均有断言，含三只怪最终 atk 与 special 差异。
    - 全部经文件内联 fake 驱动，未引用顶层真实实现（D-21）。
    - 每个新增 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 2 的 G-06-01-A/B 覆盖在阶段 1 跑绿的基础上全部跑绿后才进入阶段 3。若发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 3（完整/集成）：G-06-07-A 最大流水线组合单怪的最终 {damage,turn}</name>
  <files>packages-user/data-state/test/enemyCombination.test.ts, .planning/phases/06-unit-tests/06-COVERAGE-MAP.md</files>
  <read_first>
    packages-user/data-state/test/enemyCombination.test.ts（既有 `createCoreState`/`createContext`/`createEnemy`/`createHero`/`createContextHandler`/`createSpecial`/`createOrderAura`/`createSpecialAura`）
    packages-user/data-state/src/enemy/calculator.ts（各 special 分支：先攻/魔攻/连击/多段/破甲/反击/吸血/固伤/仇恨/坚固/模仿/支援递归）
    packages-user/data-state/src/enemy/final.ts（`MainEnemyFinalEffect`）
    packages-user/data-state/src/enemy/calculator.test.ts（各单分支的确定性数值参考）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-07-A）
    packages/common/src/logger.json
  </read_first>
  <action>
    在 `enemyCombination.test.ts` 新增阶段 3（完整/集成）用例：在 `createCoreState()` + 真实 `createContext(state, hero)`（真实 `CommonAuraConverter`/`GuardAuraConverter`/`MainEnemyFinalEffect`）之上构造一只「尽可能多流水线同时生效」的怪，然后经真实 `MainDamageCalculator` 断言唯一精确的最终 `{damage, turn}`。
    组合内容（全部同时生效）：怪物自带的顶层特殊属性（至少 先攻 1、魔攻 2、2连击 4、多段 6、破甲 7、反击 8、吸血 11、固伤 22、仇恨 17、坚固 3）、真实光环 25（`createSpecial<IHaloValue>(25, { haloRange: 0, haloSquare: false, hpBuff, atkBuff, defBuff })`）、真实支援 26（`createSpecial<void>(26, undefined)` 并由相邻支援怪提供 `guard` 坐标）、`context.registerCommonQueryEffect(25, ...)`、`context.registerSpecialQueryEffect(...)`、`context.registerFinalEffect({ priority, apply })`（自定义最终效果）；固定 `hero = createHero({ atk: 20, def: 5, hp: 100 })`；`context.setEnemyAt` 放置主怪与支援怪；`context.buildup()` 后取 `targetView.getComputedEnemy()`，以 `createContextHandler(context, computed, hero, locator)` 交给 `new MainDamageCalculator()`。
    断言：结果为单一对象 `{ damage, turn }`（`expect(info).toEqual({ damage: D, turn: T })`，`D`/`T` 为按各分支语义推导并在注释中写出推导过程的确定值；同时 `expect(Object.keys(info!).sort()).toEqual(['damage','turn'])` 证明确无额外字段）。数值须由实现者在注释中给出逐项推导（基础伤害 + 各 special 加成 + 光环改后属性 + 支援递归 + 自定义 final），不得直接抄运行结果而不写推导。可再补一条对照断言：去掉支援怪（或某一效果）后 `{damage,turn}` 与最大组合不同，证明组合确实同时生效。
    写一行中文注释说明覆盖内容。若计算器某分支与推导不符，按 D-05 写正确预期 + `it.skip` 并登记 `06-TEST-FINDINGS.md` 的 `#06-10-N`，不得弱化。
    完成后以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 的 06-10 小节：本计划**不引入新码**，因此先确认表头存在（不存在则创建），再追加 `## 06-10 战斗系统 / 顶层组合缺口补测` 小节，列出 G-06-01-A/B/C、G-06-07-A → 用例名 → 计划 06-10 的映射，并注明「本计划无新增码，复用既有可达码」；只追加/更新本小节，绝不重写他节。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-state/test/enemyCombination.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或最大组合用例未断言单一 `{damage,turn}` 精确值，或结果对象含 damage/turn 以外字段，或对照用例未证明组合生效，或 06-10 小节未写入 `06-COVERAGE-MAP.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `enemyCombination.test.ts` 零非跳过失败。
    - G-06-07-A：最大组合怪经真实 `MainDamageCalculator` 得到唯一精确 `{damage,turn}`，且注释含逐项推导。
    - 存在一条对照断言（去掉某效果/支援后结果不同），证明多流水线同时生效。
    - `06-COVERAGE-MAP.md` 存在 06-10 小节（create-or-append，未重写他节），列出 G-06-01-A/B/C、G-06-07-A 的用例映射。
    - 每个新增 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 3 的 G-06-07-A 覆盖在阶段 1/2 跑绿的基础上全部跑绿，且 06-10 小节已写入 `06-COVERAGE-MAP.md`。完成后暂停并用中文分条汇报；详细结果写入 `06-TEST-FINDINGS.md`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | 仅新增/扩展仓库内 Node Vitest 测试；无运行时输入面、网络、DOM 或文件写入。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10-01 | Tampering | `data-system/src/combat` 与 `data-state/test` 下测试文件 | low | mitigate | 只导入已跟踪模块、合成 fixture；不改核心源码；门禁 `pnpm test:ci` + 复核仅 `*.test.ts` 变更。 |
| T-06-10-02 | Information disclosure | 合成 fixture | low | accept | 合成属性/录像/地图，无真实数据或密钥（D-02）。 |
| T-06-10-03 | Repudiation | 全局 stub 泄漏 | low | accept | 既有文件已在 `afterAll`/`vi.hoisted` 管理 stub；新增用例不引入新 stub。 |
| T-06-10-SC | Tampering | 包安装 | high | mitigate | 明确不引入任何新依赖；若确需安装，停止并先走包合法性 checkpoint。 |
</threat_model>

<verification>
- 三个测试文件聚焦运行通过；`pnpm test:ci` 全量绿。
- `git status` 仅显示三个 `*.test.ts` 与 `06-COVERAGE-MAP.md` 的变更；无核心文件改动。
- 任何 `it.skip`/`it.todo` 均有 `#06-10-N` 条目（预期无需新增）。
- 以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-10 小节；表头不存在则先创建；幂等、顺序无关，不重写他节。
- **执行协议**：执行前必须先向用户确认；执行后暂停并用中文分条汇报。
</verification>

<success_criteria>
- G-06-01-A、G-06-01-B、G-06-01-C、G-06-07-A 均有可跑绿的新增用例。
- 四类效果同时生效的最终属性、多怪嵌套光环的三只怪最终属性、同点多来源地图伤害的 separated/reduced、最大组合单怪的 `{damage,turn}` 均被显式断言。
- 无生产代码改动，`pnpm test:ci` 全绿。
</success_criteria>

<artifacts_this_plan_produces>
Test files (extended):
- `packages-user/data-system/src/combat/mapDamage.test.ts`（新增 `createTypedInfo`、`createSemanticReducer` 与 3 条叠加用例）
- `packages-user/data-system/src/combat/context.test.ts`（新增 G-06-01-A 四类效果用例、G-06-01-B 多怪嵌套光环用例）
- `packages-user/data-state/test/enemyCombination.test.ts`（新增最大流水线组合用例）

Planning artifacts:
- `06-COVERAGE-MAP.md` create-or-append 06-10 小节
</artifacts_this_plan_produces>

<output>
完成时创建 `.planning/phases/06-unit-tests/06-10-SUMMARY.md`，以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-10 小节；疑似 bug 追加到 `06-TEST-FINDINGS.md` 的 `#06-10-N` 小节（若有）。执行完成后暂停并用中文分条汇报。
</output>
