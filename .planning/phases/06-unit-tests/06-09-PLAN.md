---
phase: 06-unit-tests
plan: 09
type: execute
wave: 2
depends_on: [06-03, 06-05, 06-06, 06-08]
files_modified:
  - packages-user/data-base/src/enemy/saveLoad.test.ts
  - packages-user/data-base/src/hero/saveLoad.test.ts
  - packages-user/data-base/src/map/saveLoad.test.ts
  - packages-user/data-common/src/replay/saveLoad.test.ts
  - packages-user/data-base/src/flag/saveLoad.test.ts
  - packages-user/data-state/test/saveablesRoundTrip.test.ts
  - .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
autonomous: false
requirements: [TEST-01]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "TEST-01 由数据端行为单测满足，门禁为固定非 watch 命令 `pnpm test:ci` 全绿（D-08）。"
    - "存读档集中：本计划是唯一测试 `saveState`/`loadState` 的计划；其它计划一律不测（D-32 / D-42）。"
    - "接口全覆盖：所有覆盖系统内含 `saveState`/`loadState` 的类（Enemy/special、EnemyManager、HeroAttribute/modifier、HeroLocation、HeroState、HeroEquipment、EquipmentState/HeroEquipsStore、HeroItems、HeroFollower、HeroRendering、MapState、GameMap、MapLayer、StaticTile、DynamicTile、ReplayArray、ReplaySystem、FlagSystem）的这两个方法至少一条正常用例（`ReplaySystem.saveState()`/`loadState(state)` 承载其已注册指令/route/录像状态，须有同实例往返正常用例）；`CoreState` 的公开 `saveState`/`loadState` 亦经顶层入口覆盖（D-45）；名称含 legacy 的接口/方法排除（D-30 / D-42）。"
    - "code 全覆盖：本计划可达的码 55（MapStore load 缺 reference）、58（equipment store load 找不到 maxUid）、59（equipment load 找不到 item raw data）、112（重复 saveable id）、113（bindSaveableExecuter 目标未添加）、119（loadState 缺 prefab）、120（loadState 缺 special）、122（MapStore load 楼层缺失）、124（MapLayer load 引用缺失）、177（CoreState.loadState 存档缺 saveable key）、178（CoreState.loadState 存档含未加载的 key）各至少一条触发断言（D-31 / D-42 / D-45）。"
    - "往返形式为**同实例恢复**：对可存档对象 `saveState` → 继续修改状态 → `loadState` → 关键字段回到存档点（D-12）。"
    - "压缩档全覆盖：`NoCompression`/`LowCompression`/`HighCompression` 三档（按各 `saveState`/`loadState` 签名适用者）（D-11 / D-42）。"
    - "「状态相同」只用**关键字段显式断言**，不比较派生字段/缓存/序列化容器结构，也不做整体深度相等（D-13）。"
    - "推导并写出**排除清单**（存档无关/元数据字段，如最后存档时间、dirty、缓存），落到 `06-SAVE-EXCLUSIONS.md`（D-42）。"
    - "CoreState 顶层验证（D-45）：主路径经**公开入口** `CoreState.saveState(compression)` / `loadState(state, compression)` 对 5 个已注册 id（`@system/hero`/`flags`/`maps`/`enemy`/`replay`）做整体往返（D-11），并以 `getSaveableContent(id)` 作为辅助逐 id 校验；同时断言守卫码 112/113 与 `loadState` 新增码 177/178。"
    - "执行前必须先向用户确认（D-42 / D-45）：公开 `saveState`/`loadState` 入口已就绪，且 5 个 saveable（含 `@system/replay`）均已注册；若 `@system/replay` 未注册即为阻断，须记录并暂停，不得静默通过。"
    - "测试形态（D-01）：本计划经 `CoreState` 集成路径（公开 `saveState`/`loadState`，辅以 `getSaveableContent`）与各类同实例往返验证存读档，属混合测试形态的跨系统集成一侧。"
    - "疑似缺陷按正确预期编写并标记 `it.skip`/`it.todo`，并按 D-06 的格式登记到共用的 `06-TEST-FINDINGS.md`（模块/接口、现象、最小复现、疑似原因、影响面、建议修复方向、关联 skip 用例、严重度）。"
    - "执行节奏（D-33）：本计划执行前先向用户确认；执行完成后暂停，用中文分条简要汇报验证结果，详细结果写入共用的 `06-TEST-FINDINGS.md`。"
    - "阶段化（D-43）：计划按 构件→组合→完整 三阶段组织，每阶段跑绿后进入下一阶段；阻断性 bug 暂停等确认。"
    - "质量门禁（D-44）：每个计划执行后、提交前，改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误（全局既有无关错误不在范围）；`pnpm test:ci` 全绿；任一不通过不得提交。"
  artifacts:
    - path: packages-user/data-base/src/enemy/saveLoad.test.ts
      provides: Enemy/special/EnemyManager 存读档覆盖（119/120）
    - path: packages-user/data-base/src/hero/saveLoad.test.ts
      provides: hero 各子系统存读档覆盖（58/59）
    - path: packages-user/data-base/src/map/saveLoad.test.ts
      provides: 地图各层/tile 存读档覆盖（55/122/124）
    - path: packages-user/data-common/src/replay/saveLoad.test.ts
      provides: ReplayArray 与 ReplaySystem 存读档覆盖
    - path: packages-user/data-base/src/flag/saveLoad.test.ts
      provides: FlagSystem 存读档覆盖
    - path: packages-user/data-state/test/saveablesRoundTrip.test.ts
      provides: CoreState 顶层 5 saveables × 3 压缩档经公开 `saveState`/`loadState` 往返（112/113/177/178）
    - path: .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
      provides: 存读档排除清单（存档无关/元数据字段）
  key_links:
    - "saveablesRoundTrip.test.ts 以**公开入口** `CoreState.saveState(compression)` / `loadState(state, compression)`（`data-state/src/core.ts:509-538`，D-45）为主路径，对应 `core.ts:237-241` 注册的 5 个 id（`@system/hero`/`flags`/`maps`/`enemy`/`replay`）；并以 `getSaveableContent(id)`（`core.ts:487-490`）作辅助逐 id 校验；不接触 `saveSystem.init`/IndexedDB（D-10）"
    - "各 saveLoad.test.ts 用 inline 合成对象/小地图/小录像数组；三档压缩循环参照 `data-base/src/map/mapLifecycle.test.ts:203-234` 的显式关键字段形状"
    - "`06-COVERAGE-MAP.md` 为共享 create-or-append 文件：写 06-09 小节前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本小节；幂等且与其它计划顺序无关，不重写他节"
  prohibitions:
    - "不创建或修改任何生产/核心源码，只新增 `*.test.ts` 与规划产物"
    - "不安装覆盖率工具（D-09）"
    - "不引入 `fake-indexeddb`，不做 IndexedDB 端到端存档（D-10）"
    - "疑似 bug 按正确预期编写并标记 `it.skip`/`it.todo`（D-05）"
    - "fixture 全部文件内联，不建跨文件 helper（D-02/D-03）"
    - "不使用 fake timers（D-04）"
    - "不做整体深度相等；不比较派生/缓存/序列化容器结构（D-13）"
---

<objective>
存读档独立系统（D-42 / D-45）：覆盖所有含 `saveState`/`loadState` 的类逐类同实例往返 + `CoreState` 顶层验证；仅断言关键状态字段，排除存档无关/元数据字段；压缩档全覆盖。按 D-43 的阶段化测试组织：阶段 1 构件级（enemy + hero 各类同实例往返）→ 阶段 2 组合/流水线（map + replay + flag 各类同实例往返）→ 阶段 3 完整/集成（经公开 `CoreState.saveState`/`loadState` 对 5 个 saveable 往返）。

**执行前必须先向用户确认**（D-42 / D-45：公开 `saveState`/`loadState` 入口已就绪，且 5 个 saveable 均已注册，含 `@system/replay`；`@system/replay` 未注册即为阻断）。

Output: 6 个 `*.test.ts` 与 `06-SAVE-EXCLUSIONS.md`。

阶段结构（D-43）：每阶段跑绿后再进入下一阶段；阶段 3 为经公开入口的 CoreState 顶层验证。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-unit-tests/06-CONTEXT.md
@.planning/phases/06-unit-tests/06-PATTERNS.md
@.planning/codebase/TESTING.md
@dev.md
@packages-user/data-common/src/save/types.ts
@packages-user/data-state/src/core.ts
@packages-user/data-state/test/dataClosure.test.ts
@packages-user/data-state/test/fixtures/closed-loop.ts
@packages-user/data-base/src/map/mapLifecycle.test.ts
@packages-user/data-base/src/enemy/enemy.ts
@packages-user/data-base/src/enemy/manager.ts
@packages-user/data-base/src/hero/equipStore.ts
@packages-user/data-base/src/hero/state.ts
@packages-user/data-base/src/map/mapState.ts
@packages-user/data-base/src/map/mapLayer.ts
@packages-user/data-common/src/replay/array.ts
@packages-user/data-common/src/replay/system.ts
</context>

<execution_rules>
- 只写测试，绝不修改任何生产文件。
- 每个 `it(...)` 前必须有一行中文注释（dev.md）。
- fixture 内联且最小（D-02/D-03）；用 `SaveCompression` 三档。
- 警告/错误路径经 `logger.catch` / `vi.spyOn(logger, ...)`。
- 疑似 bug：正确预期 + `it.skip`/`it.todo` + 中文注释锚定 `#06-09-N`；`pnpm test:ci` 保持全绿。
- **执行前必须先向用户确认（D-42 / D-45）公开 `CoreState.saveState`/`loadState` 入口与 5 个已注册 saveable（含 `@system/replay`）均已就绪；未确认前不得开始；`@system/replay` 未注册即阻断并记录，不得静默通过。**
- `06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写 06-09 小节前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本小节，绝不重写他节。
- **执行协议（D-33）：执行前向用户确认；执行后暂停并用中文分条汇报。**
- **阶段门禁（D-43）：本计划按 构件 → 组合/流水线 → 完整/集成 三阶段顺序执行，每阶段跑绿后才进入下一阶段；若某阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。**
- **质量门禁（D-44，每个计划执行后、提交前必过）**：对改动文件运行 `eslint --fix`，并确保 `eslint <改动文件>` **0 错误**；对**本计划改动的测试文件**运行 `pnpm exec vue-tsc --noEmit`，并按输出文件路径过滤，确保这些文件路径下 **0 类型错误**（全局既有无关错误，如 `client-modules`/`legacy-plugin-data`，不在本门禁范围）；`pnpm test:ci` **全绿**。任一不通过则**不得提交**（vitest 用 esbuild 剥类型，类型/格式错误不会让测试失败，故必须显式门禁）。
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 执行前门禁 — 用户确认公开 save/load 入口与 5 个 saveable（含 @system/replay）已就绪</name>
  <files>packages-user/data-state/test/saveablesRoundTrip.test.ts（尚未创建，仅作占位说明）</files>
  <what-built>CoreState 公开存档入口 `saveState(compression)` / `loadState(state, compression)`（D-45），以及 5 个已注册 saveable：`@system/hero`/`@system/flags`/`@system/maps`/`@system/enemy`/`@system/replay`。</what-built>
  <how-to-verify>
    1) 确认公开入口 `CoreState.saveState(compression)` / `loadState(state, compression)` 已就绪（`ICoreState extends IStateSystem, ISaveableContent<ReadonlyMap<string, unknown>>`）。
    2) 确认 5 个 saveable 均已注册：`@system/hero`/`@system/flags`/`@system/maps`/`@system/enemy` + **`@system/replay`**（核对 `core.ts` 的 `addSaveableContent` 调用）。
    3) 若 `@system/replay` 未注册，则本计划在阶段 3 前阻断：记录到 `06-TEST-FINDINGS.md` 并暂停等待用户补注册，不得静默通过。
  </how-to-verify>
  <resume-signal>用户回复「存读档入口已就绪，可以开始」则继续 Task 2/3；否则暂停本计划等待用户改代码。</resume-signal>
  <read_first>
    .planning/phases/06-unit-tests/06-CONTEXT.md（D-42 / D-45）
    packages-user/data-state/src/core.ts（saveables 注册：`core.ts:237-241`；公开 `saveState`/`loadState`：`core.ts:509-538`）
    packages-user/data-state/src/types.ts（`ICoreState extends IStateSystem, ISaveableContent<ReadonlyMap<string, unknown>>`）
    packages-user/data-common/src/save/types.ts（`ISaveableContent` / `SaveCompression` 契约）
    packages/common/src/logger.json（112/113/177/178）
  </read_first>
  <action>
    本计划依赖已落地的公开存档入口（D-45）。开始前向用户展示并确认：
    - 公开入口 `CoreState.saveState(compression)` / `loadState(state, compression)` 是否已就绪；
    - 5 个 saveable（`@system/hero`/`@system/flags`/`@system/maps`/`@system/enemy` + `@system/replay`）是否均已注册（核对 `core.ts:237-241` 的 `addSaveableContent` 调用）；
    - **`@system/replay` 注册状态必须显式确认**：若未注册，则记录为阻断（写入 `06-TEST-FINDINGS.md`）并按 D-05/D-07 暂停等待用户补注册，不得以仅 4 个 id 静默通过。
    用户确认后再继续 Task 2/3；若未就绪，暂停本计划等待用户改代码，不得自行修改核心代码。
  </action>
  <verify>
    <human-check>用户明确回复「存读档入口已就绪，可以开始」，或不通过则暂停本计划</human-check>
  </verify>
  <acceptance_criteria>
    - 用户给出明确开始确认；否则本计划保持暂停状态。
    - `@system/replay` 注册状态已显式确认；未注册即记录为阻断，不得静默通过。
  </acceptance_criteria>
  <done>公开 save/load 入口与 5 个 saveable（含 @system/replay）就绪得到用户确认，或计划暂停等待用户改代码。</done>
</task>

<task type="auto">
  <name>阶段 1（构件级）：enemy + hero 各类同实例存读档往返（含压缩档与码 119/120/58/59）</name>
  <files>packages-user/data-base/src/enemy/saveLoad.test.ts, packages-user/data-base/src/hero/saveLoad.test.ts, .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md</files>
  <read_first>
    packages-user/data-base/src/enemy/enemy.ts
    packages-user/data-base/src/enemy/manager.ts
    packages-user/data-base/src/enemy/special.ts
    packages-user/data-base/src/hero/attribute.ts
    packages-user/data-base/src/hero/modifier.ts
    packages-user/data-base/src/hero/location.ts
    packages-user/data-base/src/hero/state.ts
    packages-user/data-base/src/hero/equipment.ts
    packages-user/data-base/src/hero/equipStore.ts
    packages-user/data-base/src/hero/items.ts
    packages-user/data-base/src/hero/follower.ts
    packages-user/data-base/src/hero/rendering.ts
    .planning/phases/06-unit-tests/06-PATTERNS.md（C/E 节）
  </read_first>
  <action>
    D-43 阶段 1（构件级）：只测单个可存档类的同实例往返，不做跨系统顶层集成。
    `enemy/saveLoad.test.ts`：对 `Enemy`、`CommonSerializableSpecial`/`NonePropertySpecial`、`EnemyManager` 分别同实例往返（三档压缩）：
    - Enemy：属性关键字段 + special 关键值；`loadState` 时实例上存在但存档缺失的 special 经 `logger.catch` 观测码 120；`saveState` 返回分离克隆（改返回对象不影响实例）。
    - EnemyManager：`saveState` 只序列化脏 code；`loadState` 缺 prefab 经 `logger.catch` 观测码 119。
    - 只断言关键状态字段（D-13）。

    `hero/saveLoad.test.ts`：对 hero 各子系统同实例往返（三档压缩）：
    - `HeroAttribute`/`ValueModifier`/`PercentageModifier`：base/值关键字段恢复；modifier 存盘开关生效。
    - `HeroLocation`：x/y/floorId/direction 四字段恢复。
    - `HeroState`：attribute base 值、location、modifier 值关键字段恢复；save 禁用 modifier 不持久化。
    - `HeroEquipment`/`EquipmentState`/`HeroEquipsStore`/`HeroItems`/`HeroFollower`/`HeroRendering`：各自关键字段恢复；equipStore `loadState` 缺 item raw 观测码 59、空 equipments 观测码 58（`logger.catch`）。
    - 只断言关键字段（D-13）。

    `06-SAVE-EXCLUSIONS.md`：逐类推导并写出**排除清单**——列出**不**在断言范围内的存档无关/元数据字段（如最后存档时间、dirty/脏标记、缓存/派生字段、序列化容器结构、副本容量等），并注明依据（为什么与存档语义无关）。此文档作为 06-09 与后续维护的对照基准。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/enemy/saveLoad.test.ts packages-user/data-base/src/hero/saveLoad.test.ts</automated>
    <fails_when>运行失败，或 119/120/58/59 中任一码未被观测，或某类缺少同实例往返断言</fails_when>
  </verify>
  <acceptance_criteria>
    - 两个文件零非跳过失败。
    - enemy/hero 每个含存读档的类至少一条同实例往返；断言码 119/120/58/59。
    - 只用关键字段显式断言，无整体深度相等。
    - `06-SAVE-EXCLUSIONS.md` 存在并逐类列出排除字段与依据。
    - 每个 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 1（构件级）的 enemy/hero 存读档往返与排除清单覆盖全部跑绿后才进入阶段 2。若本阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认；不得跳过或弱化。</done>
</task>

<task type="auto">
  <name>阶段 2（组合/流水线）：map + replay + flag 各类同实例往返（replay 含 ReplaySystem/ReplayArray；码 55/122/124）</name>
  <files>packages-user/data-base/src/map/saveLoad.test.ts, packages-user/data-common/src/replay/saveLoad.test.ts, packages-user/data-base/src/flag/saveLoad.test.ts</files>
  <read_first>
    packages-user/data-base/src/map/mapState.ts
    packages-user/data-base/src/map/gameMap.ts
    packages-user/data-base/src/map/mapLayer.ts
    packages-user/data-base/src/map/staticTile.ts
    packages-user/data-base/src/map/dynamicTile.ts
    packages-user/data-common/src/replay/array.ts
    packages-user/data-common/src/replay/system.ts
    packages-user/data-base/src/flag/system.ts
    packages-user/data-base/src/map/mapLifecycle.test.ts
    .planning/phases/06-unit-tests/06-PATTERNS.md（D/E/F 节）
  </read_first>
  <action>
    D-43 阶段 2（组合/流水线）：在阶段 1 跑绿后，测地图/录像/flag 各系统的存读档往返（系统内多构件组合）。
    `map/saveLoad.test.ts`：对 `MapState`、`GameMap`、`MapLayer`、`StaticTile`、`DynamicTile` 同实例往返（三档压缩）：
    - MapState：`loadState` 在非 `NoCompression` 且未 `compareWith` 时观测码 55；缺楼层观测码 122。
    - MapLayer：关键块/点位事件恢复；引用缺失观测码 124；三档压缩循环参照 `mapLifecycle.test.ts` 的显式关键字段。
    - StaticTile/DynamicTile：关键 num 与覆盖事件恢复。
    - 只断言关键字段（D-13）。

    `replay/saveLoad.test.ts`：覆盖 `ReplaySystem` 与 `ReplayArray` 两者的同实例往返（不新增码）：
    - `ReplaySystem.saveState()`/`loadState(state)`（D-42：其实现 `ISaveableContent`，见 `data-common/src/replay/system.ts`）同实例往返：先 `registerCommand` 注册指令并 `record` 若干录像步 → `saveState()` → 继续 `record` 新步/改动 `replaying`、`sandbox` 等 → `loadState(snapshot)` → **显式关键字段断言**——录像步数（`length`）、`commandWidth`、`getCommandArray()`/`getParamArray()` 关键命令与参数（含 boolean/整数/bigint/string/数组）读回一致；并断言已注册指令经 `getCommand(code)` 仍可取回、`route` 状态回到存档点。不比较内部容量/元数据（D-13）。
    - `ReplayArray.saveState`/`loadState` 同实例往返：命令与参数（含 boolean/整数/bigint/string/数组）读回一致；不比较内部容量/元数据。

    `flag/saveLoad.test.ts`：`FlagSystem.saveState()`（无压缩参数）/`loadState` 同实例往返：已知字段值 + `occupied` 恢复；`saveState` 返回分离克隆。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/map/saveLoad.test.ts packages-user/data-common/src/replay/saveLoad.test.ts packages-user/data-base/src/flag/saveLoad.test.ts</automated>
    <fails_when>运行失败，或 55/122/124 中任一码未被观测，或某类缺少同实例往返断言，或 `ReplaySystem`/`ReplayArray` 任一缺少同实例往返关键字段断言</fails_when>
  </verify>
  <acceptance_criteria>
    - 三个文件零非跳过失败。
    - map/replay/flag 各类同实例往返（replay 含 `ReplaySystem` 与 `ReplayArray` 两者）；断言码 55/122/124。
    - 只用关键字段显式断言，无整体深度相等。
    - 每个 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 2（组合/流水线）的地图/录像/flag 存读档往返在阶段 1 跑绿的基础上全部跑绿后才进入阶段 3。若本阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认；不得跳过或弱化。</done>
</task>

<task type="auto">
  <name>阶段 3（完整/集成）：CoreState 顶层 5 saveables × 3 压缩档经公开 saveState/loadState 往返（码 112/113/177/178）</name>
  <files>packages-user/data-state/test/saveablesRoundTrip.test.ts</files>
  <read_first>
    packages-user/data-state/src/core.ts
    packages-user/data-state/test/fixtures/closed-loop.ts
    packages-user/data-state/test/dataClosure.test.ts
    packages-user/data-common/src/save/types.ts
    .planning/phases/06-unit-tests/06-PATTERNS.md（D/E/F 节）
  </read_first>
  <action>
    D-43 阶段 3（完整/集成）：在阶段 1/2 跑绿后，测 `CoreState` 顶层完整验证。
    `saveablesRoundTrip.test.ts`：**主路径经公开入口** `CoreState.saveState(compression)` / `loadState(state, compression)`（D-45）：
    - 对 5 个 id（`@system/hero`/`flags`/`maps`/`enemy`/`replay`）× 3 档 `SaveCompression`：设已知关键字段 → `core.saveState(compression)` → 改字段 → `core.loadState(snapshot, compression)` → 只断言关键字段（hero base 属性、flag 字段、map 层块/点位事件、enemy prefab 属性、replay 命令与参数）。不做整体深度相等（D-13）。
    - 断言快照形状：`saveState` 返回的映射以 5 个已注册 id 为键，且与活对象分离（save 后改活对象不影响快照关键字段）。
    - 辅助逐 id 校验：`getSaveableContent(id)` 对上述 5 个 id 各返回非 null，且其 `saveState`/`loadState` 与顶层入口结果一致。
    - `loadState` 新增码：构造缺某 saveable key 的快照触发码 177（经 `logger.catch`）；构造含未加载 key 的快照触发码 178。
    - 未知 id 返回 null。
    - 守卫：重复 `addSaveableContent` 经 `logger.catch` 观测码 112；对未添加对象 `bindSaveableExecuter` 观测码 113。
    - Node-safe `createCoreState()`；不 `saveSystem.init`、不加 `fake-indexeddb`（D-10）。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-state/test/saveablesRoundTrip.test.ts</automated>
    <fails_when>运行失败，或 112/113/177/178 中任一码未被观测，或 5×3 往返矩阵缺失某组合，或顶层往返未经公开 `saveState`/`loadState` 驱动</fails_when>
  </verify>
  <acceptance_criteria>
    - `saveablesRoundTrip.test.ts` 零非跳过失败。
    - 主路径经公开 `CoreState.saveState`/`loadState` 覆盖 5 id × 3 压缩档；`getSaveableContent(id)` 作辅助逐 id 校验。
    - 显式关键字段 + 快照分离 + 未知 id null + 码 112/113/177/178。
    - 每个 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 3（完整/集成）的 CoreState 顶层 saveables 往返在阶段 1/2 跑绿的基础上全部跑绿。若本阶段发现阻断性 bug（含 `@system/replay` 未注册），暂停并按 D-05/D-07 向用户汇报、等确认；不得跳过或弱化。完成后以 create-or-append 方式将本计划的 code→模块→用例行（55/58/59/112/113/119/120/122/124/177/178）写入 `06-COVERAGE-MAP.md` 的 06-09 小节：先检查表头，不存在则先创建，再只追加本小节，不重写他节。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | 仅新增仓库内 Node Vitest 测试；无运行时输入面、网络、DOM 或文件写入（不碰 IndexedDB）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09-01 | Tampering | `data-base`/`data-common`/`data-state/test` 下测试文件 | low | mitigate | 只导入已跟踪模块、合成快照；不安装包、不改核心源码；门禁 `pnpm test:ci` + 复核仅 `*.test.ts` 变更。 |
| T-06-09-02 | Information disclosure | 快照 fixture | low | accept | 合成状态，无真实存档文件或密钥（D-02）。 |
| T-06-09-03 | Repudiation | 全局 stub 泄漏 | low | accept | 任何 `vi.stubGlobal` 在 `afterAll` 拆除；用例独立。 |
| T-06-09-SC | Tampering | 包安装（尤其 `fake-indexeddb`） | high | mitigate | 明确不引入 `fake-indexeddb` 或任何新依赖；若确需安装，停止并先走包合法性 checkpoint。 |
</threat_model>

<verification>
- 六个测试文件聚焦运行通过；`pnpm test:ci` 全量绿。
- `git status` 仅新增 `*.test.ts` 与 `06-SAVE-EXCLUSIONS.md`；无核心文件改动。
- 任何 `it.skip`/`it.todo` 均有 `#06-09-N` 条目。
- 以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-09 小节（11 个码：55/58/59/112/113/119/120/122/124/177/178）；表头不存在则先创建；幂等、顺序无关，不重写他节。
- **执行协议**：执行前必须先向用户确认公开 save/load 入口与 5 个 saveable（含 `@system/replay`）就绪；执行后暂停并用中文分条汇报。
</verification>

<success_criteria>
- 6 个存读档测试文件存在并通过（在用户确认公开入口与 5 个 saveable 就绪之后）。
- 每个含 `saveState`/`loadState` 的类至少一条同实例往返；5 saveable × 3 压缩档经公开 `CoreState.saveState`/`loadState` 覆盖。
- 排除清单 `06-SAVE-EXCLUSIONS.md` 落盘。
- 11 个可达码全部触发断言。
</success_criteria>

<artifacts_this_plan_produces>
Test files:
- `packages-user/data-base/src/enemy/saveLoad.test.ts`
- `packages-user/data-base/src/hero/saveLoad.test.ts`
- `packages-user/data-base/src/map/saveLoad.test.ts`
- `packages-user/data-common/src/replay/saveLoad.test.ts`
- `packages-user/data-base/src/flag/saveLoad.test.ts`
- `packages-user/data-state/test/saveablesRoundTrip.test.ts`

Inline helper symbols:
- `enemy/saveLoad.test.ts`: `createEnemy`、`SAVE_COMPRESSIONS`
- `hero/saveLoad.test.ts`: `createState`、`createHeroState`、`SAVE_COMPRESSIONS`
- `map/saveLoad.test.ts`: `createMapState`、`SAVE_COMPRESSIONS`
- `replay/saveLoad.test.ts`: `createArray`、`createRoute`、`createSystem`
- `flag/saveLoad.test.ts`: `createFlags`
- `saveablesRoundTrip.test.ts`: `SAVEABLE_IDS`、`COMPRESSIONS`、`mutateKeyField`

Planning artifacts:
- `.planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md`
- `06-COVERAGE-MAP.md` create-or-append 06-09 小节
</artifacts_this_plan_produces>

<output>
完成时创建 `.planning/phases/06-unit-tests/06-09-SUMMARY.md`，以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-09 小节；疑似 bug 追加到 `06-TEST-FINDINGS.md` 的 `#06-09-N` 小节。执行完成后暂停并用中文分条汇报。
</output>
