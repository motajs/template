---
phase: 06-unit-tests
plan: 13
type: execute
wave: 3
depends_on: [06-03, 06-05, 06-06, 06-09]
files_modified:
  - packages-user/data-base/src/hero/saveLoad.test.ts
  - packages-user/data-base/src/map/saveLoad.test.ts
  - packages-user/data-state/test/saveablesRoundTrip.test.ts
  - .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TEST-01 由数据端行为单测满足，门禁为固定非 watch 命令 `pnpm test:ci` 全绿（D-08）。"
    - "G-06-09-B：所有含 `saveState`/`loadState` 的类在 `NoCompression`/`LowCompression`/`HighCompression` 三档下各验证一次往返；接受 compression 参数的类（`EquipmentState`/`HeroItems`/`HeroState`/`StaticTile`/`DynamicTile`）各自三档循环；不接受 compression 参数的类（`HeroLocation`/`HeroRendering`/`HeroEquipment`/`FlagSystem`/`ReplaySystem`）经**容器三档**（`HeroState` 与 `CoreState`）确认读取正确。"
    - "G-06-09-A：`CoreState` 顶层往返对每类 saveable 的**全部关键状态**逐一严格断言（勇士属性/位置/修饰器、flags 多字段、地图多块/激活状态、enemy 多属性、录像 ≥10 步多样化命令），必须严格一致（D-13 的关键字段显式断言）。"
    - "录像内容要求：至少 10 步、命令不全相同、多种组合（Up/Right/Down/Left/Teleport/UseItem/Equip/Unequip 混合且参数类型多样），读档后逐条 exact 断言命令与参数。"
    - "失败/受阻塞分支按 D-05 处理：既有 `#06-09-1`/`#06-09-2`/`#06-09-3`/`#06-09-5` 的 `it.skip` 保持不变；若三档扩展在某档命中已知缺陷，则写正确预期 + `it.skip` 并锚定既有 finding 条目，绝不弱化为通过；`pnpm test:ci` 保持全绿。"
    - "接口覆盖：只经既有公开接口驱动（各 `saveState`/`loadState`、`CoreState.saveState`/`loadState`、`getSaveableContent`）；名称含 legacy 的接口/方法排除（D-30）。"
    - "code 覆盖（模块可达）：本计划不引入新码；复用并再确认 06-09 既有可达码（55/58/59/112/113/119/120/122/124/177/178）的覆盖不被削弱（D-31）。"
    - "存读档集中（D-32）：本计划是本批次唯一测试 `saveState`/`loadState` 的计划；其它计划不测；不引入 `fake-indexeddb`、不 `saveSystem.init`（D-10）。"
    - "阶段化（D-43）：计划按 构件→组合→完整/集成 三阶段组织，取消 tracer-first；每阶段跑绿后才进入下一阶段；某阶段发现阻断性 bug 时暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。"
    - "质量门禁（D-44）：每个计划执行后、提交前，改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误（全局既有无关错误不在范围）；`pnpm test:ci` 全绿；任一不通过不得提交。"
    - "执行节奏（D-33）：本计划执行前先向用户确认；执行完成后暂停，用中文分条简要汇报验证结果，详细结果写入共用的 `06-TEST-FINDINGS.md`。"
    - "增量补测（D-46）：只新增/扩展 `*.test.ts`，不改动已执行的 06-01..06-09 计划与任何生产/核心源码；fixture 文件内联（D-02/D-03），不使用 fake timers（D-04）。"
  artifacts:
    - path: packages-user/data-base/src/hero/saveLoad.test.ts
      provides: G-06-09-B hero 各单档类三档化（含经 HeroState 容器三档覆盖 Location/Rendering/Equipment）
    - path: packages-user/data-base/src/map/saveLoad.test.ts
      provides: G-06-09-B StaticTile/DynamicTile 三档化
    - path: packages-user/data-state/test/saveablesRoundTrip.test.ts
      provides: G-06-09-A CoreState 顶层全关键状态严格一致 + 录像 ≥10 步多样化；G-06-09-B flags/replay 容器三档
    - path: .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
      provides: 阶段级 code → 模块 → 用例 映射表（本计划 create-or-append 06-13 小节）
  key_links:
    - "hero/saveLoad.test.ts 复用既有 `SAVE_COMPRESSIONS`、`createState`/`createEquipEnv`/`createHeroState`/`createFollowerState`/`registerItem`；三档循环参照既有 `HeroEquipsStore`/`HeroFollower` 的 `for (const compression of SAVE_COMPRESSIONS)` 形状"
    - "map/saveLoad.test.ts 复用既有 `SAVE_COMPRESSIONS`、`createMapFixture`/`setLayerReference`；三档循环参照既有 `MapLayer`/`GameMap`/`MapState` 用例形状；DynamicTile 的 `#06-09-3` skip 保持不变"
    - "saveablesRoundTrip.test.ts 以公开入口 `CoreState.saveState(compression)` / `loadState(state, compression)`（D-45）为主路径，`getSaveableContent(id)` 作辅助；录像经 `state.replaySystem.record(code, ...params)` 写入 ≥10 步；不接触 `saveSystem.init`/IndexedDB（D-10）"
    - "`06-SAVE-EXCLUSIONS.md` 为既有排除清单（存档无关/元数据字段），本计划三档断言仍只覆盖该清单允许的关键字段（D-13/D-42）"
    - "`.planning/phases/06-unit-tests/06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写 06-13 小节前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本小节，绝不重写他节"
  prohibitions:
    - "不创建或修改任何生产/核心源码，只新增/扩展 `*.test.ts` 与规划产物"
    - "不改动 06-01..06-09 的 PLAN.md"
    - "不引入 `fake-indexeddb`，不 `saveSystem.init`，不做 IndexedDB 端到端存档（D-10）"
    - "不做整体深度相等；不比较派生/缓存/序列化容器结构（D-13）"
    - "不安装覆盖率工具（D-09）"
    - "不建立跨文件共享 fixture/factory helper，fixture 全部文件内联（D-02/D-03）"
    - "不使用 fake timers（D-04）"
    - "受阻塞/已知缺陷不得弱化为通过；按正确预期编写并标记 `it.skip`/`it.todo`（D-05）"
---

<objective>
按 D-43 阶段化（构件 → 组合 → 完整/集成）补齐 06-09（存档）人工评审发现的覆盖缺口（D-46）：

- **G-06-09-A** `CoreState` 顶层关键状态不完整、录像步数过少
- **G-06-09-B** 多数 saveLoad 类未验证三档压缩均能正确读取

Purpose: 现有顶层往返对每类 saveable 只断言 1 个关键字段、录像仅 1 步；hero/map 多个类只在 `NoCompression` 验证。本计划把每类关键状态逐一严格断言、录像构造 ≥10 步多样化命令，并把单档类三档化（不区分压缩度的类经容器三档确认）。本计划只补测试，不改任何生产代码。

本计划是本批次**唯一**测试 `saveState`/`loadState` 的计划（D-32）。

Output: 3 个既有 `*.test.ts` 的扩展与 `06-COVERAGE-MAP.md` 的 06-13 小节。

阶段结构（D-43）：阶段 1 构件级（hero 单档类三档化 + 经容器覆盖无参类）→ 阶段 2 组合（map StaticTile/DynamicTile 三档化）→ 阶段 3 完整/集成（CoreState 顶层全关键状态严格一致 + 录像 ≥10 步多样化 + flags/replay 容器三档）。每阶段跑绿后再进入下一阶段。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-unit-tests/06-CONTEXT.md
@.planning/phases/06-unit-tests/06-COVERAGE-GAPS.md
@.planning/phases/06-unit-tests/06-PATTERNS.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@.planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
@.planning/codebase/TESTING.md
@dev.md
@packages-user/data-common/src/save/types.ts
@packages-user/data-base/src/hero/state.ts
@packages-user/data-base/src/hero/equipStore.ts
@packages-user/data-base/src/hero/items.ts
@packages-user/data-base/src/hero/location.ts
@packages-user/data-base/src/hero/rendering.ts
@packages-user/data-base/src/hero/equipment.ts
@packages-user/data-base/src/hero/saveLoad.test.ts
@packages-user/data-base/src/map/staticTile.ts
@packages-user/data-base/src/map/dynamicTile.ts
@packages-user/data-base/src/map/saveLoad.test.ts
@packages-user/data-state/src/core.ts
@packages-user/data-state/test/saveablesRoundTrip.test.ts
@packages-user/data-common/src/replay/system.ts
@packages-user/data-base/src/flag/system.ts
@packages/common/src/logger.json
</context>

<execution_rules>
- 只写测试，绝不修改 `packages-user/*/src` 下的非 `*.test.ts` 文件。
- 每个 `it(...)` 之前必须有一行中文注释说明覆盖内容（dev.md 注释规范），测试范围变化时同步更新。
- fixture 内联且最小（D-02/D-03），沿用目标文件既有 helper，不新造跨文件 helper。
- 三档压缩：`NoCompression`/`LowCompression`/`HighCompression`；对不接受 compression 参数的类经容器三档确认。
- 警告/错误路径经 `logger.catch` / `vi.spyOn(logger, ...)`。
- 「状态相同」只用关键字段显式断言（D-13）；排除清单见 `06-SAVE-EXCLUSIONS.md`。
- 已知缺陷：既有 `#06-09-1..5` 的 `it.skip` 保持不变；新命中的缺陷按正确预期编写、标记 `it.skip`、中文注释锚定既有 `#06-09-N`（或新建 `#06-13-N`），`pnpm test:ci` 必须保持全绿（D-05）。
- 不引入 `fake-indexeddb`，不 `saveSystem.init`（D-10）。
- `06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写入前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本计划小节，绝不重写他节。
- **执行协议（D-33）：执行本计划前必须先向用户确认；执行完成后暂停，用中文分条简要汇报本计划验证结果；详细结果写入共用的 `06-TEST-FINDINGS.md`。**
- **阶段门禁（D-43）：本计划按 构件 → 组合 → 完整/集成 三阶段顺序执行，每阶段跑绿后才进入下一阶段；若某阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。**
- **质量门禁（D-44，每个计划执行后、提交前必过）**：对改动文件运行 `eslint --fix`，并确保 `eslint <改动文件>` **0 错误**；对**本计划改动的测试文件**运行 `pnpm exec vue-tsc --noEmit`，并按输出文件路径过滤，确保这些文件路径下 **0 类型错误**（全局既有无关错误，如 `client-modules`/`legacy-plugin-data`，不在本门禁范围）；`pnpm test:ci` **全绿**。任一不通过则**不得提交**。
</execution_rules>

<tasks>

<task type="auto">
  <name>阶段 1（构件级）：G-06-09-B hero 单档类三档化 + 经 HeroState 容器三档覆盖无参类</name>
  <files>packages-user/data-base/src/hero/saveLoad.test.ts</files>
  <read_first>
    packages-user/data-base/src/hero/saveLoad.test.ts（既有 `SAVE_COMPRESSIONS` 与 EquipmentState/HeroItems/HeroState 单档用例、HeroLocation/HeroRendering/HeroEquipment 无参用例）
    packages-user/data-base/src/hero/equipStore.ts（`EquipmentState.saveState(compression)`/`loadState(save, compression)`；`#06-09-1` 相关）
    packages-user/data-base/src/hero/items.ts（`HeroItems.saveState(compression)`/`loadState(state, compression)`）
    packages-user/data-base/src/hero/state.ts（`HeroState.saveState(compression)` 聚合 location/rendering/equip/items）
    packages-user/data-base/src/hero/location.ts / rendering.ts / equipment.ts（`saveState()`/`loadState(state)` 无 compression 参数）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-09-B）
    .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
  </read_first>
  <action>
    把 hero 单档类三档化，并让无 compression 参数的类经 `HeroState` 容器三档确认：
    1) `EquipmentState`：把既有「百分比加成恢复」用例改为 `for (const compression of SAVE_COMPRESSIONS)` 三档循环（每档新建 `EquipmentState`，`saveState(compression)` → `loadState(saved, compression)` → 断言百分比修饰器值）；把既有 value-modifier 与 compressed 两条 `it.skip`（锚定 `#06-09-1`）保持不变。
    2) `HeroItems`：三档循环，每档新建 env/items → 设已知数量 → `saveState(compression)` → 改变数量 → `loadState(saved, compression)` → 断言永久/消耗两表数量与 `getItemState` 关键字段。
    3) `HeroState`：三档循环，每档新建 `createHeroState()` → 注册并插入修饰器、设 base 属性、设位置/楼层/朝向 → `saveState(compression)` → 改状态 → `loadState(saved, compression)` → 断言 base 属性、final 属性、`getLocation()` 与 `location.floorId`。
    4) 无参类经容器三档：新增用例，三档循环内用 `hero.saveState(compression)`/`loadState(saved, compression)`，断言 `hero.location`（坐标/楼层/朝向）、`hero.rendering.alpha`（若 `HeroState` 公开该成员）、`hero.equip.getEquipped(...)`（若公开）在每档均恢复到存档点；成员名以 `state.ts` 实际公开成员为准，不确定时只断言确定公开的 location 字段并在注释中说明。
    实现时若某档命中已知缺陷（如 `#06-09-1`），按 D-05 将该档改为正确预期 `it.skip` 并中文注释锚定既有 `#06-09-N`，不得弱化为通过；若发现新缺陷，登记 `06-TEST-FINDINGS.md` 的 `#06-13-N`。每个新增/改写的 `it` 前保持单行中文注释。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/hero/saveLoad.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 EquipmentState/HeroItems/HeroState 任一未三档循环，或 Location/Rendering/Equipment 未经容器三档确认，或既有 `#06-09-1`/`#06-09-2` skip 被取消/改写</fails_when>
  </verify>
  <acceptance_criteria>
    - `hero/saveLoad.test.ts` 零非跳过失败。
    - G-06-09-B（hero 部分）：EquipmentState/HeroItems/HeroState 三档循环；HeroLocation/HeroRendering/HeroEquipment 经 HeroState 容器三档确认；无参类三档均有断言。
    - 既有 `#06-09-1`/`#06-09-2` skip 保持不变；新命中缺陷按 D-05 处理并锚定。
    - 每个新增/改写的 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 1 的 G-06-09-B（hero）覆盖全部跑绿后才进入阶段 2。若发现阻断性 bug，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 2（组合）：G-06-09-B map StaticTile/DynamicTile 三档化</name>
  <files>packages-user/data-base/src/map/saveLoad.test.ts</files>
  <read_first>
    packages-user/data-base/src/map/saveLoad.test.ts（既有 `SAVE_COMPRESSIONS`、`createMapFixture`/`setLayerReference`，与 StaticTile/DynamicTile 单档用例）
    packages-user/data-base/src/map/staticTile.ts / dynamicTile.ts（`saveState()`/`loadState(save)`；既有 `#06-09-3` skip）
    packages-user/data-base/src/map/mapLayer.ts（tile `saveState(SaveCompression.NoCompression)` 的调用形状）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-09-B）
    .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
  </read_first>
  <action>
    把 map 的两个单档 tile 类三档化：
    1) `StaticTile`：把既有「覆盖事件恢复」用例改为 `for (const compression of SAVE_COMPRESSIONS)` 三档循环，每档新建 fixture/tile、设覆盖事件 → `tile.saveState(compression)` → 改事件 → `tile.loadState(saved, compression)` → 断言事件映射恢复到存档点（沿用既有锚点与关键字段断言）。
    2) `DynamicTile`：同样三档循环断言覆盖事件恢复；既有「恢复图块数字」的 `it.skip`（锚定 `#06-09-3`）保持不变，不改写。
    若某档命中已知缺陷，按 D-05 将该档改为正确预期 `it.skip` 并锚定既有 `#06-09-3`（或登记 `#06-13-N`），不得弱化为通过。每个改写的 `it` 前保持单行中文注释并更新为「三档」描述。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/map/saveLoad.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 StaticTile/DynamicTile 未三档循环，或既有 `#06-09-3` skip 被取消/改写，或既有 MapLayer/GameMap/MapState 三档用例回归失败</fails_when>
  </verify>
  <acceptance_criteria>
    - `map/saveLoad.test.ts` 零非跳过失败。
    - G-06-09-B（map 部分）：StaticTile 与 DynamicTile 均三档循环并断言关键事件字段恢复。
    - 既有 `#06-09-3` skip 保持不变。
    - 每个改写的 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 2 的 G-06-09-B（map）覆盖在阶段 1 跑绿的基础上全部跑绿后才进入阶段 3。若发现阻断性 bug，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 3（完整/集成）：G-06-09-A 顶层全关键状态严格一致 + 录像 ≥10 步 + G-06-09-B flags/replay 容器三档（含覆盖表写入）</name>
  <files>packages-user/data-state/test/saveablesRoundTrip.test.ts, .planning/phases/06-unit-tests/06-COVERAGE-MAP.md</files>
  <read_first>
    packages-user/data-state/test/saveablesRoundTrip.test.ts（既有 `SAVEABLE_IDS`/`COMPRESSIONS`/`seedState`/`mutateState`/`assertRestored` 与 5 saveable × 3 档用例）
    packages-user/data-state/src/core.ts（公开 `saveState`/`loadState`、`getSaveableContent`、5 个注册 id）
    packages-user/data-common/src/replay/system.ts（`record(code, ...params)`/`route`）
    packages-user/data-common/src/replay/types.ts（`ReplayCommandCode` 0..7）
    packages-user/data-base/src/flag/system.ts（`setFieldValue`/`addFieldValue`/`getFieldValue`）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-09-A、G-06-09-B）
    .planning/phases/06-unit-tests/06-SAVE-EXCLUSIONS.md
  </read_first>
  <action>
    扩展 `saveablesRoundTrip.test.ts`（主路径保持公开 `CoreState.saveState`/`loadState`，D-45）：
    1) G-06-09-A 全关键状态：扩展 `seedState` 写入**每类 saveable 的全部关键状态**——勇士：base 属性 hp/atk/def/money/exp、`state.hero.location.setPos(x, y)` / `state.hero.location.setFloor('F1')`、朝向经 `state.hero.location.mover.setFaceDir(FaceDirection.Up)`（**`HeroLocation` 自身没有 `setFaceDir`**：其 `saveState`/`loadState` 通过 `mover.faceDirection` 与 `mover.setFaceDir` 读写朝向，故必须先 `mover` 再 `setFaceDir`，并需从 `@user/data-common` 导入 `FaceDirection`）、修饰器值；flags：`setFieldValue('score',7)`、`setFieldValue('coins',12)`、`addFieldValue('stage',2)`；地图：`createMap('F1',2,2)` → `addLayer` → `setZIndex(0)` → `setBlock(5,0,0)`/`setBlock(7,1,1)` → `setActiveStatus(true)`；enemy：`addPrefab` → `compareWith` → `modifyPrefabAttribute` 设 hp 30 与 atk 9；录像：`record` ≥10 步多样化命令。
    2) 录像 ≥10 步多样化序列（命令不全相同）：`state.replaySystem.record(0)`、`record(1)`、`record(2)`、`record(3)`、`record(4, 2, 3)`（Teleport）、`record(5, 12)`（UseItem）、`record(6, 3, 0, true)`（Equip）、`record(7, 0)`（Unequip）、`record(1)`、`record(5, 'potion')`；断言 `state.replaySystem.route.length === 10` 且对每步 `route.get(i)` 精确等于 `{command, params, index}`。
    3) `mutateState` 同样改遍上述全部关键状态；`assertRestored` 对上述**每一项**逐一严格断言（勇士属性/位置/修饰器、flags 三字段、地图两块与激活状态、enemy 两属性、录像 10 步逐条）。保留 5 saveable × 3 压缩档循环（D-11），并使用既有 `#06-09-5` 的 skip 不变。
    4) G-06-09-B（flags/replay 容器三档）：新增用例对 `@system/flags` 与 `@system/replay` 三档循环——每档改 flags 字段与追加录像步 → `saveState(compression)` → 再改 → `loadState(snapshot, compression)` → 断言 flags 字段与录像步数/逐条命令恢复到存档点（不区分压缩度的类经容器三档确认读取正确）。
    若某档命中已知缺陷（`#06-09-1..5`），按 D-05 写正确预期 + `it.skip` 并锚定；不得弱化为通过。每个新增/改写的 `it` 前有单行中文注释。
    完成后以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 的 06-13 小节：本计划**不引入新码**，先确认表头存在（不存在则创建），再追加 `## 06-13 存档缺口补测` 小节，列出 G-06-09-A、G-06-09-B → 用例名 → 计划 06-13 的映射，并注明「本计划无新增码，复用 06-09 既有可达码」；只追加/更新本小节，绝不重写他节。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-state/test/saveablesRoundTrip.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或顶层往返未对每类 saveable 的全部关键状态逐一断言，或录像少于 10 步/命令全相同，或 flags/replay 未三档经容器确认，或 06-13 小节未写入 `06-COVERAGE-MAP.md`，或既有 `#06-09-5` skip 被取消</fails_when>
  </verify>
  <acceptance_criteria>
    - `saveablesRoundTrip.test.ts` 零非跳过失败。
    - G-06-09-A：每类 saveable 的全部关键状态逐一严格断言；录像 ≥10 步多样化命令逐条 exact 断言。
    - G-06-09-B（flags/replay）：三档经容器确认读取正确。
    - `06-COVERAGE-MAP.md` 存在 06-13 小节（create-or-append，未重写他节），列出 G-06-09-A/B 的用例映射。
    - 既有 `#06-09-1..5` skip 保持不变；每个新增/改写的 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 3 的 G-06-09-A/B（顶层）覆盖在阶段 1/2 跑绿的基础上全部跑绿，且 06-13 小节已写入 `06-COVERAGE-MAP.md`。完成后暂停并用中文分条汇报；详细结果写入 `06-TEST-FINDINGS.md`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | 仅新增/扩展仓库内 Node Vitest 测试；无运行时输入面、网络、DOM 或文件写入（不碰 IndexedDB）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13-01 | Tampering | `data-base/src/hero`、`data-base/src/map`、`data-state/test` 下测试文件 | low | mitigate | 只导入已跟踪模块、合成快照；不安装包、不改核心源码；门禁 `pnpm test:ci` + 复核仅 `*.test.ts` 变更。 |
| T-06-13-02 | Information disclosure | 快照 fixture | low | accept | 合成状态，无真实存档文件或密钥（D-02）。 |
| T-06-13-03 | Repudiation | 全局 stub 泄漏 | low | accept | 既有文件已在 `vi.hoisted`/`afterAll` 管理 stub；新增用例不引入新 stub。 |
| T-06-13-SC | Tampering | 包安装（尤其 `fake-indexeddb`） | high | mitigate | 明确不引入 `fake-indexeddb` 或任何新依赖；若确需安装，停止并先走包合法性 checkpoint。 |
</threat_model>

<verification>
- 三个测试文件聚焦运行通过；`pnpm test:ci` 全量绿。
- `git status` 仅显示三个 `*.test.ts` 与 `06-COVERAGE-MAP.md` 的变更；无核心文件改动。
- 既有 `#06-09-1..5` 的 `it.skip` 保持不变；任何新增 skip 均有 `#06-13-N`（或既有）条目。
- 以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-13 小节；表头不存在则先创建；幂等、顺序无关，不重写他节。
- **执行协议**：执行前必须先向用户确认；执行后暂停并用中文分条汇报。
</verification>

<success_criteria>
- G-06-09-A：顶层每类 saveable 的全部关键状态逐一严格断言；录像 ≥10 步多样化命令逐条 exact 断言。
- G-06-09-B：所有含 `saveState`/`loadState` 的类三档覆盖（无参类经容器三档）。
- 无生产代码改动，`pnpm test:ci` 全绿。
</success_criteria>

<artifacts_this_plan_produces>
Test files (extended):
- `packages-user/data-base/src/hero/saveLoad.test.ts`（EquipmentState/HeroItems/HeroState 三档化 + 经 HeroState 容器覆盖 Location/Rendering/Equipment）
- `packages-user/data-base/src/map/saveLoad.test.ts`（StaticTile/DynamicTile 三档化）
- `packages-user/data-state/test/saveablesRoundTrip.test.ts`（全关键状态严格一致 + 录像 ≥10 步多样化 + flags/replay 容器三档）

Planning artifacts:
- `06-COVERAGE-MAP.md` create-or-append 06-13 小节
</artifacts_this_plan_produces>

<output>
完成时创建 `.planning/phases/06-unit-tests/06-13-SUMMARY.md`，以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-13 小节；疑似 bug 追加到 `06-TEST-FINDINGS.md` 的 `#06-13-N` 小节（若有；若为既有缺陷复用 `#06-09-N` 锚点，不新建）。执行完成后暂停并用中文分条汇报。
</output>
