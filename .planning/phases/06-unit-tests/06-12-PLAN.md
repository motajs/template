---
phase: 06-unit-tests
plan: 12
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - packages-user/data-common/src/replay/array.test.ts
  - packages-user/data-base/src/enemy/manager.test.ts
  - .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
autonomous: true
requirements: [TEST-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TEST-01 由数据端行为单测满足，门禁为固定非 watch 命令 `pnpm test:ci` 全绿（D-08）。"
    - "G-06-04-B（可跑绿）：构造 5+ 条异质录像命令（参数个数与类型各不相同，混合 boolean/各路宽整数/float/string/bigint），经 `createReadStream` 与 `get` 逐条读回一致（`createReadStream` 的 `index === position + 1`、`get(i)` 的 `index === i`，故两者只比较 `command`/`params`，不对整体对象做相等）；并覆盖异质序列删除首步后的读回（删除首步为当前可用路径）。"
    - "G-06-04-A（正确预期 + `it.skip`）：多字节 bigint 与超 int32 的 int64 编解码受 `#06-04-1`/`#06-04-2` 阻塞，按正确预期编写并保持 `it.skip`，中文注释锚定 `06-TEST-FINDINGS.md` 的 `#06-04-1`/`#06-04-2`；`pnpm test:ci` 保持全绿。"
    - "G-06-03-A（正确预期 + `it.skip`）：多 code/id（4 朝向）复用同一 prefab 并各自生成互相独立怪物的完整链路受 `#06-03-1` 阻塞，按正确预期编写并标记 `it.skip`，中文注释锚定 `#06-03-1`；`pnpm test:ci` 保持全绿。"
    - "接口覆盖：本计划只经既有公开接口驱动（`ReplayArray` 的 add/delete/set/get/createReadStream/setCommandWidth/rebuildIndexArray/getCommandArray/getParamArray，`EnemyManager` 的 addPrefab/reusePrefab/getPrefab/createEnemy/createEnemyById），名称含 legacy 的接口/方法排除（D-30）。"
    - "code 覆盖（模块可达）：本计划不引入新码；复用并再确认既有可达码（如 148/152/155/163）的覆盖不被削弱（D-31）。"
    - "测试形态（D-01）：纯数据模块单测（fake / 显式依赖注入，延续 Phase 3 D-03）；不引入真实录像数据（D-02）。"
    - "阶段化（D-43）：计划按 构件→组合→完整 三阶段组织，取消 tracer-first；每阶段跑绿后才进入下一阶段；某阶段发现阻断性 bug 时暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。"
    - "质量门禁（D-44）：每个计划执行后、提交前，改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误（全局既有无关错误不在范围）；`pnpm test:ci` 全绿；任一不通过不得提交。"
    - "执行节奏（D-33）：本计划执行前先向用户确认；执行完成后暂停，用中文分条简要汇报验证结果，详细结果写入共用的 `06-TEST-FINDINGS.md`。"
    - "增量补测（D-46）：只新增/扩展 `*.test.ts`，不改动已执行的 06-01..06-09 计划与任何生产/核心源码；fixture 文件内联（D-02/D-03），不使用 fake timers（D-04）。"
    - "受阻塞缺口不得弱化为通过：G-06-04-A、G-06-03-A 一律以正确预期 + `it.skip` 登记（D-05），不得写成可通过的断言（D-46）。"
  artifacts:
    - path: packages-user/data-common/src/replay/array.test.ts
      provides: G-06-04-B 异质多命令序列读回（可跑绿）与 G-06-04-A 多字节 bigint/int64 正确预期 skip
    - path: packages-user/data-base/src/enemy/manager.test.ts
      provides: G-06-03-A 多 code/id 复用同一 prefab 生成独立怪物的正确预期 skip
    - path: .planning/phases/06-unit-tests/06-COVERAGE-MAP.md
      provides: 阶段级 code → 模块 → 用例 映射表（本计划 create-or-append 06-12 小节）
  key_links:
    - "array.test.ts 复用既有 `createArray(overrides)`、`paramCases`、`firstParamToken(array)` 与 `ReplayCommandWidth`/`ReplayParamValue`；异质序列经 `add`/`get`/`createReadStream`/`getCommandArray`/`getParamArray`/`setCommandWidth` 观测，不读内部实现细节"
    - "既有 `it.skip`（`round-trips a multi-byte bigint` 锚定 `#06-04-2`、`round-trips int64 values above the int32 range` 锚定 `#06-04-1`）与 `deletes a middle step and shifts later param indexes`（`#06-04-3`）、`reads the new order after inserting a step`（`#06-04-4`）保持不变，不得取消 skip"
    - "manager.test.ts 复用既有 `createManager`/`createPrefab`/`bridge`/`dirtyCodes`；复用链路经 `addPrefab(1,'slime')` → `reusePrefab(1, 100|101|102|103, ...)` → `createEnemy(100..103)`/`createEnemyById(...)` 驱动；既有 `#06-03-1` skip 不改动"
    - "`.planning/phases/06-unit-tests/06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写 06-12 小节前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本小节，绝不重写他节"
  prohibitions:
    - "不创建或修改任何生产/核心源码，只新增/扩展 `*.test.ts` 与规划产物"
    - "不改动 06-01..06-09 的 PLAN.md"
    - "不测试 `saveState`/`loadState`（集中到 06-13）（D-32）"
    - "不安装覆盖率工具（D-09）"
    - "不建立跨文件共享 fixture/factory helper，fixture 全部文件内联（D-02/D-03）"
    - "不使用 fake timers（D-04）"
    - "受阻塞缺口不得弱化为通过；按正确预期编写并标记 `it.skip`/`it.todo`（D-05）"
---

<objective>
按 D-43 阶段化（构件 → 组合 → 完整）补齐 06-04（录像）与 06-03（enemy 数据模型）人工评审发现的覆盖缺口（D-46）：

- **G-06-04-B** 异质多命令序列的读回未专门验证（`replay/array.test.ts`，可跑绿）
- **G-06-04-A** 多字节 bigint / int64 参数类型未真正验证（`replay/array.test.ts`，受 `#06-04-1`/`#06-04-2` 阻塞 → 正确预期 `it.skip`）
- **G-06-03-A** 多 code/id（4 朝向）共用同一 prefab 的实例化路径未验证（`enemy/manager.test.ts`，受 `#06-03-1` 阻塞 → 正确预期 `it.skip`）

Purpose: 现有用例的录像序列参数基本同质，enemy 复用的**读**路径有断言但**生成**路径受 bug 阻塞。本计划补「异质序列整体读回」的可跑绿用例，并把两个受阻塞缺口以正确预期 `it.skip` 纳入（D-46），不修任何生产代码。

Output: 2 个既有 `*.test.ts` 的扩展与 `06-COVERAGE-MAP.md` 的 06-12 小节。

阶段结构（D-43）：阶段 1 构件级（G-06-04-B 异质序列读回）→ 阶段 2 组合（G-06-04-A 受阻塞的编解码正确预期 skip）→ 阶段 3 完整（G-06-03-A 复用实例化正确预期 skip）。每阶段跑绿后再进入下一阶段。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-unit-tests/06-CONTEXT.md
@.planning/phases/06-unit-tests/06-COVERAGE-GAPS.md
@.planning/phases/06-unit-tests/06-PATTERNS.md
@.planning/phases/06-unit-tests/06-TEST-FINDINGS.md
@.planning/codebase/TESTING.md
@dev.md
@packages-user/data-common/src/replay/array.ts
@packages-user/data-common/src/replay/types.ts
@packages-user/data-common/src/replay/array.test.ts
@packages-user/data-base/src/enemy/manager.ts
@packages-user/data-base/src/enemy/manager.test.ts
@packages/common/src/logger.json
</context>

<execution_rules>
- 只写测试，绝不修改 `packages-user/*/src` 下的非 `*.test.ts` 文件。
- 每个 `it(...)` 之前必须有一行中文注释说明覆盖内容（dev.md 注释规范），测试范围变化时同步更新。
- fixture 内联且最小（D-02/D-03），沿用目标文件既有 helper，不新造跨文件 helper。
- 警告/错误路径经 `logger.catch` / `vi.spyOn(logger, ...)`。
- 受阻塞缺口：按正确预期编写、标记 `it.skip`、中文注释锚定既有 `#06-04-1`/`#06-04-2`/`#06-03-1`；`pnpm test:ci` 必须保持全绿（D-05）。
- 本计划不测 `saveState`/`loadState`（D-32）。
- `06-COVERAGE-MAP.md` 为共享 create-or-append 文件（幂等、顺序无关）：写入前先检查表头（`| code | 模块 | 用例 | 计划 |`）是否存在，不存在则先创建，再只追加/更新本计划小节，绝不重写他节。
- **执行协议（D-33）：执行本计划前必须先向用户确认；执行完成后暂停，用中文分条简要汇报本计划验证结果；详细结果写入共用的 `06-TEST-FINDINGS.md`。**
- **阶段门禁（D-43）：本计划按 构件 → 组合 → 完整 三阶段顺序执行，每阶段跑绿后才进入下一阶段；若某阶段发现阻断性 bug 使后续阶段无法运行，暂停并按 D-05/D-07 向用户汇报、等确认，不得跳过或弱化。**
- **质量门禁（D-44，每个计划执行后、提交前必过）**：对改动文件运行 `eslint --fix`，并确保 `eslint <改动文件>` **0 错误**；对**本计划改动的测试文件**运行 `pnpm exec vue-tsc --noEmit`，并按输出文件路径过滤，确保这些文件路径下 **0 类型错误**（全局既有无关错误，如 `client-modules`/`legacy-plugin-data`，不在本门禁范围）；`pnpm test:ci` **全绿**。任一不通过则**不得提交**。
</execution_rules>

<tasks>

<task type="auto">
  <name>阶段 1（构件级）：G-06-04-B 5+ 条异质录像命令序列的整体读回</name>
  <files>packages-user/data-common/src/replay/array.test.ts</files>
  <read_first>
    packages-user/data-common/src/replay/array.ts（`add`/`delete`/`set`/`get`/`createReadStream`/`getCommandArray`/`getParamArray`/`setCommandWidth`/`rebuildIndexArray`/`length`）
    packages-user/data-common/src/replay/array.test.ts（既有 `createArray`/`paramCases`/`firstParamToken` 与「stream and buffer combination」分区）
    packages-user/data-common/src/replay/types.ts（`ReplayParamValue`/`ReplayCommandWidth`）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-04-B）
    .planning/phases/06-unit-tests/06-PATTERNS.md（B 节 array + 共享样板）
  </read_first>
  <action>
    在 `array.test.ts` 的「stream and buffer combination」分区新增可跑绿用例。构造 6 条参数个数与类型各不相同的命令（保持一致、无需修复）：`add(1, [10])`（单 int）、`add(2, [true, 'x'])`（boolean + 短 string）、`add(3, [])`（无参数）、`add(4, [100n])`（单字节 bigint）、`add(5, [1, -32769, 3])`（多 int）、`add(6, ['a'.repeat(300), 1.5])`（长 string + float）。断言：`array.length === 6`；`createReadStream(0)` 依次 `read()` 的 `command`/`params` 精确等于每一步期望，末尾 `read()` 返回 `null`；`setCommandWidth(ReplayCommandWidth.Uint16)` 后再次逐条读回一致。
    **索引语义（务必区分，否则整体对象相等会失败）**：`createReadStream(0)` 读回的 `index` 为 `position + 1`（即 1..6，与既有 `reads a sequence of steps through a read stream` 的 L292 形状一致），而 `array.get(i)` 的 `index === i`（0..5）。因此逐条把 `get(i)` 与流读回对应时，**只比较 `command` 与 `params`**（`expect(array.get(i)).toMatchObject({ command, params })`），或对 `index` 做 ±1 校正后比较，**不要**对 `get(i)` 与 `read()` 的返回值做 `toEqual` 整体相等。
    再增一条可跑绿用例覆盖异质序列的**删除首步**：在上述 6 步基础上 `delete(0)`，断言 `array.length === 5` 且 `get(0)` 等于原第 2 步（`{command:2, params:[true,'x'], index:0}`，`get` 的 index 从 0 起）；随后 `createReadStream(0)` 逐条读回剩余 5 步——同样只比较 `command`/`params`（原第 2..6 步），流读回的 `index` 为 1..5，而 `get(i)` 为 0..4（删除首步为当前可用路径，`#06-04-3` 仅阻塞中间删除）。
    注意：**不要**在本计划重写或取消既有 `it.skip`（`deletes a middle step...` `#06-04-3`、`reads the new order after inserting a step` `#06-04-4`、`round-trips a multi-byte bigint` `#06-04-2`、`round-trips int64 values above the int32 range` `#06-04-1`）。每步用一个内联 `expectedSteps` 数组保存期望，避免重复字面量。两条用例前各写一行中文注释。若读回与期望不符，按 D-05 写正确预期 + `it.skip` 并登记 `06-TEST-FINDINGS.md` 的 `#06-12-N`，不得弱化。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-common/src/replay/array.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或异质序列未覆盖 5+ 条且参数类型/个数各异，或未同时经 `createReadStream` 与 `get` 逐条断言，或删除首步后的异质读回缺失，或既有 148/152/153/155 用例回归失败</fails_when>
  </verify>
  <acceptance_criteria>
    - `array.test.ts` 零非跳过失败。
    - G-06-04-B：5+ 条异质命令（不同参数个数与类型，含 boolean/整数/float/string/bigint）逐条读回一致；删除首步后的异质序列读回一致；`createReadStream`（`index = position + 1`）与 `get(i)`（`index = i`）的比对只覆盖 `command`/`params`。
    - 未改动既有 `it.skip` 集合（4 条 skip 数量与锚点不变）。
    - 每个新增 `it` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 1 的 G-06-04-B 覆盖全部跑绿后才进入阶段 2。若发现阻断性 bug，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 2（组合）：G-06-04-A 多字节 bigint / 超 int32 int64 的正确预期 skip（含异质序列）</name>
  <files>packages-user/data-common/src/replay/array.test.ts</files>
  <read_first>
    packages-user/data-common/src/replay/array.test.ts（既有两条 `it.skip`：多字节 bigint、int64 超 int32）
    packages-user/data-common/src/replay/array.ts（`normalizeParam` 与 `decodeParam` 的 bigint/int64 分支，**只读参考**）
    .planning/phases/06-unit-tests/06-TEST-FINDINGS.md（`#06-04-1`、`#06-04-2`）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-04-A）
  </read_first>
  <action>
    G-06-04-A 的两个受阻塞缺口按 D-46 以正确预期 `it.skip` 纳入：
    1) 确认既有 `it.skip('round-trips a multi-byte bigint')`（锚定 `#06-04-2`）与 `it.skip('round-trips int64 values above the int32 range')`（锚定 `#06-04-1`）存在且断言按正确预期编写；若任一缺失则按既有形状补回（含中文注释锚点）。**不得取消 skip、不得修改其断言为可通过**。
    2) 新增一条 `it.skip` 组合用例 `G-06-04-A：异质序列中的多字节 bigint 与超 int32 int64 一起读回`：`const array = createArray()`；`array.add(0, [true, 0x0102030405060708n, 2147483648, 'x'])`；按正确预期断言 `array.get(0).params` 精确等于 `[true, 0x0102030405060708n, 2147483648, 'x']`；中文注释锚定 `06-TEST-FINDINGS.md` 的 `#06-04-1`/`#06-04-2` 并说明「修复两个编码/解码缺陷后取消 skip」。该用例为 `it.skip`，不影响 `pnpm test:ci` 绿。
    若执行时发现 `#06-04-1`/`#06-04-2` 已在后续修复批次解决（编码/解码已正确），则取消对应 skip 并转为可跑绿回归用例，同时在 `06-TEST-FINDINGS.md` 更新状态；否则保持 skip。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-common/src/replay/array.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 G-06-04-A 缺少正确预期的 `it.skip`（多字节 bigint / 超 int32 int64），或 skip 用例被改写成可通过断言、或中文注释锚点缺失</fails_when>
  </verify>
  <acceptance_criteria>
    - G-06-04-A：两条既有 skip 保持（锚定 `#06-04-1`/`#06-04-2`），并新增一条异质序列组合 skip（正确预期）。
    - `pnpm test:ci` 仍全绿（新增 skip 不产生失败）。
    - 每个新增 `it.skip` 前有单行中文注释并锚定 `06-TEST-FINDINGS.md` 条目（D-05）。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 2 的 G-06-04-A 以正确预期 skip 纳入且在阶段 1 跑绿基础上保持全绿后才进入阶段 3。若发现阻断性 bug，暂停并按 D-05/D-07 向用户汇报、等确认。</done>
</task>

<task type="auto">
  <name>阶段 3（完整）：G-06-03-A 多 code/id 复用同一 prefab 生成独立怪物的正确预期 skip（含覆盖表写入）</name>
  <files>packages-user/data-base/src/enemy/manager.test.ts, .planning/phases/06-unit-tests/06-COVERAGE-MAP.md</files>
  <read_first>
    packages-user/data-base/src/enemy/manager.test.ts（既有 `createManager`/`createPrefab`/`bridge` 与 `#06-03-1` 的 skip）
    packages-user/data-base/src/enemy/manager.ts（`addPrefab`/`reusePrefab`/`getPrefab`/`getPrefabById`/`createEnemy`/`createEnemyById`，**只读参考**）
    .planning/phases/06-unit-tests/06-TEST-FINDINGS.md（`#06-03-1`）
    .planning/phases/06-unit-tests/06-COVERAGE-GAPS.md（G-06-03-A）
  </read_first>
  <action>
    在 `manager.test.ts` 的「reuse mapping」分区新增一条正确预期 `it.skip`，专门覆盖「4 个朝向 code → 1 个 prefab → 分别 createEnemy 得到 4 个互相独立的怪」的完整链路（当前受 `#06-03-1` 阻塞）：
    1) `const manager = createManager()`；`manager.addPrefab(createPrefab(1, 'slime'))`；
    2) 四个朝向复用同一来源：`manager.reusePrefab(1, 100, 'slime-up')`、`reusePrefab(1, 101, 'slime-right')`、`reusePrefab(1, 102, 'slime-down')`、`reusePrefab(1, 103, 'slime-left')`；
    3) 正确预期：先断言读路径 `getPrefab(100..103)` 均等于 `getPrefab(1)`；再 `const created = [100, 101, 102, 103].map(code => manager.createEnemy(code))`，断言 `created.every(e => e !== null)`、各 `e.id === 'slime'`、四者互不相同（`new Set(created).size === 4`）、且与模板不同；对第一个 `setAttribute('hp', 99)` 后断言其余三者与模板 `hp` 仍为 20，证明互相独立；
    4) 同样对 `createEnemyById('slime-up'|'slime-right'|'slime-down'|'slime-left')` 断言读回同一来源且各自独立。
    整条用例为 `it.skip`，中文注释锚定 `06-TEST-FINDINGS.md` 的 `#06-03-1` 并说明「修复复用映射后取消 skip」；不改动既有同锚点 skip。注意 D-30：不使用 `fromLegacyEnemy` 等 legacy 接口。
    完成后以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 的 06-12 小节：本计划**不引入新码**，先确认表头存在（不存在则创建），再追加 `## 06-12 录像与 enemy 模型缺口补测` 小节，列出 G-06-04-A（skip）/G-06-04-B/G-06-03-A（skip）→ 用例名 → 计划 06-12 的映射，并注明「本计划无新增码，复用既有可达码；两条为受阻塞的正确预期 skip」；只追加/更新本小节，绝不重写他节。
  </action>
  <verify>
    <automated>pnpm exec vitest run packages-user/data-base/src/enemy/manager.test.ts</automated>
    <fails_when>该文件运行失败（非 skip），或 G-06-03-A 缺少 4 朝向复用 → 4 个独立怪物的正确预期 `it.skip`，或该 skip 被写成可通过断言，或 06-12 小节未写入 `06-COVERAGE-MAP.md`，或既有 manager 用例回归失败</fails_when>
  </verify>
  <acceptance_criteria>
    - `manager.test.ts` 零非跳过失败。
    - G-06-03-A：新增正确预期 `it.skip` 覆盖 4 个朝向 code/id 复用同一 prefab 并生成 4 个互相独立怪物的完整链路，锚定 `#06-03-1`。
    - `06-COVERAGE-MAP.md` 存在 06-12 小节（create-or-append，未重写他节），列出 G-06-04-A/B、G-06-03-A 的用例映射。
    - 每个新增 `it.skip` 前有单行中文注释。
    - D-44 门禁通过：改动文件 `eslint --fix` 后 `eslint <改动文件>` 0 错误；本计划改动的测试文件在 `pnpm exec vue-tsc --noEmit` 输出中按文件路径过滤 0 类型错误；`pnpm test:ci` 全绿。
  </acceptance_criteria>
  <done>阶段 3 的 G-06-03-A 正确预期 skip 在阶段 1/2 跑绿基础上保持全绿，且 06-12 小节已写入 `06-COVERAGE-MAP.md`。完成后暂停并用中文分条汇报；详细结果写入 `06-TEST-FINDINGS.md`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | 仅新增/扩展仓库内 Node Vitest 测试；无运行时输入面、网络、DOM 或文件写入。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12-01 | Tampering | `data-common/src/replay` 与 `data-base/src/enemy` 下测试文件 | low | mitigate | 只导入已跟踪模块、合成 fixture；不改核心源码；门禁 `pnpm test:ci` + 复核仅 `*.test.ts` 变更。 |
| T-06-12-02 | Information disclosure | 合成 fixture | low | accept | 合成录像/怪物属性，无真实数据或密钥（D-02）。 |
| T-06-12-03 | Repudiation | 受阻塞 skip 被误改为通过 | low | mitigate | skip 用例保留中文注释与 `06-TEST-FINDINGS.md` 锚点；`pnpm test:ci` 全绿即证明未弱化。 |
| T-06-12-SC | Tampering | 包安装 | high | mitigate | 明确不引入任何新依赖；若确需安装，停止并先走包合法性 checkpoint。 |
</threat_model>

<verification>
- 两个测试文件聚焦运行通过；`pnpm test:ci` 全量绿。
- `git status` 仅显示两个 `*.test.ts` 与 `06-COVERAGE-MAP.md` 的变更；无核心文件改动。
- G-06-04-A、G-06-03-A 均为正确预期 `it.skip` 且有 `06-TEST-FINDINGS.md` 锚点；G-06-04-B 为可跑绿。
- 以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-12 小节；表头不存在则先创建；幂等、顺序无关，不重写他节。
- **执行协议**：执行前必须先向用户确认；执行后暂停并用中文分条汇报。
</verification>

<success_criteria>
- G-06-04-B 有可跑绿的异质多命令序列读回用例。
- G-06-04-A、G-06-03-A 以正确预期 `it.skip` 纳入并锚定既有 finding 条目，`pnpm test:ci` 保持全绿。
- 无生产代码改动。
</success_criteria>

<artifacts_this_plan_produces>
Test files (extended):
- `packages-user/data-common/src/replay/array.test.ts`（新增异质序列读回 2 条可跑绿用例 + 1 条组合正确预期 skip）
- `packages-user/data-base/src/enemy/manager.test.ts`（新增 4 朝向复用生成独立怪物的正确预期 skip）

Planning artifacts:
- `06-COVERAGE-MAP.md` create-or-append 06-12 小节
</artifacts_this_plan_produces>

<output>
完成时创建 `.planning/phases/06-unit-tests/06-12-SUMMARY.md`，以 create-or-append 方式写入 `06-COVERAGE-MAP.md` 06-12 小节；疑似 bug 追加到 `06-TEST-FINDINGS.md` 的 `#06-12-N` 小节（若有；受阻塞项复用既有 `#06-04-1`/`#06-04-2`/`#06-03-1` 锚点，不新建）。执行完成后暂停并用中文分条汇报。
</output>
