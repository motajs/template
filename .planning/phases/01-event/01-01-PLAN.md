---
phase: 01-event
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages-user/data-common/src/event/event.ts
  - packages-user/data-common/src/store/index.ts
  - packages-user/data-base/src/map/eventView.ts
  - packages-user/data-base/src/map/tile.ts
  - packages-user/data-base/src/map/staticTile.ts
  - packages-user/data-base/src/map/dynamicTile.ts
  - packages-user/data-base/src/map/mapLayer.ts
  - packages-user/data-base/src/map/mapState.ts
autonomous: false
requirements: [EVT-01, EVT-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "开发者能通过 IGameEvent 数据接口定义事件（EventTrigger 枚举，D-02），GameEvent.compile() 编译一次后写回 compiled 缓存（D-07）"
    - "GameEventStore 与 MapStore 均可通过桶导出 import { GameEventStore, MapStore } from '@user/data-common' 访问；事件存储走 id 引用、不进存档（D-11）"
    - "图块实例通过 tileEvent() 返回 ILayerEventView，通过 pointEvent() 返回所在位置的 ILayerEventView 或 null，对象+地图格混合绑定（D-01/D-03/D-04）"
    - "ILayerEventView 的 set/delete/clear/get/markPure/dirty 全部可用，set 遇到重复优先级走 logger.warn(136, ...)（D-06/D-12）"
    - "StaticTile/DynamicTile 的 saveState/loadState 读写 events（ReadonlyMap<number,string>）而非 triggers（IMapBlockSaveBase.events），图块默认事件同样走 eventStore id 引用（D-05）"
    - "MapLayer.event(x,y)/getPointEvent(x,y) 返回点事件视图，点事件不随图块移动（D-03）"
    - "MapState.fromRaw 从 raw.events 装配点位事件（不再读 blockData/trigger）"
  artifacts:
    - packages-user/data-base/src/map/eventView.ts
    - "store/index.ts 的 mapStore 桶导出"
  key_links:
    - "MapTileBase.tileEvent()/pointEvent() 连接 LayerEventView 与 IMapLayer.event(x,y)"
    - "StaticTile/DynamicTile.saveState/loadState 连接 tileEvent().dirty()/get()/set() 与 IMapBlockSaveBase.events"
    - "MapState.fromRaw 连接 IMapRawData.events 与 layer 静态图块 tileEvent().set()"
---

<objective>
把用户已设计的 L0/L1 事件数据接口落地为实现：L0 的事件编译缓存与存储桶导出，L1 的图块/点位事件视图、存读档迁移、点位事件装配。本计划不触碰 L2 事件执行器（执行器接口已由用户设计完成，Plan 02 直接实现）。

Purpose: 打通「事件定义 → 编译 → 图块/点位绑定 → 存读档」的 L0→L1 垂直切片，为 Plan 02 的执行器（实现用户已设计的 IGameEventExecutor/IGameEventSystem）与 Plan 03 的踩踏触发重写提供已落地的数据层骨架。
Output: `GameEvent.compile` 缓存回写、`store/index.ts` 导出 mapStore、`LayerEventView` 实现、`MapTileBase.tileEvent/pointEvent`、`StaticTile/DynamicTile` 存读档 `events`、`MapLayer.event/getPointEvent` + `syncStaticEvent`、`MapState.fromRaw` 从 `raw.events` 装配。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-event/01-CONTEXT.md
@.planning/phases/01-event/01-RESEARCH.md
@.planning/phases/01-event/01-PATTERNS.md

# 已设计接口（只读，禁止修改）
@packages-user/data-common/src/event/types.ts
@packages-user/data-common/src/event/event.ts
@packages-user/data-common/src/store/types.ts
@packages-user/data-common/src/store/eventStore.ts
@packages-user/data-common/src/store/mapStore.ts
@packages-user/data-base/src/map/types.ts
@packages-user/data-system/src/event/types.ts

# 本计划要修改/新建的实现文件
@packages-user/data-base/src/map/tile.ts
@packages-user/data-base/src/map/staticTile.ts
@packages-user/data-base/src/map/dynamicTile.ts
@packages-user/data-base/src/map/mapLayer.ts
@packages-user/data-base/src/map/mapState.ts
@packages-user/data-common/src/store/index.ts
@packages-user/data-common/src/event/event.ts
</context>

<tasks>

<task type="tracer">
  <name>端到端切片：事件「定义→编译→图块/点位绑定」路径跑通（L0 编译缓存 + L1 事件视图）</name>
  <files>
    packages-user/data-common/src/event/event.ts
    packages-user/data-common/src/store/index.ts
    packages-user/data-base/src/map/eventView.ts
    packages-user/data-base/src/map/tile.ts
    packages-user/data-base/src/map/mapLayer.ts
  </files>
  <read_first>
    - packages-user/data-common/src/event/types.ts（EventTrigger 枚举 + IGameEvent/IReadonlyGameEvent，只读）
    - packages-user/data-common/src/event/event.ts（GameEvent 现状）
    - packages-user/data-common/src/store/eventStore.ts（GameEventStore 现状）
    - packages-user/data-common/src/store/index.ts（桶导出现状：缺 mapStore）
    - packages-user/data-base/src/map/types.ts（IReadonlyEventView/ILayerEventView/ITileBase/IReadonlyTileBase/IMapLayer.event/getPointEvent/IMapBlockSaveBase.events，只读）
    - packages-user/data-base/src/map/tile.ts（MapTileBase 现状：triggers/block 待清）
    - packages-user/data-base/src/map/mapLayer.ts（现状：syncStaticTrigger/createDynamic/transferTo*）
    - .planning/phases/01-event/01-PATTERNS.md 的 Pattern 2/5/9 与「错误处理」段（logger 数字错误码、isNil、getOrInsertComputed）
  </read_first>
  <action>
    实现一条「事件定义到图块/点位绑定」的最小可运行路径，逐层如下（只实现已设计接口，不改 types.ts）：

    L0 — `packages-user/data-common/src/event/event.ts`：把 `GameEvent.compile()` 由「直接返回 `this.interpreter.compile(this.rawEvent)`」改为「先 `this.compiled = this.interpreter.compile(this.rawEvent)` 再 `return this.compiled`」，使 `execute()` 的 `if (this.compiled)` 分支真正命中缓存（setRaw 已正确置 `this.compiled = null`，勿改）。

    L0 — `packages-user/data-common/src/store/index.ts`：补一行桶导出 `export * from './mapStore';`（当前缺 mapStore，导致 core.ts 只能相对路径绕过）。保持现有 eventStore/itemStore/tileStore/types 导出不变。

    L1 — `packages-user/data-base/src/map/eventView.ts`（新建）：实现 `class LayerEventView implements ILayerEventView`，内部持有 `private readonly store: Map<number, string>` 与一个 `private reference: Map<number, string> | null`（dirty 基准，D-12）。方法：`get()` 返回 `ReadonlyMap`；`set(priority, event)` 遇 `store.has(priority)` 时 `logger.warn(136, priority.toString())` 后覆盖；`delete`/`clear` 直接操作 store；`markPure()` 把当前 store 内容拷贝进 reference；`dirty()` 在 reference 为 null 时对空 store 判断，否则逐项比对 store 与 reference。私有成员必须加 jsDoc（code.md 规则 8）。

    L1 — `packages-user/data-base/src/map/tile.ts`：删除 `triggers: Set<number> | null` 成员及 `addTrigger`/`deleteTrigger`/`clearTrigger`/`useEmptyTrigger`，新增 `private tileEvents: LayerEventView`（构造器初始化 `new LayerEventView()` 并以空 Map `markPure()`），实现 `tileEvent(): ILayerEventView` 返回该视图，实现 `pointEvent(): ILayerEventView | null` 委托 `this.layer.event(this.locator.x, this.locator.y)`。移除引用未定义类型 `IMapPointRawData` 的 `block()` 方法与对应 import（该类型已不在 @user/data-common，见「Blockers」）。`MapTileBase` 现需 `import { LayerEventView } from './eventView';` 且 `implements ITileBase` 应补齐 `tileEvent`/`pointEvent`。

    L1 — `packages-user/data-base/src/map/mapLayer.ts`：新增 `event(x, y): ILayerEventView | null` 与 `getPointEvent(x, y): ReadonlyMap<number, string> | null`。点事件视图需要一份图层级存储（点事件不随图块移动，D-03）：新增私有成员 `private readonly pointEvents: Map<number, Map<number, LayerEventView>>`（外层 key = y，内层 key = x），`event(x, y)` 惰性创建并返回 `ILayerEventView`（`getOrInsertComputed` 惯例）；`getPointEvent` 返回 `event(x, y)?.get() ?? null`。私有成员加 jsDoc。

    约束：禁止出现 `as` 关键字、禁止 `value === undefined`（用 `isNil`）、公共方法必须先在接口定义再 implements、对象成员用接口类型、所有私有成员加 jsDoc。
  </action>
  <acceptance_criteria>
    - `grep -c "mapStore" packages-user/data-common/src/store/index.ts` 命中 `export * from './mapStore'`
    - `grep -n "triggers" packages-user/data-base/src/map/tile.ts` 无 `Set<number>` 命中
    - `grep -n "tileEvent\|pointEvent" packages-user/data-base/src/map/tile.ts` 各至少命中一次
    - `grep -n "event(x: number, y: number)" packages-user/data-base/src/map/mapLayer.ts` 命中一次
    - `grep -n "getPointEvent(x: number, y: number)" packages-user/data-base/src/map/mapLayer.ts` 命中一次
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "eventView\.ts|map/tile\.ts|map/mapLayer\.ts|event/event\.ts|store/index\.ts"</automated>
    <fails_when>命中以上 L0/L1 文件的任何 TS 错误（有输出即失败）。staticTile/dynamicTile/mapState 的 triggers 迁移在 Task 2/3 处理，core.ts 缺失 eventStore/eventSystem、moverImpl 引用旧 trigger、client-modules、tileStore.getTrigger 等属跨计划/阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>一条事件从 IGameEvent 定义、经 compile 缓存、经 tileEvent/pointEvent 视图绑定到图块/点位的路径，类型层面成立；旧 triggers 成员已从 MapTileBase 移除。</done>
</task>

<task type="auto">
  <name>图块事件存读档：StaticTile/DynamicTile 从 triggers 迁移到 events</name>
  <files>
    packages-user/data-base/src/map/staticTile.ts
    packages-user/data-base/src/map/dynamicTile.ts
  </files>
  <read_first>
    - packages-user/data-base/src/map/types.ts（IMapBlockSaveBase.events / IStaticBlockSave / IDynamicBlockSave，只读）
    - packages-user/data-base/src/map/staticTile.ts（现状）
    - packages-user/data-base/src/map/dynamicTile.ts（现状）
    - packages-user/data-base/src/map/tile.ts（Task 1 改造后的 tileEvent/pointEvent）
  </read_first>
  <action>
    把两个类的存读档字段从旧 `triggers` 迁到 `events`（`IMapBlockSaveBase.events?: ReadonlyMap<number, string>`），结构保持不变、仅换字段与存取方式：

    `staticTile.ts`：`shouldSave()` 由 `return !!this.triggers` 改为 `return this.tileEvent().dirty();`；`saveState()` 由 `save.triggers = this.triggers` 改为「当 `this.tileEvent().dirty()` 为 true 时 `save.events = this.tileEvent().get()`」；`loadState(save)` 由 `save.triggers.forEach(v => this.addTrigger(v))` 改为「读 `save.events`：无则 `this.tileEvent().clear()`，有则遍历 `[priority, id]` 调用 `this.tileEvent().set(priority, id)`，读完 `this.tileEvent().markPure()`」。

    `dynamicTile.ts`：`saveState()` 同样把 `save.triggers = this.triggers` 改为 dirty 时 `save.events = this.tileEvent().get()`；`loadState(save)` 同上迁移到 `tileEvent().set/clear/markPure`。

    注意：`this.triggers` / `useEmptyTrigger` / `addTrigger` / `clearTrigger` 已在 Task 1 从 `MapTileBase` 移除，本任务不得再引用它们；`save` 对象字面量继续用接口类型 `IStaticBlockSave`/`IDynamicBlockSave`（其 `events` 为可选只读字段）。禁用 `as`、私有成员加 jsDoc。
  </action>
  <acceptance_criteria>
    - `grep -n "triggers" packages-user/data-base/src/map/staticTile.ts packages-user/data-base/src/map/dynamicTile.ts` 无命中
    - `grep -n "\.events" packages-user/data-base/src/map/staticTile.ts packages-user/data-base/src/map/dynamicTile.ts` 各至少命中一次
    - `grep -n "tileEvent()" packages-user/data-base/src/map/staticTile.ts packages-user/data-base/src/map/dynamicTile.ts` 各至少命中一次
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "map/staticTile\.ts|map/dynamicTile\.ts"</automated>
    <fails_when>命中 staticTile.ts/dynamicTile.ts 的任何 TS 错误（有输出即失败）。mapState 的迁移在 Task 3，core.ts/moverImpl/client-modules/tileStore.getTrigger 属跨计划/阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>StaticTile/DynamicTile 存读档写 `events` Map（dirty 门控），不再引用 `triggers` 或已删除的 trigger 方法。</done>
</task>

<task type="auto">
  <name>点位事件装配与图层事件同步：MapLayer 静态/动态转换 + MapState.fromRaw</name>
  <files>
    packages-user/data-base/src/map/mapLayer.ts
    packages-user/data-base/src/map/mapState.ts
  </files>
  <read_first>
    - packages-user/data-base/src/map/types.ts（IMapLayer.event/getPointEvent/transferToDynamic/transferToStatic 的 keepEvent 参数，只读）
    - packages-user/data-common/src/store/types.ts（IMapRawData.events 的 Record<z, Record<pos, Record<priority, id>>> 结构，只读）
    - packages-user/data-base/src/map/mapLayer.ts（Task 1 已加 event/getPointEvent，本任务补 syncStaticEvent/transferTo*）
    - packages-user/data-base/src/map/mapState.ts（现状：fromRaw 中 raw.blockData/data.trigger/addTrigger）
  </read_first>
  <action>
    `mapLayer.ts`（在 Task 1 已加 event/getPointEvent 基础上继续）：
    1. `createDynamic(num, x, y)`：把 `location.static.triggers` 的复制改为「从 `location.static.tileEvent()` 视图读取 id Map，逐项 `tile.tileEvent().set(priority, id)`」。
    2. `syncStaticTrigger(tile, keepTrigger)` 重命名为 `syncStaticEvent(tile, keepEvent)`：`keepEvent` 为 true 时把 `tile.tileEvent().get()` 逐项 `staticTile.tileEvent().set(priority, id)`，否则 `staticTile.tileEvent().clear()`；`transferToStatic`/`transferToStaticIfSafe` 同步改用 `keepEvent` 参数（参数名按 map/types.ts 已定义的 `keepEvent`，替换原 `keepTrigger`）。
    3. `transferToDynamic(x, y, keepEvent)` 中 `staticTile.clearTrigger()` 改为 `staticTile.tileEvent().clear()`，`tile.clearTrigger()` 改为 `tile.tileEvent().clear()`。

    `mapState.ts` `fromRaw`：把「读 `raw.blockData[z]` + `data.trigger` + `addTrigger`」整段替换为「读 `raw.events[z]`：对每层 z、每个 pos 索引、每个 `[priority, id]`，由 pos 索引算出 x/y（`index % raw.width` / `Math.floor(index / raw.width)`），经 `layer.getLocationData(x, y)` 拿到静态图块，调用 `static.tileEvent().set(priority, id)`」。

    禁用 `as`、`isNil` 判空、logger 数字错误码、私有成员 jsDoc。
  </action>
  <acceptance_criteria>
    - `grep -n "syncStaticTrigger" packages-user/data-base/src/map/mapLayer.ts` 无命中（已改名 syncStaticEvent）
    - `grep -n "blockData\|\.trigger\b" packages-user/data-base/src/map/mapState.ts` 无命中
    - `grep -n "raw\.events" packages-user/data-base/src/map/mapState.ts` 命中一次
    - `grep -n "keepEvent" packages-user/data-base/src/map/mapLayer.ts` 命中
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "map/mapLayer\.ts|map/mapState\.ts"</automated>
    <fails_when>命中 mapLayer.ts/mapState.ts 的任何 TS 错误（有输出即失败）。core.ts/moverImpl/client-modules/tileStore.getTrigger 属跨计划/阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>点事件视图经 `IMapLayer.event/getPointEvent` 落地，静态/动态转换同步 `events`，`MapState.fromRaw` 从 `raw.events` 装配点位事件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 地图原始数据 `IMapRawData.events` → 图块/点位事件绑定 | 优先级与事件 id 来自外部序列化，可能是未知/损坏的 id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|----------------|
| T-01-01 | Tampering | `LayerEventView.set(priority, id)` 收到重复优先级 | low | mitigate | `logger.warn(136, priority.toString())` 后覆盖，不抛异常 |
| T-01-02 | Tampering | `MapState.fromRaw` 读取 `raw.events` 中损坏的 pos 索引/优先级 | low | mitigate | `getLocationData(x,y)` 越界返回 null 时跳过；优先级/坐标为数值，`isNaN` 时走 logger 错误码跳过 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划无新依赖安装；`anon-tokyo@0.0.0-alpha.0` 为已锁定既有依赖（RESEARCH Package Legitimacy Audit 标记 SUS，仅提示不阻断） |
</threat_model>

<verification>
本计划验证聚焦「本计划涉及文件类型成立 + 旧 triggers 字段在本计划涉及文件内清零」。整体阶段绿门（`check:type` + 旧 ITrigger 引用清零）在 Plan 03 统一验收；执行器装配在 Plan 02 处理。
</verification>

<success_criteria>
- `GameEvent.compile()` 写回 `compiled` 缓存；`store/index.ts` 导出 `mapStore`
- `LayerEventView` 实现 `ILayerEventView`（含 dirty/markPure）
- `MapTileBase` 提供 `tileEvent()`/`pointEvent()`，移除 `triggers`/`block()`
- `StaticTile`/`DynamicTile` 存读档 `events`；`MapLayer.event/getPointEvent` + `syncStaticEvent` + `MapState.fromRaw` 落地
</success_criteria>

<output>
Create `.planning/phases/01-event/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `GameEvent.compile()` 缓存回写逻辑（修改 `data-common/src/event/event.ts`）
- `store/index.ts` 新增 `mapStore` 桶导出
- `LayerEventView` 类（`data-base/src/map/eventView.ts`，新建，implements `ILayerEventView`）
- `MapTileBase.tileEvent()` / `pointEvent()` 方法（`data-base/src/map/tile.ts`，替换 `triggers`/`addTrigger`/`clearTrigger`/`useEmptyTrigger`/`block`）
- `StaticTile.shouldSave/saveState/loadState`、`DynamicTile.saveState/loadState` 迁移到 `events`
- `MapLayer.event(x,y)` / `getPointEvent(x,y)` + `syncStaticEvent`（替换 `syncStaticTrigger`）
- `MapState.fromRaw` 从 `raw.events` 装配点事件

## Blockers / Deferred（非本计划落地项）

1. **执行器接口 `IGameEventExecutor`（A3 / D-10）**：已由用户设计完成（`data-system/src/event/types.ts` 中 `EventExecuteMode`/`IGameEventExecutor`/`IGameEventSystem`/`IGameEventBuiltinFunction` 均已定义），Plan 02 直接实现，无需 checkpoint。本计划不导出/实现执行器，仅在本计划 context 中把它列为只读接口。
2. **`IMapPointRawData`（A1）**：`tile.ts` 原 `block()` 引用的 `IMapPointRawData` 已不在 `@user/data-common`，本计划移除该残留方法与 import；点事件的承载字段已在 `IMapRawData.events`（三级结构）落地，本计划实现。
3. **`ITileStore.getTrigger(num): number[]`**：`store/types.ts:85` 接口仍声明 `getTrigger` 返回 `number[]`，但 `ITileRawData` 已改为 `events: Record<number, string>`，实现 `tileStore.ts:27-29` 读 `.trigger` 已失效。此为用户接口遗留，删除/改签名需用户决定，本计划不动 `store/types.ts` 与 `tileStore.ts`。
4. **内建函数清单（A2）**：本阶段完全不落地（已移出本阶段，归入收尾工作），本计划不涉及。
5. **移动四钩子 → EventTrigger 映射（A4）**：本计划不落地（Plan 03）。

## 错误码

- 复用：`136`（重复优先级）。执行器的 `171`（事件 id 未找到）在 Plan 02 与执行器装配一并落地。
