---
phase: 01-event
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - packages-user/data-state/src/hero/moverImpl.ts
autonomous: false
requirements: [EVT-02, EVT-03]

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "英雄移动的踩踏触发从旧 TriggerCollector/TriggerType 改为：moverImpl 收集点事件+图块事件、按 D-06（先点后块、各自优先级降序）排序后 await state.eventSystem.executor.execute(events[], param, env)（D-06/D-08）"
    - "enter 映射 EventTrigger.OnEnter、leave 映射 EventTrigger.OnLeave、hit 映射 EventTrigger.OnTouch（映射依据 OnTouch 注释，触碰触发，直接走入不触发）"
    - "moverImpl.ts 不再 import 任何 @user/data-system 的旧 trigger 符号（ITriggerCollector/ITriggerHandler/TriggerType），也不再访问 ILayerLocation.raw（改用 location.static.raw()）"
    - "全仓 packages-user 内 ITrigger/TriggerRegistry/TriggerCollector/TriggerCollection/BaseTrigger/TriggerType 引用清零（EventTrigger/BlockEventType 为合法新符号，除外）"
    - "pnpm check:type（本阶段涉及文件无报错）、pnpm check:circular、pnpm lint:user 全绿（阶段外残差另记）"
  artifacts:
    - packages-user/data-state/src/hero/moverImpl.ts
  key_links:
    - "DefaultHeroMoveTopImpl.commonTrigger 收集点事件+图块事件并按 D-06 排序后，连接 state.eventSystem.executor.execute(events[], param, env)"
    - "EventTrigger.OnEnter/OnLeave/OnTouch 连接移动四钩子 enter/leave/hit"
---

<objective>
把英雄移动的踩踏触发从旧触发器收集器重写为：moverImpl 收集点事件+图块事件并按 D-06 排序后，委托新事件执行器的 `execute(events[], param, env)` 执行，同时同步修复 moverImpl 对已变更 `ILayerLocation`（`raw` → `static.raw()`）的引用，完成事件系统与移动流程的对接，随后执行阶段级验收（旧体系引用清零 + 门禁）。

Purpose: EVT-02「事件系统能驱动踩踏触发链路」的关键集成点；重写后 `moverImpl` 只负责「收集 → D-06 排序 → 委托执行器」，返回值合并语义（EventExecuteMode）集中在执行器（Plan 02 已落地），同时让 `check:type` 中 moverImpl 相关报错清零。内建函数（对话/开门/道具/战斗）已移出本阶段，不在本计划范围。
Output: `moverImpl.ts` 重写（含 raw 迁移 + 收集/排序/委托 execute）+ 阶段级验证结论。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-event/01-CONTEXT.md
@.planning/phases/01-event/01-RESEARCH.md
@.planning/phases/01-event/01-PATTERNS.md
@.planning/phases/01-event/01-01-PLAN.md
@.planning/phases/01-event/01-02-PLAN.md
@.planning/phases/01-event/01-02-SUMMARY.md

@packages-user/data-state/src/hero/moverImpl.ts
@packages-user/data-base/src/hero/types.ts
@packages-user/data-base/src/map/types.ts
@packages-user/data-common/src/event/types.ts
@packages-user/data-system/src/event/types.ts
</context>

<tasks>

<task type="auto">
  <name>moverImpl 重写：raw 迁移 + 踩踏触发委托事件执行器（enter/leave/hit → EventTrigger）</name>
  <files>
    packages-user/data-state/src/hero/moverImpl.ts
  </files>
  <read_first>
    - packages-user/data-state/src/hero/moverImpl.ts（现状：canPass/shouldHit 用 location.raw，commonTrigger 用 collector）
    - packages-user/data-base/src/hero/types.ts（IHeroMoveTopImpl 的 enter/leave/hit/cannotEnter 契约，只读）
    - packages-user/data-base/src/map/types.ts（ILayerLocation.static.raw()，只读）
    - packages-user/data-common/src/event/types.ts（EventTrigger 枚举：OnEnter/OnLeave/OnTouch 注释，只读）
    - packages-user/data-system/src/event/types.ts（IGameEventExecutor.execute(events: string[], param, env): Promise<R> 签名 + EventExecuteMode，只读）
  </read_first>
  <action>
    重写 `packages-user/data-state/src/hero/moverImpl.ts`，分两部分：

    A. 修复已变更的 `ILayerLocation` 引用（`canPass`/`shouldHit`）：
       - `canPass` 中 `curr.raw.pass.outPass`/`next.raw.pass.inPass` 改为 `curr?.static.raw()?.pass.outPass`/`next?.static.raw()?.pass.inPass`（`ILayerLocation` 已无 `raw`，改经 `static.raw()` 取 `ITileRawData`，raw() 可能为 null，用 `isNil`/可选链判空后默认 true 可通行）。外层 `if (curr && curr.raw)` 相应改为对 `static.raw()` 结果判空。
       - `shouldHit` 中 `next.raw.eventPass` 改为 `next?.static.raw()?.eventPass`（raw 为 null 时返回 false）。

    B. 触发器行为改为「收集 + D-06 排序 → 委托执行器 execute」：
       1. 顶部 import：删除 `ITriggerCollector/ITriggerHandler/TriggerType`（来自 `@user/data-system`）；改为从 `@user/data-common` 引入 `EventTrigger`，从 `@user/data-base` 引入 `IBlockEventParam/IBlockEventEnv/BlockEventType`，从 `@user/data-system` 引入 `IGameEventExecutor`，从 `@motajs/common` 引入 `ITileLocator`；`state` 类型仍是 `IStateSystem`。
       2. 构造器：把 `private readonly collector: ITriggerCollector` 与 `this.collector = state.triggerCollector` 改为 `private readonly executor: IGameEventExecutor` 与 `this.executor = state.eventSystem.executor`。
       3. `commonTrigger` 改为签名 `commonTrigger(trigger: EventTrigger, handler: IHeroMoveTopHandler, heroLoc: ITileLocator, x: number, y: number): Promise<void>`，方法体（D-06：先点事件、后图块事件，各自 priority 降序）：
          - `if (isNil(handler.floorId)) return Promise.resolve();` → `const map = this.maps.getMap(handler.floorId); if (!map) return Promise.resolve(); const event = map.eventLayer; if (!event) return Promise.resolve();`
          - 收集点事件：`const point = event.getPointEvent(x, y);`（`ReadonlyMap<number,string> | null`）。
          - 收集图块事件：`const loc = event.getLocationData(x, y);`，`const tileEvents = loc?.static.tileEvent().get();`（`ReadonlyMap<number,string>`；`loc` 为 null 时按空处理）。
          - 排序合并：把 `point` 与 `tileEvents` 各自的 `[priority, id]` 条目按 priority 降序排成 id 数组，先点后块拼成 `const sortedIds: string[]`（D-06）。
          - 构造 `const param: IBlockEventParam = { custom: {} };` 与 `const env: IBlockEventEnv = { type: BlockEventType.TileEvent, trigger, heroLocator: heroLoc, triggerLocator: { x, y }, tile: loc?.static ?? null, layer: event, map };`
          - `await this.executor.execute<void>(sortedIds, param, env);`（执行器 `execute` 返回 `Promise<R>`，直接 `await`，D-09 长事件 await）。不再构造 `ITriggerHandler` 对象。
       4. 四个钩子改为映射（映射为规划期最佳解读，A4 已延后确认，见「Blockers」）：
          - `enter(handler)` → `this.commonTrigger(EventTrigger.OnEnter, handler, handler.nextLoc, handler.nextLoc.x, handler.nextLoc.y)`
          - `leave(handler)` → `this.commonTrigger(EventTrigger.OnLeave, handler, handler.currLoc, handler.currLoc.x, handler.currLoc.y)`
          - `hit(handler)` → `this.commonTrigger(EventTrigger.OnTouch, handler, handler.currLoc, handler.nextLoc.x, handler.nextLoc.y)`
          - `cannotEnter(handler)` → `return Promise.resolve();`（新 EventTrigger 无 CannotEnter 对应，A4 已延后，先空实现并加 jsDoc 注明）

    约束：禁用 `as`；`isNil`/可选链判空；公共方法不新增（接口契约未变）；保留 4 钩子的 async 签名与返回类型。
  </action>
  <acceptance_criteria>
    - `grep -n "TriggerType\|ITriggerCollector\|ITriggerHandler\|triggerCollector" packages-user/data-state/src/hero/moverImpl.ts` 无命中
    - `grep -n "\.raw\b" packages-user/data-state/src/hero/moverImpl.ts` 无命中（已改 static.raw()）
    - `grep -n "EventTrigger.OnEnter\|EventTrigger.OnLeave\|EventTrigger.OnTouch" packages-user/data-state/src/hero/moverImpl.ts` 各命中一次
    - `grep -n "getPointEvent\|tileEvent()\|executor.execute" packages-user/data-state/src/hero/moverImpl.ts` 命中（收集点事件/图块事件并委托 execute）
    - `grep -n "eventSystem\|executor" packages-user/data-state/src/hero/moverImpl.ts` 命中
    - `pnpm check:type` 中无 moverImpl.ts 报错
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "hero/moverImpl\.ts"</automated>
    <fails_when>命中 hero/moverImpl.ts 的任何 TS 错误（有输出即失败）。client-modules、tileStore.getTrigger 属阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>moverImpl 三个移动钩子（enter/leave/hit）收集点事件+图块事件并按 D-06 排序后委托 executor.execute，踩踏触发走 OnEnter/OnLeave/OnTouch，不再引用旧 trigger 符号或 ILayerLocation.raw。</done>
</task>

<task type="auto">
  <name>阶段验收：旧体系引用清零 + 门禁</name>
  <files>
    packages-user/data-state/src/hero/moverImpl.ts
  </files>
  <read_first>
    - .planning/phases/01-event/01-RESEARCH.md 的「Validation Architecture → Phase gate」
    - .planning/phases/01-event/01-01-PLAN.md 与 01-02-PLAN.md 的 must_haves
  </read_first>
  <action>
    执行阶段级验收（本任务无源码改动，只跑门禁并记录结论）：

    1. 旧体系引用清零：`grep -rn "ITrigger\|TriggerRegistry\|TriggerCollector\|TriggerCollection\|BaseTrigger" packages-user` 应为空；`grep -rn "\bTriggerType\b" packages-user` 应为空（旧枚举已全删）。注意：`EventTrigger`（新枚举，data-common/src/event/types.ts）与 `BlockEventType`（data-base/src/map/types.ts）是合法符号，不得被误判为残留。

    2. `pnpm check:type`：本阶段涉及文件（eventView/tile/staticTile/dynamicTile/mapLayer/mapState/event/executor/system/types/index/core/moverImpl）应无报错；阶段外残差（client-modules 的 HeroMover/degradeFace/FaceDirection 等、tileStore.getTrigger）记入 SUMMARY 的 blockers，注明「非本阶段引入」。

    3. `pnpm check:circular`：无新增循环依赖（event 执行器只依赖 IStateBase，不可反向）。

    4. `pnpm lint:user`：packages-user 无 lint 错误（eventView/executor/moverImpl 等新文件符合 code.md）。

    把四项结论（命令 + 通过/失败 + 残差）写进 01-03-SUMMARY.md。若 check:type 出现非 mover 残留的既有错误，不静默忽略——在 SUMMARY 的 blockers 记录并注明「非本阶段引入」。
  </action>
  <acceptance_criteria>
    - 旧 trigger 符号 grep 三组命令输出均为空
    - 本阶段涉及文件在 `pnpm check:type` 中无报错（阶段外残差记录在案）
    - `pnpm check:circular` 退出码 0
    - `pnpm lint:user` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:circular; if ($?) { pnpm lint:user }</automated>
    <fails_when>任一命令非零退出（check:type 的全仓退出码因阶段外 client-modules/tileStore 残差可能非零，故以「本阶段文件无报错」为准，见 Task 1 的 verify）</fails_when>
  </verify>
  <done>阶段验收通过：旧 ITrigger 体系无残留，事件系统骨架类型（本阶段文件）/循环依赖/lint 门禁通过，阶段外残差已在 SUMMARY 记录。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 英雄移动钩子 → 事件执行器 → `Statement[]` 执行 | 移动触发的图块/点事件数据为不可信输入，执行器须容忍未知 id 与非法参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|----------------|
| T-03-01 | Tampering | `moverImpl` 委托执行器触发含未知事件 id 的图块 | low | mitigate | 执行器 `getEvent` 为 null 时 `logger.warn(171)` 跳过，不阻断移动 |
| T-03-02 | DoS | 踩踏触发执行长事件阻塞移动循环 | low | accept | D-09 长事件 `await` 为既定语义，属设计范围，不序列化执行状态 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划无新依赖安装；`anon-tokyo` 已锁定（SUS，仅提示） |
</threat_model>

<verification>
阶段门禁：`grep` 旧 trigger 符号三组命令均为空；`pnpm check:circular` + `pnpm lint:user` 全绿；`check:type` 以「本阶段文件无报错」为准（阶段外 client-modules/tileStore 残差记录）。
</verification>

<success_criteria>
- 踩踏触发（enter/leave/hit）经事件执行器驱动
- 旧 ITrigger 体系在全仓无残留引用
- moverImpl 不再访问 ILayerLocation.raw
- 阶段门禁通过（本阶段文件类型成立 + circular + lint），阶段可进入 review/transition
</success_criteria>

<output>
Create `.planning/phases/01-event/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `moverImpl.ts` 踩踏触发重写：`commonTrigger(EventTrigger, handler, heroLoc, x, y)` 收集点事件+图块事件、按 D-06 排序、委托 `executor.execute(events[], param, env)` + `raw` → `static.raw()` 迁移
- 阶段验收结论（旧体系引用清零 + 门禁），记录于 01-03-SUMMARY.md

## Blockers / Deferred

1. **移动四钩子 → EventTrigger 映射（A4，已延后）**：`enter→OnEnter`/`leave→OnLeave`/`hit→OnTouch` 为规划期最佳解读（依据 OnTouch 注释），`cannotEnter` 无 EventTrigger 对应（空实现）。OnTouch/OnEnter 边界与 cannotEnter 去留须用户后续确认。
2. **内建函数（对话/开门/道具/战斗，A2，已移出本阶段）**：EVT-02 的「对话/开门」端到端验证不在本阶段，归入收尾工作。
3. **执行器 sync-R vs 异步事件（已解决）**：`execute` 已按用户批准改为返回 `Promise<R>`（Plan 02 在 `event/types.ts` 落地签名变更），moverImpl 以 `execute<void>` 消费并 `await`（D-09 长事件 await）。
4. **执行器 env 的 `type`/heroLocator 精确值**：`type` 暂统一为 `BlockEventType.TileEvent`（踩踏触发以图块为触发位置）；`heroLocator` 已按 enter/hit=nextLoc、leave=currLoc 传入，`triggerLocator={x,y}`。接入 hero.location 后如需区分点事件类型再核对（不阻塞骨架）。

## 错误码

- 复用 `171`（执行器未找到事件 id）——Plan 02 已落地文案。
