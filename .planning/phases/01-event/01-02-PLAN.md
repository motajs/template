---
phase: 01-event
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages-user/data-system/src/event/types.ts
  - packages-user/data-system/src/event/executor.ts
  - packages-user/data-system/src/event/system.ts
  - packages-user/data-system/src/event/index.ts
  - packages-user/data-system/src/index.ts
  - packages-user/data-system/src/types.ts
  - packages-user/data-state/src/core.ts
  - packages/common/src/logger.json
files_deleted:
  - packages-user/data-system/src/trigger/types.ts
  - packages-user/data-system/src/trigger/trigger.ts
  - packages-user/data-system/src/trigger/registry.ts
  - packages-user/data-system/src/trigger/collector.ts
  - packages-user/data-system/src/trigger/collection.ts
  - packages-user/data-system/src/trigger/index.ts
  - packages-user/data-state/src/content/triggers.ts
autonomous: false
requirements: [EVT-01, EVT-02, EVT-03]

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "data-system/src/event/executor.ts 实现 IGameEventExecutor 接口：execute(events: string[], param, env): Promise<R> 接收预先收集的事件 id 数组，await 每个事件后按 EventExecuteMode 合并返回值（D-10 专门对象；execute 返回 Promise<R> 为用户批准的接口变更）"
    - "data-system/src/event/system.ts 实现 IGameEventSystem：executor + store + useStore() 完整装配，不出现只有 { executor } 的残缺装配"
    - "IStateSystem 不再含 triggerRegistry/triggerCollector，保留已有 eventSystem（IGameEventSystem）成员（D-13）"
    - "CoreState 实例化 GameEventStore 赋值 this.eventStore，装配完整 eventSystem（含 executor/store/useStore）并赋值 this.eventSystem"
    - "data-system/src/trigger/ 目录与 data-state/src/content/triggers.ts（ChangeFloorTrigger）已删除"
    - "@user/data-system 桶导出 event 执行器与事件系统（export * from './event'）"
    - "logger.json 新增错误码 171 文案，执行器取回未知事件 id 时 logger.warn(171, id) 而非 throw（D-08）"
  artifacts:
    - packages-user/data-system/src/event/types.ts
    - packages-user/data-system/src/event/executor.ts
    - packages-user/data-system/src/event/system.ts
    - "IStateSystem 移除 trigger 成员（data-system/src/types.ts）"
    - "删除清单（files_deleted）全部移除"
  key_links:
    - "IGameEventSystem.executor 连接 EventExecutor 实现与 CoreState.eventSystem 装配（含 store/useStore）"
    - "EventExecutor.execute 连接 storeRef().getEvent(id) 与 await GameEvent.execute(param, env)，按 mode 合并返回值"
    - "data-system/src/index.ts 桶导出连接 @user/data-system 消费方（moverImpl/combat）"
---

<objective>
实现 `IGameEventExecutor`/`IGameEventSystem` 接口（D-10 专门对象）并接入 L2/L3 装配，同时删除旧 `ITrigger` 体系（D-13）。接口本身仅做一处用户批准的单行变更——`IGameEventExecutor.execute` 返回类型由 `R` 改为 `Promise<R>`（消除 sync-R vs async 事件的 tension），其余接口成员一律不动。`mode`/`store` 两个 `readonly` 接口成员以普通类字段实现（`.agents/code.md` 禁用 getter），仅经 `setMode`/`useStore` 变更。本计划不实现任何内建函数（对话/开门/道具/战斗）——内建函数已移出本阶段，归入收尾工作。

> **Scope note（回应 scope_sanity）：** 本计划文件操作中，7 个为机械目录删除（`data-system/src/trigger/` 整目录 6 文件 + `data-state/src/content/triggers.ts`），不含逻辑改写；另含 1 个用户批准的单行接口签名变更（`event/types.ts` 的 `execute` → `Promise<R>`），其余为常规实现/装配/桶导出改动。

Purpose: 完成「旧体系删除 + 新执行器接入」的切换，使 `@user/data-system` 只暴露事件执行器接口，`CoreState` 完整实现 `IStateSystem`（含 `eventStore` 与 `eventSystem`）；这是 EVT-02（踩踏触发链路，由 Plan 03 的 moverImpl 收集+排序后调用 executor.execute）与 EVT-03（初学者抽象、无旧残留）的执行器骨架。
Output: `EventExecutor`（implement `IGameEventExecutor`，execute 返回 `Promise<R>`）、`GameEventSystem`（implement `IGameEventSystem`）、`data-system/src/event/types.ts` 单行签名变更（execute → Promise<R>）、`data-system/src/event/index.ts` + `src/index.ts` 桶导出、`IStateSystem` 移除 trigger 成员、`CoreState` 装配 eventStore/eventSystem（含 store/useStore）、trigger/ 目录与 content/triggers.ts 删除、logger.json 171。
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-event/01-CONTEXT.md
@.planning/phases/01-event/01-RESEARCH.md
@.planning/phases/01-event/01-PATTERNS.md
@.planning/phases/01-event/01-01-PLAN.md

@packages-user/data-system/src/types.ts
@packages-user/data-system/src/index.ts
@packages-user/data-system/src/event/types.ts
@packages-user/data-system/src/event/index.ts
@packages-user/data-system/src/trigger/types.ts
@packages-user/data-system/src/trigger/collector.ts
@packages-user/data-system/src/trigger/collection.ts
@packages-user/data-system/src/trigger/registry.ts
@packages-user/data-system/src/trigger/trigger.ts
@packages-user/data-state/src/content/triggers.ts
@packages-user/data-state/src/core.ts
@packages-user/data-common/src/event/types.ts
@packages-user/data-base/src/map/types.ts
@packages/common/src/logger.json
</context>

<tasks>

<task type="auto">
  <name>实现 EventExecutor + GameEventSystem（implement 已有接口，execute 返回 Promise<R>）+ event 桶导出 + logger 错误码 171</name>
  <files>
    packages-user/data-system/src/event/types.ts
    packages-user/data-system/src/event/executor.ts
    packages-user/data-system/src/event/system.ts
    packages-user/data-system/src/event/index.ts
    packages-user/data-system/src/index.ts
    packages/common/src/logger.json
  </files>
  <read_first>
    - packages-user/data-system/src/event/types.ts（EventExecuteMode/IGameEventBuiltinFunction/IGameEventExecutor/IGameEventSystem；本计划仅把 IGameEventExecutor.execute 返回类型改为 Promise<R>（用户批准），其余成员只读、禁止修改）
    - packages-user/data-system/src/event/index.ts（现状：仅 export * from './types'）
    - packages-user/data-system/src/index.ts（现状：combat/trigger/types）
    - packages-user/data-common/src/event/types.ts（IReadonlyGameEvent.execute(param, env): Promise<R>，只读）
    - packages-user/data-common/src/store/types.ts（IGameEventStore.getEvent，只读）
    - packages-user/data-base/src/map/types.ts（IBlockEventParam/IBlockEventEnv/BlockEventType，只读）
    - packages-user/data-base/src/types.ts（IStateBase/IDataBaseExtended，只读）
    - .planning/phases/01-event/01-PATTERNS.md Pattern 1/2 与「错误处理」段（logger 数字错误码、isNil）
  </read_first>
  <action>
    实现 `IGameEventExecutor` 与 `IGameEventSystem` 接口。除下述用户批准的单行变更（`execute` 返回 `Promise<R>`）外，不得新增/修改其他公共接口签名。执行器接收「预先收集好的事件 id 数组」，不负责收集事件（收集 + D-06 排序是 Plan 03 moverImpl 的职责）：

    0. `packages-user/data-system/src/event/types.ts`（用户批准的单行接口变更）：把 `IGameEventExecutor.execute` 的返回类型由 `R` 改为 `Promise<R>`（`execute<R = void>(events: string[], param: IBlockEventParam, env: IBlockEventEnv): R` → `...: Promise<R>`），其余接口成员（`mode`/`interpreter`/`setMode` 及整个 `IGameEventSystem`）一概不动。

    1. `packages-user/data-system/src/event/executor.ts`（新建）：实现 `class EventExecutor implements IGameEventExecutor`。
       - 构造器 `constructor(readonly interpreter: AnonTokyoInterpreter, private readonly storeRef: () => IGameEventStore | null)`：`interpreter` 为公共只读字段；`storeRef` 是惰性取 store 的回调，供 `execute` 解析事件 id（避免与 `IGameEventSystem.useStore` 换 store 后脱节）。
       - 用普通类字段满足接口的 `readonly mode`（`.agents/code.md` 禁用 getter，不用 `get mode()`）：`mode: EventExecuteMode = EventExecuteMode.ListReturn;`（普通非 readonly 字段即可满足 `readonly mode` 接口成员），`setMode(mode)` 直接赋值 `this.mode = mode`。
       - `async execute<R = void>(events: string[], param: IBlockEventParam, env: IBlockEventEnv): Promise<R>`：遍历 `events`，对每个 id 调 `storeRef()?.getEvent(id)`；返回 null 时 `logger.warn(171, id)` 后跳过（D-08，绝不 throw）；命中则 `await event.execute(param, env)` 取到实际返回值。按 `this.mode` 合并各事件 `await` 后的返回值：
          - `ListReturn`：收集所有返回值组成数组作为整体返回值。
         - `OrReduce`：顺序短路取或；遇非布尔值按短路运算符规则输出并 `logger.warn`。
         - `AndReduce`：顺序短路取与；非布尔值同上。
         - `CutIfFalsy`：任意事件返回 falsy 立即停止，把前面返回值组成数组返回。
        - 返回值：`execute` 为 `async`，最终 `return` 按 `this.mode` 合并后的结果（`ListReturn`/`CutIfFalsy` 返回数组，`OrReduce`/`AndReduce` 返回短路运算结果），类型满足 `Promise<R>`。
       - 约束：禁用 `as`；`isNil` 判空；公共方法先定义于接口再 implements；私有成员加 jsDoc；本阶段不实现任何内建函数。

    2. `packages-user/data-system/src/event/system.ts`（新建）：实现 `class GameEventSystem implements IGameEventSystem`。
       - `readonly state: IStateBase`（构造器注入，满足 `IDataBaseExtended`）。
       - `readonly executor: IGameEventExecutor`：构造时 `new EventExecutor(interpreter, () => this.store)`，`interpreter` 用 `new AnonTokyoInterpreter({ builtInFunctions: [], globalFunctions: [] })`（内建函数本阶段留空骨架）。
       - 用普通类字段满足接口的 `readonly store`（`.agents/code.md` 禁用 getter，不用 `get store()`）：`store: IGameEventStore | null;`（普通非 readonly 字段即可满足 `readonly store` 接口成员）；`useStore(store)` 直接赋值 `this.store = store`（executor 的 `storeRef` 闭包随之读到新 store）。构造时 `this.store = state.eventStore`。
       - 约束同上。

    3. `packages-user/data-system/src/event/index.ts`：追加 `export * from './executor';` 与 `export * from './system';`（保留 `export * from './types';`）。

    4. `packages-user/data-system/src/index.ts`：把 `export * from './trigger';` 替换为 `export * from './event';`（保留 `export * from './combat';` 与 `export * from './types';`）。

    5. `packages/common/src/logger.json`：在 `warn` 段追加 `"171": "Event id '$1' not found in event store, event will be skipped."`（英文文案，沿用现有风格）。
  </action>
  <acceptance_criteria>
    - `grep -n "class EventExecutor" packages-user/data-system/src/event/executor.ts` 命中
    - `grep -n "class GameEventSystem" packages-user/data-system/src/event/system.ts` 命中
    - `grep -n "Promise<R>" packages-user/data-system/src/event/types.ts` 命中（execute 返回 Promise<R>，用户批准的单行接口变更）
    - `grep -n "execute<R" packages-user/data-system/src/event/executor.ts` 命中（签名为 `events: string[]`，返回 `Promise<R>`）
    - `grep -n "get mode\|get store" packages-user/data-system/src/event/executor.ts packages-user/data-system/src/event/system.ts` 无命中（code.md 禁用 getter，mode/store 用普通字段）
    - `grep -n "export \* from './executor'" packages-user/data-system/src/event/index.ts` 命中
    - `grep -n "export \* from './system'" packages-user/data-system/src/event/index.ts` 命中
    - `grep -n "export \* from './event'" packages-user/data-system/src/index.ts` 命中
    - `grep -n "171" packages/common/src/logger.json` 命中一次（warn 段）
    - `Test-Path packages-user/data-system/src/event/builtin.ts` 返回 False（本阶段不实现内建函数）
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "event/types\.ts|event/executor\.ts|event/system\.ts|event/index\.ts|data-system/src/index\.ts"</automated>
    <fails_when>命中 event/types.ts、event/executor.ts、event/system.ts、event/index.ts、data-system/src/index.ts 的任何 TS 错误（有输出即失败）。IStateSystem/core.ts 的 trigger 成员删除在 Task 2，moverImpl 引用旧 trigger、client-modules、tileStore.getTrigger 属跨计划/阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>EventExecutor 实现 IGameEventExecutor（execute 返回 Promise<R>，await 每个事件并按 mode 合并返回值，mode 为普通字段由 setMode 赋值），GameEventSystem 实现 IGameEventSystem（executor+store+useStore，store 为普通字段由 useStore 赋值），`@user/data-system` 桶导出 event，错误码 171 文案可用。</done>
</task>

<task type="auto">
  <name>删除旧 ITrigger 体系 + CoreState 装配完整 eventSystem（executor+store+useStore）（D-13）</name>
  <files>
    packages-user/data-system/src/types.ts
    packages-user/data-state/src/core.ts
  </files>
  <read_first>
    - packages-user/data-system/src/types.ts（IStateSystem 现状：triggerRegistry/triggerCollector + eventSystem）
    - packages-user/data-system/src/index.ts（Task 1 已切 trigger→event）
    - packages-user/data-system/src/event/types.ts 与 event/system.ts、event/executor.ts（Task 1 产出）
    - packages-user/data-state/src/core.ts（现状 L0/L2 初始化区）
    - packages-user/data-common/src/store/eventStore.ts（GameEventStore，只读）
    - packages-user/data-common/src/types.ts（IDataCommon.eventStore，只读）
    - packages-user/data-state/src/hero/moverImpl.ts（确认其 import 的 TriggerType/ITriggerCollector/ITriggerHandler 将在本计划移除后失效，Plan 03 重写）
  </read_first>
  <action>
    1. 删除 `packages-user/data-system/src/trigger/` 整个目录（types.ts/trigger.ts/registry.ts/collector.ts/collection.ts/index.ts，共 6 文件），删除 `packages-user/data-state/src/content/triggers.ts`（ChangeFloorTrigger 及其自引用的 TriggerType 枚举）。这些文件中的 `TriggerType`（Enter/Leave/Hit/CannotEnter 与 ChangeFloor）均为旧体系，D-13 要求一并删除。

    2. `packages-user/data-system/src/types.ts`：`IStateSystem` 删除 `triggerRegistry: ITriggerRegistry` 与 `triggerCollector: ITriggerCollector` 两个成员及其 `import { ITriggerCollector, ITriggerRegistry } from './trigger';`。保留已有 `eventSystem: IGameEventSystem`（其 import 已存在）。

    3. `packages-user/data-state/src/core.ts`：
       - L0 区新增 `const eventStore = new GameEventStore(); this.eventStore = eventStore;` 并补 `import { GameEventStore } from '@user/data-common';`（同时把第 79 行 `import { MapStore } from '../../data-common/src/store/mapStore'` 改为桶导出 `import { MapStore } from '@user/data-common';`，Plan 01 已补 mapStore 桶导出）。
       - L2 区删除 `ITriggerCollector/ITriggerRegistry/TriggerCollector/TriggerRegistry` import（第 42-46 行）、`triggerRegistry`/`triggerCollector` 成员声明（第 98-99 行）、装配段（第 202-207 行），改为 `const eventSystem = new GameEventSystem(this); this.eventSystem = eventSystem;` 并补 `import { GameEventSystem } from '@user/data-system';`。注意装配的是完整 `IGameEventSystem`（含 executor/interpreter/mode/setMode 与 store/useStore），绝不装配 `{ executor }` 单独对象。

    约束：禁用 `as`；删除后 `grep` 在 `packages-user/data-system` 与 `data-state/src/{core.ts,content}` 内不得残留旧符号；`EventTrigger`（新枚举）与 `BlockEventType`（新枚举）不受影响。
  </action>
  <acceptance_criteria>
    - `grep -rn "ITrigger\|TriggerRegistry\|TriggerCollector\|TriggerCollection\|BaseTrigger\|TriggerType" packages-user/data-system packages-user/data-state/src/core.ts packages-user/data-state/src/content 2>$null` 无命中（注意 `EventTrigger`/`BlockEventType` 不属匹配目标）
    - `Test-Path packages-user/data-system/src/trigger` 返回 False
    - `Test-Path packages-user/data-state/src/content/triggers.ts` 返回 False
    - `grep -n "eventStore" packages-user/data-state/src/core.ts` 命中
    - `grep -n "eventSystem" packages-user/data-system/src/types.ts packages-user/data-state/src/core.ts` 各命中
    - `grep -n "GameEventSystem" packages-user/data-state/src/core.ts` 命中（装配完整系统，而非 `{ executor }`）
  </acceptance_criteria>
  <verify>
    <automated>pnpm check:type 2>&1 | Select-String -Pattern "data-system/src/types\.ts|data-state/src/core\.ts"</automated>
    <fails_when>命中 data-system/types.ts、data-state/core.ts 的任何 TS 错误（有输出即失败）。moverImpl 引用旧 trigger、client-modules、tileStore.getTrigger 属跨计划/阶段外残差，不计入本任务失败</fails_when>
  </verify>
  <done>旧 ITrigger 体系删除完毕，`@user/data-system` 只暴露 event 执行器与事件系统，`CoreState` 装配 `eventStore` + 完整 `eventSystem`（executor+store+useStore）并完整实现 IStateSystem。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 blockly 编辑器 → `Statement[]` → AnonTokyoInterpreter | 事件数据为不可信输入，执行器须容忍未知 id 与非法参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|----------------|
| T-02-01 | Tampering | `EventExecutor` 对 `getEvent(id)` 返回 null 的未知 id | low | mitigate | `logger.warn(171, id)` 跳过，绝不 throw 中断游戏 |
| T-02-02 | DoS | 恶意/损坏事件引用被删除的旧 trigger 概念 | low | accept | D-13 删除后旧体系无残留，无对应执行面 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本计划无新依赖；`anon-tokyo` 为已锁定 SUS 依赖，仅提示不阻断 |
</threat_model>

<verification>
本计划切换后，`check:type` 的预期残差仅剩 `moverImpl.ts`（仍 import 已删除的旧 trigger 符号，Plan 03 重写）与阶段外残差（client-modules、tileStore.getTrigger）。完整 eventSystem 装配、IStateSystem、CoreState、删除项在 `check:type` 中应无新增错误。
</verification>

<success_criteria>
- 旧 ITrigger 体系（trigger/ 6 文件 + content/triggers.ts）全部删除，无残留引用
- `EventExecutor` 实现 `IGameEventExecutor`（`execute` 返回 `Promise<R>`，`await` 每个事件并按 mode 合并返回值，`mode` 为普通字段由 `setMode` 赋值）；`GameEventSystem` 实现 `IGameEventSystem`（executor + store + useStore，`store` 为普通字段）
- `IStateSystem` 只含 `eventSystem`（无 triggerRegistry/triggerCollector）
- `CoreState` 装配 `eventStore` + 完整 `eventSystem`，完整实现 IStateSystem
- logger.json 171 就位；无内建函数实现（内建函数移出本阶段）
</success_criteria>

<output>
Create `.planning/phases/01-event/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- 删除：`data-system/src/trigger/`（6 文件）、`data-state/src/content/triggers.ts`（ChangeFloorTrigger + ChangeFloor TriggerType）
- `data-system/src/event/types.ts` 单行变更：`IGameEventExecutor.execute` 返回 `Promise<R>`（用户批准）
- `data-system/src/event/executor.ts`（EventExecutor，实现 IGameEventExecutor；execute 返回 Promise<R>，mode 为普通字段由 setMode 赋值）
- `data-system/src/event/system.ts`（GameEventSystem，实现 IGameEventSystem；store 为普通字段由 useStore 赋值）
- `data-system/src/event/index.ts` 追加 executor/system 导出；`src/index.ts` 桶导出 trigger→event
- `IStateSystem` 移除 trigger 成员（保留 eventSystem）
- `CoreState.eventStore` + `CoreState.eventSystem` 装配（删除 TriggerRegistry/TriggerCollector，装配完整 eventSystem）
- `logger.json` 错误码 171

## Blockers / Deferred

1. **内建函数清单（A2，用户 deferred）**：对话/开门/道具/战斗的具体 `BuiltInFunction` 清单与命名已移出本阶段，归入收尾工作。本计划不实现、不新建 builtin 文件；`AnonTokyoInterpreter` 以空 `LanguageFeature`（builtInFunctions/globalFunctions 均空）构造。
2. **执行器 sync-R vs 异步事件（已解决）**：`IGameEventExecutor.execute` 已按用户批准改为返回 `Promise<R>`（本计划在 `event/types.ts` 落地该单行签名变更），执行器 `async execute` `await` 每个事件并按 mode 合并返回值。原「sync-R 声明 vs async 事件」的 tension 已消除。
3. **moverImpl.ts 暂时编译失败**：删除旧 trigger 导出后 `moverImpl.ts` 的 import 失效，属预期过渡态，Plan 03 重写修复（重写后负责收集 + D-06 排序并调用 executor.execute）。

## 错误码

- 新增：`171`（事件 id 未在 eventStore 中找到）——文案在本计划落地。
