# AI Agent 指南

在开始前，务必认真阅读 [dev.md](./dev.md) 来了解项目基本要求。

作为 AI Agent，你还需要遵循如下要求：

## 额外要求

- 不得擅自修改所有 jsDoc 注释，不得擅自修改任何已有注释，但是这不意味着不让你写注释，只是不得修改现有的，新增注释随便新增，但必须遵循项目要求。
- dev.md 中所有关于建议做的事、最好怎么做的事，都视为必须这么做；而关于不建议怎么做的事，尽量不怎么做的事，都视为必须不能这么做。
- 如果发现任何问题，永远不要擅自将其视为问题并记录，必须经由我确认其为问题后才可记录。你认为是问题的地方很多时候是设计如此。
- 测试文件不必遵循上述规则，只需要遵循一个规则：每个测试前（it）都必须用一行注释说明这个测试是干什么的。测试文件全部由你维护，我一般不会动。
- 任何计划的执行，其**第一个 block 点必须是向我汇报计划内容并等待我确认**；无论触发方式是什么（包括但不限于 `/gsd-execute-phase`、`/gsd-manager`、`/gsd-autonomous`、子代理/执行器派发），都不得例外，不得擅自执行。只有在我明确回复「可以执行」这四个字之后，才允许开始执行该计划；没有「可以执行」四个字，任何计划的任何一步都不得执行，也不得派发执行子代理。子代理/执行器同样受此约束，遇到该关卡必须停下并等待。汇报时说明计划准备解决什么问题，大致的解决方案是什么；如果是修复问题的计划，那么应该还需要汇报问题是什么。
- 除非我明确要求，否则任何修改必须经由 GSD 标准流程：Plan -> Execute -> Verify，不得自行修改。
- 任何公共、受保护、私有方法、字段等内容的命名必须先向我反馈，我同意后才可以使用，不得擅自使用我未同意的命名。
- 在 GSD 流程的 Discuss 讨论阶段中，全部由我主导，而不是由你主导。进入讨论阶段的第一件事不是开子代理探索这一阶段的需求，也不是自己分析需求并提问问题，而是让我先说这一阶段的计划。
